# DoKey — Открытые вопросы и допущения

Документ ведётся с этапа дискавери. Каждый вопрос закрывается решением, данными или
экспериментом; закрытые переносятся в раздел «Закрытые» с датой и ссылкой на решение.

- **Дата создания:** 2026-09-02 · **Последнее обновление:** 2026-09-22
- **Источник этой итерации:** интервью с product owner (2 раунда) + делегированные решения
  + рыночное исследование [research.md](research.md) (Q-23…Q-31)
  + полный разбор всех открытых вопросов 2026-09-02/03 (Q-32…Q-43, решения D-07…D-34)
  + написание [vision.md](vision.md) 2026-09-03 (Q-44…Q-46)
  + написание [brd.md](brd.md) 2026-09-03 (Q-47…Q-55, допущения A-11…A-15; A-13 и A-14 выведены D-42 и D-43)
  + написание [prd.md](prd.md) 2026-09-03 (Q-56…Q-61)
  + написание [glossary.md](glossary.md) 2026-09-03 (Q-62…Q-67)
  + написание [user-flows.md](user-flows.md) 2026-09-03 (Q-68…Q-88)
  + написание [user-stories.md](user-stories.md) 2026-09-03 (Q-89…Q-97)
  + написание [backlog.md](backlog.md) 2026-09-03 (Q-98…Q-103)
  + написание [roadmap.md](roadmap.md) 2026-09-03 (Q-104…Q-108)
  + написание [design-plan.md](design-plan.md) 2026-09-03 (Q-109…Q-116)
  + написание [content-guide.md](content-guide.md) 2026-09-03 (Q-117…Q-121)
  + написание [screen-map.md](screen-map.md) 2026-09-03 (Q-122…Q-128)
  + написание [data-model.md](data-model.md) 2026-09-03 (Q-129…Q-137)
  + ревизия [data-model.md](data-model.md) 2026-09-07 под D-81…D-83 (Q-138…Q-142)
  + написание [tech-stack.md](tech-stack.md) 2026-09-07 (Q-143…Q-150)
  + написание [architecture.md](architecture.md) 2026-09-07 (Q-151…Q-158)
  + написание [api-spec.md](api-spec.md) 2026-09-07 (Q-159…Q-172)
  + написание [security-and-access.md](security-and-access.md) 2026-09-07 (Q-173…Q-187)
  + написание [analytics-spec.md](analytics-spec.md) 2026-09-07 (Q-188…Q-197)
  + написание [test-plan.md](test-plan.md) 2026-09-07 (Q-198…Q-210)
  + написание [implementation-plan.md](implementation-plan.md) и [tasks.md](tasks.md) 2026-09-07 (Q-211…Q-220)
  + разбор всех открытых вопросов 2026-09-15: семнадцать закрыты решениями D-158…D-174
  + смена домена на `dokey.ru` 2026-09-16 (Q-240 закрыт D-175, Q-241 открыт)
  + выполнение T-001 2026-09-16 (Q-238, Q-239); Q-241 закрыт D-176
  + планирование T-011 2026-09-17 (Q-242, Q-243); закрыты D-178 и D-179
  + выполнение T-011 2026-09-18 (Q-244)
  + /check T-234 2026-09-22 (Q-245, закрыт D-181); планирование T-239 2026-09-22 (Q-246, закрыт D-182)
- **Принятые решения:** см. [decisions.md](decisions.md) — D-01…D-179
- **Владелец по умолчанию:** PO — команда состоит из одного человека + AI, поэтому
  отдельно помечены только вопросы, где нужен внешний вход (юрист, SEO, пользователи).

## Приоритеты

| Приоритет | Значение |
|---|---|
| **P1** | Блокирует vision / архитектуру / решение о запуске. Ответ нужен до написания PRD. |
| **P2** | Влияет на скоуп MVP и roadmap. Ответ нужен до старта разработки соответствующей части. |
| **P3** | Уточняется по ходу, не блокирует. |

---

## Открытые вопросы

> **В реестре два открытых вопроса на 2026-09-22** — Q-238 и Q-239 (P3); P1 и P2 — ноль.
> Всего заведено 246, закрыто 244. Ни одна задача не заблокирована вопросом.
> Прежняя строка 2026-09-22: три открытых вопроса — Q-246 (P2), Q-238 и Q-239 (P3); Q-246 блокировал T-239.
> Строка до неё 2026-09-22: два открытых вопроса — Q-238 и Q-239 (P3); Q-245 закрыт D-181.
> И до неё 2026-09-22: три открытых вопроса — Q-245 (P2), Q-238 и Q-239 (P3).
> Прежняя строка 2026-09-18: два открытых вопроса, Q-238 и Q-239 (P3), заведено 244.
> Строка до неё: открытых вопросов не было на 2026-09-15. Все 79 вопросов, заведённых с 2026-09-02, были
> закрыты решениями D-01…D-83; ревизия [data-model.md](data-model.md) в тот же день открыла пять
> новых (Q-138…Q-142), написание [tech-stack.md](tech-stack.md) — ещё восемь (Q-143…Q-150), из них
> четыре с приоритетом P1, написание [architecture.md](architecture.md) — ещё восемь (Q-151…Q-158),
> из них один с приоритетом P1, написание [api-spec.md](api-spec.md) и генерация `openapi.yaml`
> — ещё четырнадцать (Q-159…Q-172), из них три с приоритетом P1, написание
> [security-and-access.md](security-and-access.md) — ещё пятнадцать (Q-173…Q-187), из них пять с
> приоритетом P1, написание [analytics-spec.md](analytics-spec.md) — ещё десять (Q-188…Q-197), из
> них один с приоритетом P1, написание [test-plan.md](test-plan.md) — ещё тринадцать
> (Q-198…Q-210), из них два с приоритетом P1, написание
> [implementation-plan.md](implementation-plan.md) и [tasks.md](tasks.md) — ещё десять
> (Q-211…Q-220), из них два с приоритетом P1. Ниже сохранён разбор волн: он объясняет, откуда вопросы брались и что каждая
> волна документов находила в предыдущих. Новые вопросы заводятся сюда же — правило закрытия задано
> D-75 п. 4: вопрос закрывается до начала волны, в которой нужен, а счётчик блокировок входит в
> чек-лист готовности волны.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| Q-238 | **Включать ли DNSSEC на зоне `dokey.ru`.** Ни один документ его не решает. Подпись защищает ответы зоны от подмены, но запись DS у регистратора ломает быстрое переключение NS по ST-03: при смене DNS-провайдера DS нужно снять заранее, иначе зона перестаёт резолвиться у проверяющих резолверов. Владелец — PO | [infra/dns/README.md](../infra/dns/README.md), T-001 × ST-03 | P3 |
| Q-239 | **Назван ли почтовый ящик пересылки обработчиком ПДн.** Алиасы `privacy@`, `security@`, `conduct@` пересылаются в личный ящик мейнтейнера (T-001, T-225), и обращения субъектов данных проходят через его провайдера, а ЧЛ-71 перечисляет только Cloudflare, провайдера VPS и GitHub. Адрес пересылки не публикуется (D-125 п. 2) — вопрос о строке реестра ПДн, а не об адресе. Владелец — PO | [launch-checklist.md](launch-checklist.md) ЧЛ-71 × D-125 п. 2 | P3 |
| ~~Q-246~~ | **Закрыт D-182 2026-09-22: генератор `scripts/gen-openapi.ts` и перегенерацию делает отдельная задача T-245, запрет на ручную правку остаётся; T-239 правит только api-spec, ответ — [tasks.md](tasks.md) T-245 и T-239.** **Что значит «перегенерировать `openapi.yaml` из api-spec», если генератора нет.** docs/README и CLAUDE.md-шаблон называют файл генерируемым и запрещают ручную правку, `.claude/settings.json` запрещает агенту `Edit`/`Write` файла, /ship п. 3 требует «regenerate openapi.yaml from api-spec» — но ни скрипта, ни цели в `package.json`, ни описанной процедуры нет. Прошлые правки (997b99b, T-209) внесены точечно в обход запрета, а D-91, D-93, D-163, D-164 отложены фразой «подхватит при генерации», которая не случилась: в файле нет ЭП-12 `/robots.txt`, а упразднённый ЭП-08 `/s/script.js` стоит. T-239 требует «перегенерацию, не правку руками» и не может её выполнить. Варианты: (а) `scripts/gen-openapi.ts` из размеченных блоков api-spec — отдельная задача до T-239, api-spec придётся разметить; (б) полная перегенерация файла из текущей api-spec отдельной задачей, в сессии которой PO снимает запрет, — собирает весь дрейф разом, T-239 правит только api-spec и получает `блок:` на неё; (в) точечная перегенерация операции ЭП-06 внутри T-239 с явным разрешением PO на обход запрета — дёшево, но дрейф остаётся и прецедент закрепляется. Рекомендация — (б): прозу api-spec механически не вывести, а точечные правки уже дали дрейф в четыре решения. Владелец — PO | T-239 × `.claude/settings.json` × /ship п. 3, найдено при планировании T-239 | закрыт |
| ~~Q-245~~ | **Закрыт D-181 2026-09-22: хеш содержимого в имени шрифта, ответ — [api-spec.md](api-spec.md) ЭП-06.** **Какой `Cache-Control` у шрифтов `/fonts/*`, если в их именах нет хеша.** `infra/headers/_headers` и `infra/docker/nginx.conf` отдают `/fonts/*` год и `immutable`, а ЭП-06 (api-spec §4.6) и architecture §5 «Ответы хостинга» дают `immutable` только хешированным ассетам — инвалидация сменой хеша в имени. Имена шрифтов постоянные (`/fonts/manrope-400.woff2`, Design System `tokens/fonts.css`), T-028 кладёт их в `public/fonts/*`, где сборка хеш не ставит. До T-234 правило фактически не действовало — склеивалось с `max-age=0, must-revalidate` из `/*`; после T-234 замена шрифта застрянет в кешах браузера и края на год. Варианты: хеш в имени файла шрифта (правит T-028 и `fonts.css`); короткий TTL с ревалидацией для `/fonts/*`; явный пункт про шрифты с постоянным именем в ЭП-06. Ответ нужен до T-028. Владелец — PO | [api-spec.md](api-spec.md) ЭП-06 × `infra/headers/_headers` `/fonts/*`, найдено в /check T-234 | закрыт |
| ~~Q-242~~ | **Закрыт D-178 2026-09-17: тег `v0.0.0`.** **Каким тегом T-011 выкатывает первую заглушку на прод.** Прод меняется только тегом (implementation-plan §4), environment `production` принимает только `v*` (D-162 п. 3, T-215), ручной деплой — авария (SEC-19). Тег `v0.0.1` уже занят приёмкой T-015 и T-032, а любой тег `v*` ещё и публикует образ в GHCR с меткой `latest` — рунбук `infra/access/README.md` запрещает ставить тег ради проверки. Варианты: отдельный тег `v0.0.0`; перенести приёмку T-011 на тег `v0.0.1` из T-015; разовый ручной деплой, записанный как авария. Владелец — PO | [tasks.md](tasks.md) T-011 × T-015, T-032 × implementation-plan §4 × SEC-19 | закрыт |
| ~~Q-244~~ | **Закрыт D-180 2026-09-18: NEL выключается настройкой зоны.** **Что делать с заголовками `Report-To` и `NEL`, которые Cloudflare добавляет к ответам `dokey.ru`.** Замечены 2026-09-18 на стартовом Worker'е после привязки домена (T-011): `report_to` ведёт на `a.nel.cloudflare.com`, `success_fraction: 0.0` — при сетевой ошибке браузер посетителя сам отправляет отчёт с адресом страницы на сторонний origin. CSP `connect-src` это не ограничивает, `gate:egress` не видит, а ИНВ-01 требует сторонних origin ровно 0. Ввод в адресе не живёт (ИНВ-02), поэтому это утечка факта визита и сбоя, а не ввода. Варианты: снять оба заголовка на краю — правилом в панели (вторая копия политики вне репозитория, ST-03) или в самом Worker'е; принять как свойство хостинга и записать исключение в ИНВ-01 и страницу доверия; проверить, остаются ли они на ответах нашего Worker'а после тега `v0.0.0`, и решать по факту. **Факт 2026-09-18:** после выкладки `v0.0.0` оба заголовка стоят и на ответах нашего Worker'а, рядом с политикой из `_headers`. Владелец — PO | [architecture.md](architecture.md) ИНВ-01 × ADR-10, T-011 | закрыт |
| ~~Q-243~~ | **Закрыт D-179 2026-09-17: разовая привязка в панели.** **Кто привязывает `dokey.ru` к Worker'у, если у токена деплоя одно право `Workers Scripts:Edit`** (D-162 п. 2). По документации Cloudflare (developers.cloudflare.com/workers/authorization/workers, проверено 2026-09-17) добавить или изменить Custom Domain можно только с `Workers Routes Write` на зоне; последующие деплои обходятся правом на скрипт, пока привязка не меняется. Варианты: ROLE-03 один раз привязывает домен в панели, а `wrangler.toml` повторяет привязку для сверки; добавить токену второе право `Zone · Workers Routes · Edit` только на `dokey.ru` (меняет D-162 п. 2, SEC-14, ЧЛ-92); первая привязка ручным `wrangler login`. Владелец — PO | [tasks.md](tasks.md) T-011 × D-162 п. 2 × ADR-10 | закрыт |
| ~~Q-241~~ | **Закрыт D-176 2026-09-16: замер после T-011.** **Доступен ли прод на Cloudflare из России и что делаем, если нет.** Прод — Cloudflare Workers (D-86), а домен теперь `.ru` (D-175), то есть доля посетителей из России вырастет. По публикации самого Cloudflare (июнь 2025) российские провайдеры обрезают ответы сайтов за его прокси после первых килобайт; состояние на сегодня не проверено. Решить до первой проксируемой записи: принять риск, снять прокси с записи или вынести раздачу на VPS по ST-03. Владелец — PO, ответ нужен до T-011 | [decisions.md](decisions.md) D-86 × D-175 | закрыт |

Ниже — разбор волн: закрытые вопросы, сохранённые в том порядке, в каком они заводились.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-140~~ | **Закрыт D-159 2026-09-15: одно предложение и вход в палитру.** **Текста страницы выведенной позиции не существует.** D-82 п. 3 обязывает маршрут `withdrawn` отдавать «страницу с объяснением» и входом в палитру, но ни content-guide, ни требование этой строки не содержат. REQ-LIMIT-04 запрещает молчаливый отказ, и это ровно тот случай: человек пришёл по сохранённому ярлыку за инструментом, которого больше нет | [data-model.md](data-model.md) §3.1, D-82 п. 3 × REQ-LIMIT-04 | закрыт |
| ~~Q-141~~ | **Закрыт D-160 2026-09-15: `description` обязателен.** **Есть ли у служебной страницы `description`.** Гейт уникальности `title`/`description` сформулирован для лендингов инструментов (REQ-CONTENT-03), а состав записи служебной страницы (D-82 п. 1) поля `description` не содержит. Либо гейт у́же реестра, либо состав записи неполон; для четырёх страниц, на которые ведут ссылки из шапки и подвала, это видно в выдаче | [data-model.md](data-model.md) §2.1, D-82 п. 1 × REQ-CONTENT-03 | закрыт |
| ~~Q-142~~ | **Закрыт D-161 2026-09-15: классы `desktop` и `mobile`.** **`platformClass` не покрывает мобильный Chrome.** Ключ порога — тройка «инструмент × класс платформы × ось» (D-73 п. 1), классов объявлено два: `desktop` и `mobileSafari`. NFR-14 называет четыре браузера в последних двух мажорных версиях, и мобильный Chrome не относится ни к одному классу — а порог обязан быть у каждой пары, иначе REQ-LIMIT-03 не выполняется на части устройств | [data-model.md](data-model.md) §2.1, NFR-14 × D-73 п. 1 | закрыт |
| ~~Q-150~~ | **Закрыт D-162 2026-09-15: токен со сроком, второй носитель — отложенный.** **Владение DNS и хранение токена деплоя.** TECH-01 занимает домен, ADR-10 добавляет автоматический деплой из CI; ни один документ не говорит, где лежит секрет, кто его ротирует и что происходит при потере доступа к аккаунту хостера — при команде из одного человека это единственная точка, у которой нет второго носителя | [tech-stack.md](tech-stack.md) ADR-10, TECH-01 | закрыт |

**Общее у первых двух.** Q-138 и Q-139 — цена того, что запись реестра за одну сессию выросла
трижды (D-49, D-56, D-73) и перестала быть только данными. Оба вопроса отвечаются до первой закрытой
позиции каталога, потому что оба меняют форму реестра, а реестр по PRD §9 закладывается до первого
инструмента.

**Общее у восьми вопросов стека.** Q-143…Q-150 распадаются на три группы, и это распределение
само по себе результат. Три вопроса (Q-143…Q-145) — прямое следствие того, что контекст интервью
пришёл незаполненным: команда, хостер и объём эксплуатации нигде в документах не заданы числом,
хотя каждый из них ограничивает решение. Три (Q-146…Q-148) — совместимость, проверяемая на
каркасе за часы, но обязательная до В-0 и В-10. Два (Q-149, Q-150) — дыры, найденные при попытке
описать гейты и деплой как механизмы: бюджет NFR-03 не имеет машинного определения, а у секрета
деплоя нет второго носителя при команде из одного человека.

**Общее у восьми вопросов архитектуры.** Q-151…Q-158 распадаются на две группы, и распределение
снова говорит больше самих вопросов. **Шесть из восьми — про то, как мы узнаём, а не про то, как мы
работаем** (Q-151…Q-156): у продукта без сервера почти вся эксплуатация состоит из наблюдения за
чужими механизмами — за защищённостью контекста у развёртывающего, за расписанием у GitHub, за
диском VPS, за лимитом у Docker Hub, за доступностью прода. Каждый из шести — место, где решение уже
принято, а способ заметить его нарушение не назначен. **Два оставшихся (Q-157, Q-158) — про
единственный источник истины** там, где конфигурация раздвоилась: умолчание провайдера против
NFR-11 и две копии заголовков ответа. Ни один из восьми не блокирует В-0: три нужны до В-6, три до
В-9, два до релиза. Единственный P1 — Q-151, потому что он касается не реализации, а публичного
обещания NFR-12, у которого в целевой конфигурации FL-17 обнаружилось необъявленное исключение —
ровно тот дефект, который D-63 уже один раз устранял, а не описывал.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-161~~ | **Закрыт D-163 2026-09-15: в `selfhosted` sitemap нет.** **Нужен ли `sitemap.xml` в профиле `selfhosted`.** ИНВ-06 делает реестр единственным источником sitemap, и гейт «sitemap ≡ реестр» не разделяет профили; при этом во внутреннем контуре краулера нет по построению, а ИНВ-11 требует от образа нуля сетевых обращений и отсутствия аналитики физически. Либо гейт проверяет документ, который в одном из двух артефактов не нужен, либо у профилей расходится состав, и это расхождение нигде не объявлено | [api-spec.md](api-spec.md) §4.0, ENT-26 × ИНВ-06 | закрыт |
| ~~Q-163~~ | **Закрыт D-164 2026-09-15: `robots.txt` из признака индексации, `/s/` закрыт.** **`robots.txt` не упомянут ни одним документом проекта.** Канал привлечения — поисковая выдача long-tail RU (D-07), sitemap публикуется и сверяется гейтом (ИНВ-06), но файл, которым sitemap объявляется краулеру и которым закрываются служебные пути, не назван ни в архитектуре, ни в tech-stack. Побочный эффект отсутствия: путь прокси `/s/*` ничем не закрыт от обхода | [api-spec.md](api-spec.md) §4.3, D-07 × ИНВ-06 | закрыт |
| ~~Q-168~~ | **Закрыт D-165 2026-09-15: `application/json`.** **`Content-Type` события не выбран, и на self-hosted это перестаёт быть безразличным.** На проде запрос same-origin, поэтому `application/json` и `text/plain` равнозначны. В профиле `selfhosted` аналитики нет вовсе (ИНВ-11), то есть вопрос там не возникает — но он возникает у любой конфигурации, где страница и приёмник разъезжаются по хостам: `application/json` вызывает preflight, `text/plain` его избегает, и это разница в один сетевой запрос на первом экране, то есть в показании счётчика ENT-24 | [api-spec.md](api-spec.md) §4.8, D-22 × ENT-24 | закрыт |
| ~~Q-169~~ | **Закрыт D-166 2026-09-15: `404`, как на любой несуществующий путь.** **Ответ образа на запрос `/s/*` не определён.** В профиле `selfhosted` прокси и скрипта нет физически (ИНВ-11), но запрос к ним возможен: старая вкладка, кешированный HTML, ручная проверка ROLE-05. `404` формально честен и неотличим от поломки при разборе жалобы; явный ответ «в этой сборке аналитики нет» полезнее, но добавляет в артефакт текст, которого нет в content-guide, и требует конфигурации nginx, расходящейся с `_headers` (Q-158) | [api-spec.md](api-spec.md) §4.7, ИНВ-11 × REQ-LIMIT-04 | закрыт |
| ~~Q-170~~ | **Закрыт D-167 2026-09-15: `Retry-After` не ставится, отвечает платформа.** **`Retry-After` при исчерпании лимита Worker: бессмысленный для клиента, нужный для проверки.** Ретраев у отправителя ноль (NFR-11, D-44), поэтому заголовок клиенту не адресован ничему. Но синтетическая проверка Q-156 по `429` без него не отличит суточный лимит (ожидаемое состояние в конце месяца) от отказа маршрута, и заведёт issue на штатное поведение — то есть ровно тот шум, из-за которого D-59 ограничил алерты тремя предохранителями | [api-spec.md](api-spec.md) §3.3, NFR-11 × [architecture.md](architecture.md) §8.3 | закрыт |


**Общее у четырнадцати вопросов спецификации API.** Q-159…Q-172 распадаются на три группы, и первая
из них — единственная неожиданная. **Три вопроса (Q-162, Q-164, Q-171) — про имена и числа, которые
уже объявлены неизменными, но ещё не зафиксированы.** Все три P1, и все три блокируют не код, а
первую же публикацию:
`Tool.id` неизменен навсегда по D-82 п. 2, маршрут пожизненен по D-82 п. 3, на маршрутах стоит канал
D-07 — при этом ни одного конкретного slug в тринадцати документах проекта нет. Цена ошибки здесь
равна цене решения, а механизма, который заставил бы её обсудить, не существует: первый коммит с
маршрутом закроет оба вопроса молча. Q-171 из той же группы и найден иначе — не рассуждением, а
сверкой трёх документов на одном абзаце: мощность закрытого списка полей объявлена семью и
перечислена восемью в трёх согласованных на вид текстах. **Пять (Q-159, Q-167, Q-168, Q-169, Q-170) — про прокси:**
двадцать строк кода, единственный исполняемый вне устройства артефакт, оказались единственным местом
проекта, где контракт не выведен ни из одного решения, — и четыре из пяти всплыли не в архитектуре, а
при попытке описать ответ, а не запрос. **Оставшиеся пять (Q-160, Q-161, Q-163, Q-165, Q-166) — про
границы применимости:** статус пожизненного маршрута, состав второго профиля, объявление sitemap,
достоверность метрик и имена событий. Ни один из четырнадцати не блокирует В-0; три P1 нужны до первого
маршрута в реестре, то есть до конца В-1. Q-172 стоит особняком: он единственный найден не
чтением документов и не их сверкой, а **валидацией сгенерированного артефакта** — линтер спросил
про поле, которого нет во всём проекте.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-186~~ | **Закрыт D-168 2026-09-15: линейность по построению, проверка в RUN-01.** **Устойчивость распознавателей ступеней к катастрофическому бэктрекингу не проверяется.** Девять распознавателей ступеней ENT-19 (`recognizer` записи реестра, D-73 п. 3) разбирают враждебную по содержанию строку, которую человек вставил сам; специально составленный образец заставляет распознаватель жечь CPU на устройстве. Worker и обязательная отмена (REQ-LIMIT-04) делают это не отказом, а затратой, но корпус фикстур ловит регрессию поведения и не ловит время. Требуется: линейность распознавателя ступени как правило, ограничение по времени распознавания и образец в корпусе | [security-and-access.md](security-and-access.md) §6 THR-04, ENT-19 × REQ-INPUT-06 | закрыт |
| ~~Q-187~~ | **Закрыт D-169 2026-09-15: дамп шифруется `age` и живёт не дольше записей.** **Ретенция и шифрование дампов приёмника не назначены.** A-15 называет бэкапы работой на 1,5–2,5 дня, [architecture.md](architecture.md) §7.2 требует дампа базы перед миграцией Umami — ни один документ не говорит, где дамп лежит, сколько живёт и шифруется ли. Дамп содержит всё, что содержит база, то есть является вторым экземпляром PD-04 без срока (Q-153) и без контроля доступа | [security-and-access.md](security-and-access.md) §5.2, A-15 × Q-153 | закрыт |

**Общее у пятнадцати вопросов безопасности.** Пять P1 распадаются на две группы, и обе — про
расхождение между объявленным и обеспеченным. **Три (Q-173, Q-175, Q-182)** — места, где запрет
объявлен как свойство архитектуры, а механизма запрета нет: скрипт с чужой машины под своим origin,
гейт, измеряющий origin вместо содержимого, и гейты, которые выключаются той же рукой, которую они
ограничивают. **Два (Q-174, Q-177)** — места, где документы противоречат друг другу или молчат там,
где цена молчания измеряется деньгами регулятора. Отдельное наблюдение: двенадцать из пятнадцати
вопросов порождены прокси `/s/*` и его следствиями — двадцать строк кода, заведённые ради нулевого
счётчика, перенесли чужую машину внутрь границы доверия и добавили обработку IP на нашей стороне.
Это не довод против D-22, а довод за то, чтобы чужая машина перестала отдавать код и только
принимала события (Q-173). Ещё одно следствие волны — **допущение A-08 опровергнуто в трёх местах**
(security-and-access §4.6) и его приоритет проверки поднимается с P3 до P1.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-197~~ | **Закрыт D-170 2026-09-15: визит.** **Единица PM-02 и PM-09 названа двумя документами по-разному.** BRD формулирует M-09 как «долю **событий** результат не получен в разрезе причин», а CB-06 — как «долю **событий** по причине превышен лимит»; D-55 п. 2 назначает единицей PM-02 и PM-09 **визит**. Разница не стилистическая: при визите как единице визит с двумя разными причинами попадает в обе строки и сумма долей превышает 100 %, при событии как единице распределение нормируется, но один человек, десять раз вставивший битый JSON, весит как десять. Выбор влияет на то, какая причина окажется топовой, а правило M-09 обязывает адресовать топовую причину решением | [analytics-spec.md](analytics-spec.md) §1.1, [brd.md](brd.md) §2 Ц-3, §3 × D-55 п. 2 | закрыт |

**Общее у десяти вопросов инструментовки.** Ни один не про то, чего измерять: состав событий
сошёлся с первого раза — пять событий, семь полей, ни одного нового поля. Все десять — про
**границы уже принятых решений**, и они распались на три группы. **Четыре (Q-190, Q-191, Q-192,
Q-196)** пришли из одного места: событие объявлено требованием, а шаг сценария, на котором оно
уходит, требованию не принадлежит — отмена долгой операции, недоступный инструмент, счётчик ввода,
второй шаг петли. **Три (Q-188, Q-193, Q-197)** — про знаменатели: метрика названа долей, а от
чего доля, не сказано ни BRD, ни PRD; это ровно тот класс умолчания, из-за которого D-55 однажды
уже пришлось писать. **Два (Q-194, Q-195)** — про то, что гейт проверяет не то, что обещано:
состав полей вместо имён и исходящая запись вместо хранимой; оба той же природы, что Q-175 из
волны безопасности, где гейт измеряет origin вместо содержимого. **Единственный P1 — Q-189:**
четыре метрики механизмов входа и само определение визита стоят на событии, которого нет ни в
одном требовании. Отдельно стоит наблюдение, которое вопросом не является: **два поля из семи не
читает ни одна метрика и ни одна воронка** — `browser` (приходит производным от User-Agent на
приёмнике) и `screenResolution`. Второе к тому же единственное поле, читающее устройство, и его
удаление закрывает половину Q-178 без обсуждения риска (analytics-spec §8.3).

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-208~~ | **Закрыт D-171 2026-09-15: US-061 и US-004.** **NFR-18 не покрыт ни одной историей.** user-stories §14 перечисляет, в каких историях живут NFR-01…NFR-17; NFR-18 в перечислении отсутствует. Требование задаёт два числа — шрифты на критическом пути ≤ 48 KB gzip и сумма чанков каталога в precache ≤ 120 KB gzip, — и гейт у них есть (тот же, что у NFR-03, по D-78), а критерия приёмки нет ни одного. Значит числа проверяются машиной, но не принимаются человеком ни в одной истории, и при их нарушении непонятно, какая история не закрыта | [test-plan.md](test-plan.md) §4.4, NFR-18 × user-stories §14 × D-78 | закрыт |
| ~~Q-209~~ | **Закрыт D-172 2026-09-15: семь пометок сняты.** **Пометка «Блокировано: Q-72» пережила закрытие вопроса.** US-001 (крит. 5), US-004, US-005 (крит. 4) и US-061 (крит. 5) несут строку «Блокировано: Q-72», тогда как US-067 фиксирует, что Q-72 закрыт D-63, а user-stories §14 объявляет: «Что не берётся в спринт, пока вопросы открыты — на 2026-09-07 таких историй нет». Расхождение стоит дороже опечатки: по правилу приёмки ПРИЁМ-02 история с такой строкой не принимается, а по §14 она уже не заблокирована. Нужно снять пометки или вернуть блокировку | [test-plan.md](test-plan.md) §3.2, user-stories §14 × D-63 | закрыт |
| ~~Q-210~~ | **Закрыт D-173 2026-09-15: перечень только в NFR-17, с NFR-18.** **Состав гейтов расходится между глоссарием и PRD.** Глоссарий §2, ROLE-06: «сборка, нарушающая NFR-01…NFR-06, NFR-09 или NFR-10, не проходит». PRD NFR-17: те же плюс **NFR-08 и NFR-13**. Расхождение историческое — NFR-08 попал в гейты решением D-75, NFR-13 решением D-50, — но читается оно как два разных определения допуска релиза в двух нормативных документах | [test-plan.md](test-plan.md) §8, glossary §2 × NFR-17 × D-50 × D-75 | закрыт |

**Общее у тринадцати вопросов тестирования.** Ни один не про то, что тестировать: состав проверок
сошёлся с требованиями без остатка, и ни одного места, где план захотел бы проверить поведение,
которого нет в критериях, не нашлось. **Двенадцать из тринадцати — про исполнителя:** величина
объявлена, а кто и чем её меряет, не сказано. Пять таких мест собрались вокруг лабораторных чисел
(Q-198…Q-201, Q-204), и все пять приходят из одного корня — **north star проекта определён как
величина пользовательского потока, а гейтом ей назначен инструмент, который меряет навигацию.**
Ещё три (Q-206, Q-207, Q-209) — про совпадение проверяющего и проверяемого: автор принимает свою
историю, гейт никем не проверен на срабатывание, пометка о блокировке пережила закрытие вопроса.
Это тот же класс, что Q-182 волны безопасности, где гейты выключаются той же рукой, которую они
ограничивают. **Единственный содержательный итог в пользу набора:** двадцать шесть мер SEC и
четырнадцать гейтов TECH-06 получили адреса прогонов без единого пробела — мер без исполнителя не
осталось, в отличие от NFR.

**Общее у десяти вопросов плана реализации.** Ни один не про то, **как** строить: порядок работ,
зависимости и состав срезов сошлись с backlog и архитектурой без единого спорного места.
**Четыре из десяти — про арифметику плана** (Q-212, Q-213, Q-216, Q-219), и все четыре приходят из
одного корня: **работа, не выраженная пользовательской историей, не попала ни в один бюджет.**
Пятнадцать TECH-задач, четырнадцать гейтов, стенд замеров и матрица браузеров существуют в
документах, имеют исполнителя и не имеют цены — отсюда и разрыв между 37,5–44,5 дня, записанными в
backlog §5.5, и 49,75–80,25, которые даёт та же шкала, применённая ко всему составу работ. Ещё
три (Q-211, Q-214, Q-217) — про единственный источник истины там, где документы разошлись: две
волны у одной истории, два числа гейтов в критерии допуска, две волны у образа. Оставшиеся три
(Q-215, Q-218, Q-220) — про границу между тем, что проект обещает проверять, и тем, что он может
проверить: staging, который не проверяет миграцию, прод, открытый за три месяца до релиза, и
защита ветки, которую снимает та же рука, что её ставила. Последний — прямое продолжение Q-182 и
Q-207, и это уже третье появление одного и того же класса в трёх подряд волнах документов.

Все 43 вопроса дискавери закрыты решениями D-01…D-34; написание vision.md добавило три
(Q-44…Q-46), закрытые решениями D-35…D-37 в тот же день. Написание [brd.md](brd.md)
2026-09-03 открыло девять новых (Q-47…Q-55), и почти все они про одно: **перевод принятых
решений в измеримые числа и сроки**. Это ожидаемый эффект — D-11, D-14 и D-34 сознательно
отказались от целевых чисел, а BRD обязан их назвать; расхождение разрешается здесь, а не
умолчанием. Остальное неизвестное живёт в разделе допущений ниже, в решении D-31,
перенёсшем проверку гипотез за релиз, и в трёх триггерах переоткрытия из D-35, D-36 и D-37.

**Все девять закрыты.** D-38 закрыл Q-47 и вместе с ним Q-51 — тот утратил предмет, как
только доля операций без клика стала лабораторным бюджетом. D-39 закрыл Q-48 и по ходу открыл
Q-55. D-40 закрыл Q-49 и задал правило, которое тут же сработало в D-41 на Q-50. D-42 закрыл
Q-52, D-43 — Q-53: оба решения PO против рекомендации, и оба сняли числовые предохранители
с процесса. Принцип, который за ними стоит, назван в D-43: число остаётся там, где его
проверяет машина. D-44 закрыл Q-54, попутно исправив две ошибки в постановке самого вопроса,
а D-45 закрыл Q-55 как надуманный: разбор показал, что D-36 уже развёл релиз и объявление.

**Волна пришла оттуда, откуда её ждали.** Написание [prd.md](prd.md) 2026-09-03 открыло шесть
вопросов (Q-56…Q-61), и они распадаются на две группы. Первая — два требования, не служащие ни
одной M-NN: они не отброшены, но помечены **⚠** в PRD, потому что «полезно» и «служит цели» —
разные основания, и второе проверяемо. Вторая — четыре места, где PRD обязан был назвать число
или состав, а ни одно решение D-01…D-45 их не называет. Ни один из шести не блокирует старт
разработки; Q-58 и Q-60 нужно закрыть до первой строки кода соответствующей части. **Четыре из
шести закрыты в тот же день** — Q-58 (D-49), Q-60 (D-50), Q-59 (D-51), Q-61 (D-52); открытыми
закрыты и оба вопроса о требованиях без M-NN: Q-56 — D-58 (требование о теме расщеплено,
системная тема служит M-14 и NFR-13, переключатель ушёл в P0b), Q-57 — D-59 (расписание
пересборки — исполнение D-28, а не метрика; предохранители против RISK-11 названы). **Волна PRD
закрыта целиком.** **Три разбора из
четырёх сместили предмет вопроса.** D-49: состав минимума подтверждён, но как рычаг он стоит три
дня из тридцати семи с половиной и планом реакции не является, а демонтаж каталога ломает
лестницу D-14 — ступень переехала из константы определителя в запись реестра. D-50: половина
вопроса о доступности оказалась закрыта design-plan §7, зато нашлась дыра, которой не было ни в
одном документе, — ядро продукта не происходит для скринридера (REQ-CORE-08), а конфликт с CB-01
живёт не в доступности, а в headless-библиотеке, которая её приносит. D-52: трудных случаев
оказалось не два, а три, и третий — разобранный пример первого экрана — делал успешным каждый
визит вообще; попутно снят конфликт METR-01 с METR-02 на отрицательном вердикте валидации.
Только D-51 ответил ровно на заданный вопрос — процедурой замера и гейтом на `provisional`.

**Глоссарий нашёл не термины, а расхождения.** Написание [glossary.md](glossary.md) 2026-09-03
добавило шесть вопросов (Q-62…Q-67), и ни один из них не про выбор слова. Три — про места, где
два документа проекта называют или считают одно и то же по-разному: имена инструментов (Q-62,
закрыт D-56 — имён два, и они не спорят, а делят роли),
статус счётчика ввода как позиции каталога (Q-65, закрыт D-57), единица счёта продуктовых метрик
(Q-66, закрыт D-55).
Один — про прямой конфликт дизайн-системы с бюджетами NFR-03 и NFR-09 (Q-63); он единственный
из шести измерялся в килобайтах и сторонних origin, а не в трактовках, и **закрыт D-54**:
заменяются механизмы поставки, а не язык. Оставшиеся два — цена
правила «один глагол везде» (Q-64, закрыт D-60) и незаполненная английская колонка глоссария
(Q-67, закрыт D-61 — правилом вывода вместо строк). **Волна глоссария закрыта целиком.** Q-63 закрыт
2026-09-03; **Q-66 закрыт D-55** — двумя единицами вместо одной: восемь прокси-метрик делились
на две семьи, которым нужен разный знаменатель, а автотрекинг URL считал бы внутреннюю подмену
маршрута просмотром.

**Сценарии нашли то, чего не находят требования.** Написание
[user-flows.md](user-flows.md) 2026-09-03 добавило двадцать один вопрос (Q-68…Q-88), и природа
у них другая, чем у предыдущих волн. PRD спрашивал числа, глоссарий — имена; сценарии
спрашивают, **что происходит на шаге**. Отсюда три группы. Первая и самая дорогая — одно
прямое противоречие внутри PRD: REQ-CORE-03 обещает переход «с сохранением введённого», а
NFR-10 требует, чтобы 0 полей ввода переживало смену маршрута, и проверяет это автотестом
(Q-70). Оно блокирует роутер, teardown и гейт допуска релиза, поэтому закрывается первым.
Вторая — двенадцать мест, где требование описывает удачный исход и молчит о неудачном:
недоступный API (Q-76), отсутствующий чанк (Q-72), отменённая операция (Q-74), `alg` не HS256
(Q-80). Третья — восемь мест, где два документа проекта или два требования смотрят в разные
стороны: контракт против релизной логики (Q-85), «продолжить» против «Продолжить с этим»
(Q-78), «сценарий» против §6 глоссария (Q-88). Ни один из двадцати одного не блокирует старт
разработки целиком, но Q-70, Q-72, Q-76 и Q-77 нужно закрыть до вёрстки оболочки, а Q-86 —
проверить на реальном OpenSearch-описании, потому что за ним стоит обещание D-13.

**Истории нашли отсутствующие требования, а не отсутствующие ответы.** Написание
[user-stories.md](user-stories.md) 2026-09-03 добавило девять вопросов (Q-89…Q-97), и волна
опять другой природы. Сценарии спрашивали, что происходит на шаге; истории спрашивают, **чем
это проверяется** — и на четырёх местах выяснилось, что проверять нечего, потому что требования
нет вовсе: копирование результата (Q-90), «Очистить» (Q-91), статус «Офлайн» и обвязка оболочки
(Q-92), событие открытия инструмента (Q-93). Все четыре присутствуют в глоссарии и в сценариях
и отсутствуют в PRD — это не пропуск сценариев, а пропуск требований, и закрывается он
заведением REQ, а не трактовкой. Вторая группа — три места, где требование есть, но написано
словами, которые нельзя превратить в критерий: «осмысленный результат» (Q-89), «два нажатия»
(Q-95), «производные значения» в teardown (Q-96). Последнее дороже прочих: на нём стоит
автотест NFR-10, то есть допуск релиза. Третья — два непроверяемых обещания наружу: состав
бенчмарка (Q-94) и единица счёта NFR-08 (Q-97). Q-89, Q-93, Q-95 и Q-96 нужно закрыть до
первой строки кода соответствующей части.

**Backlog спросил не про продукт, а про план.** Написание [backlog.md](backlog.md) 2026-09-03
добавило шесть вопросов (Q-98…Q-103), и природа волны сменилась в третий раз. PRD спрашивал,
какое число назвать; истории — чем это проверяется; backlog спрашивает **когда и в каком
порядке**, и на этом уровне выяснилось, что документы проекта планируют работу, но не планируют
собственное доделывание. Три вопроса про это прямо (к 2026-09-07 все три закрыты, а блокировок не осталось ни одной): сорок две истории из шестидесяти семи несли
блокировку, и ни у одного блокирующего вопроса нет даты ответа относительно T0 (Q-99); шесть
требований покрыты только историями, которые не берутся в спринт, а матрица покрытия считает их
покрытыми (Q-100); M-03 калибрует позицию поверх каркаса, который в неё не входит и отдельной
оценки не имеет (Q-98). Оставшиеся три — механика приёмки: сквозные истории без момента
закрытия (Q-101), архитектурные истории без предмета проверки до первого инструмента (Q-102) и
порядок «гейт раньше приёмника» в инструментовке (Q-103). Ни один из шести не требует внешнего
входа: все шесть — решения PO о собственном процессе. **Q-98, Q-99 и Q-100 нужно закрыть до
первой строки кода**, потому что от них зависит, что именно считать началом.

**Роадмап спросил, что происходит, когда план не выполняется.** Написание
[roadmap.md](roadmap.md) 2026-09-03 добавило пять вопросов (Q-104…Q-108), и природа волны снова
сменилась: backlog спрашивал, когда и в каком порядке делать работу, роадмап — **что остаётся,
когда работа не помещается, и чем определяется следующий релиз**. Отсюда две пары и один хвост.
Первая пара — план реакции, который режет не то: жёсткий минимум RISK-06 записан диапазоном волн
и в этом виде выносит из релиза третий артефакт M-01, всю M-07 и обязательные к публикации числа
порогов (Q-104); а две работы канала, не входящие ни в A-06, ни в A-15, стоят в неделе, где
слака нет (Q-105). Вторая пара — метрика без исполнителя и релиз без данных: гейта на CB-08 нет
в TECH-06, хотя BRD называет его механизмом измерения (Q-107), а P0b выпускается раньше двух из
трёх baseline-ов, из-за чего правило M-09 «топовая причина адресована в следующем релизе» при
текущем календаре невыполнимо (Q-106). Хвост — четыре релиза, названные тремя системами имён, к
которым роадмап добавил четвёртую (Q-108). Ни один из пяти не требует внешнего входа. **Q-104 и
Q-106 нужно закрыть до 2026-10-05** — оба принимаются на холодную голову или не принимаются
вовсе: первый читается в декабре, когда времени нет, второй — в январе, когда две трети P0b уже
построены.

**План дизайна спросил, как продукт выглядит — и нашёл, что это уже решено не тем брифом.**
Написание [design-plan.md](design-plan.md) 2026-09-03 добавило восемь вопросов (Q-109…Q-116),
и первый из них крупнее остальных семи вместе взятых. Q-63 разобрал конфликт готовой
дизайн-системы с бюджетами как конфликт **механизмов** (React, шрифты с CDN, бандл Lucide) и
прямо предположил, что «токены, типографика, правила контента и словарь иконок переносятся без
изменений». План дизайна утверждал обратное: кит собран по брифу «нейтральный shadcn-дефолт с
фирменным акцентом», то есть слой токенов и есть то, что нужно заменить (Q-109).

**Оба закрыты 2026-09-03, и решение противоположно предположению плана.** D-53: источник истины —
кит, план дизайна понижен до подчинённого документа и приведён к нему, §8 снят; узнаваемость
видом не преследуется — это названный риск. D-54: заменяются ровно три механизма — острова без
фреймворка вместо React, самохостинг четырёх woff2 вместо CDN, спрайт из восьми глифов вместо
бандла Lucide. Побочный улов разбора — гейт D-50, применённый к токенам кита до первой строки
вёрстки: девять значений не проходили контраст, все девять починены сдвигом на ступень
собственной шкалы кита.

Остальные шесть — цена конкретных решений плана: тема как **Could** и негде хранить ручной
выбор (Q-110, закрыт D-58 вместе с Q-56), отсутствие байтового бюджета на шрифты вообще (Q-111),
непроверенная кириллица в
сабсете (Q-112, теперь про Manrope и JetBrains Mono), двойной смысл акцентной подложки (Q-114),
размер цели 2.5.8, который не ловит Lighthouse из NFR-13 (Q-115), и знак, который попадёт в
листинги раньше, чем продукт (Q-116). Q-113 закрыт вместе с Q-109: подсветка JSON берётся из
палитры кита. Q-111 нужно закрыть до первой закрытой позиции — он измеряется на ней же.

**Тексты почти ничего не спросили — и это главный результат волны.** Написание
[content-guide.md](content-guide.md) 2026-09-03 добавило пять вопросов (Q-117…Q-121), и все,
кроме первого, — мелкие формы записи. Причина в том, что словарь уже закрыт глоссарием, а
запреты продукта сделали за райтера половину работы: писем нет, потому что нет адресов;
подтверждений нет, потому что нечего терять безвозвратно; слова «удалить» нет, потому что
нечего удалять. Два столкновения гайд закрыл предложением, а не вопросом: кнопка мягкого порога
называет действие инструмента («Форматировать всё равно»), что снимает два «продолжить» на
одном экране из Q-78, а список стоп-слов из §1 предлагается как недостающий механизм CB-08 из
Q-107. Единственный существенный вопрос волны — **Q-117**: на каком языке ведутся публичные
тексты репозитория. Он датирован жёстче прочих: по A-11 репозиторий создаётся 2026-09-07, а
README и шаблон issue пишутся в тот же день и переписываются потом дороже.

**Карта экранов нашла ровно то, что должна была найти, — расхождения между уже принятыми
документами.** Написание [screen-map.md](screen-map.md) 2026-09-03 добавило семь вопросов
(Q-122…Q-128), и ни один из них не про то, какие экраны нужны продукту: реестр из пятнадцати
экранов собрался из FL-01…FL-20 без единого добавления «для полноты», а обратная проверка не
нашла ни одного экрана без сценария. Зато сведение сценариев, историй, плана дизайна и гайда по
текстам на один макет вскрыло три прямых противоречия — **Q-122** (чипсы типа стоят под
результатом по D-18 и рядом с полем по FL-02, FL-03 и US-009), **Q-123** (генератор показывает
значение до человека по US-066 и пустое состояние по content-guide §5), **Q-128** (палитра ищет
шесть инструментов по тексту гайда и восемь позиций по критерию US-014) — и две дыры, которые
раньше были не видны, потому что до макета никто не спрашивал «а куда отсюда»: **Q-124**
(«путь в каталог» и вордмарк ведут в главную, которой D-18 не оставил) и **Q-125** (маршруты
пяти служебных страниц не описаны ни реестром, ни требованием). **Q-122 закрывается первым:** на
обнаружении чипсов стоит вторая по цене точка потери во всём наборе (user-flows §4), и она же
единственная, которая читается в PM-03 с обратным знаком.

**Модель данных проверяла продукт, у которого нет базы данных, — и нашла ровно то, что бывает у
такого продукта.** Написание [data-model.md](data-model.md) 2026-09-03 добавило девять вопросов
(Q-129…Q-137), и ни один не про то, где хранить данные: персистентность равна нулю, четыре контура
хранения (сборка, память вкладки, кеш устройства, аналитика) собрались без единого допущения, все
ENT-01…ENT-29 нашли место. Вопросы пришли из двух мест. Первое — **поля, которых требование
требует, а документ не называет:** состав записи реестра целиком (**Q-130**) и внутри него признак
«инструмент с чувствительным вводом», от которого зависит объём teardown ENT-23; разобранный пример
первого экрана (**Q-137**), который обязан быть на сборке, синтетическим и уникальным для каждого
инструмента, но не принадлежит ни реестру, ни лендингу; размерность ключа порога (**Q-135**), где
NFR-14 и REQ-LIMIT-05 требуют не одного числа на инструмент, а таблицы. Второе — **два публичных
значения без правила неизменности:** `Tool.id` уходит в аналитику и при переименовании рвёт ряд
PM-01…PM-03 (**Q-131**), маршрут выведенной позиции не описан ничем (**Q-132**). Отдельно стоит
**Q-133**: хеш ленивого чанка меняется на каждой сборке, и вкладка со старой оболочкой запрашивает
исчезнувший файл — это третий случай Q-72, но причиной у него релиз, а не офлайн, и сообщением он
не лечится. Q-133 сцеплен с Q-79 и решается вместе с ним, до настройки service worker.

| ID | Вопрос | Источник | Приоритет | Владелец |
|---|---|---|---|---|
| — | Открытых вопросов нет | — | — | — |

**Приёмка набора в репозиторий нашла один содержательный пробел и три механических.**
Инвентаризация `/adopt` 2026-09-07 ([INVENTORY.md](INVENTORY.md)) прогнала весь набор машинной
сверкой: шапки полны в 25 документах из 25, отклонений формата идентификаторов ноль, все шесть
инвариантов держатся, `openapi.yaml` выведен из api-spec строго, и ни один Q-NN не потерян в
хвосте документа вместо реестра. Три из четырёх находок механические и в реестр не заводятся —
путь `agent-rules.md`, дубль `Design System.zip` и ложные счётчики хука
`scripts/session-context.sh` (INVENTORY §5.1, §5.2, §5.4). Содержательная одна, **Q-221**: у трека B
есть результат, но нет породившего его промта, — и это единственное место во всём наборе, где
артефакт не воспроизводится из документов.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-221~~ | **Закрыт восстановлением 2026-09-07.** У дизайн-кита не было промта, из которого он выведен: трек B требует `design-system-prompt.md` и `component-library-prompt.md`, их результат — `docs/Design System/` (токены, 22 компонента, 20 специменов) — существовал и объявлен D-53 источником истины, а сами промты нет. Написаны [design-system-prompt.md](design-system-prompt.md) и [component-library-prompt.md](component-library-prompt.md); направление зависимости закреплено явно (промт → `tokens/*.css` → потребители), запрет второго источника истины по токенам не ослаблен. Процедура синхронизации промта с китом осталась ненаписанной — **Q-222**; при восстановлении вскрылось **Q-223** | `/adopt` 2026-09-07, [INVENTORY.md](INVENTORY.md) §5.3, D-53 × [screens-prompt.md](screens-prompt.md) шапка | закрыт |

**Восстановление промтов трека B нашло два расхождения кита с решениями проекта.** Написание
[design-system-prompt.md](design-system-prompt.md) и
[component-library-prompt.md](component-library-prompt.md) 2026-09-07 закрыло Q-221 и подняло два
вопроса — оба про то, что **решение принято, а файл кита остался прежним**. Первый (Q-222) — общий:
правки D-50 и D-53 п. 5 вносились прямо в `tokens/colors.css`, промт узнал о них задним числом, и
процедуры, которая держит их в согласии, не существует ни в одну сторону. Второй (**Q-223**)
— конкретный и с ценой: `Icon.jsx`, `Icon.prompt.md` и `assets/README.md` по-прежнему предписывают
грузить Lucide с `unpkg.com`, хотя D-54 п. 3 заменил бандл инлайн-спрайтом из восьми глифов, —
и **design-plan §5.1 при этом утверждает, что правка уже внесена в файлы кита.** Спецификация,
которую по TECH-15 переносят в продукт, противоречит ИНВ-01, а документ сообщает, что противоречия
нет. D-54 п. 4 выводит из-под запрета только демонстрационные `*.card.html` и `ui_kits/*`;
`Icon.prompt.md` к ним не относится — это контракт компонента.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-226~~ | **Закрыт D-174 2026-09-15: `.dockerignore` в корне.** **`.dockerignore` не может лежать там, где его рисует дерево скелета.** [implementation-plan.md](implementation-plan.md) §2.1 помещает его в `infra/docker/` рядом с `Dockerfile` и `nginx.conf`. Docker читает `.dockerignore` из корня контекста сборки, а контекст здесь — корень репозитория (`Dockerfile` ссылается на `package.json` и `src/`); BuildKit поддерживает `<dockerfile>.dockerignore`, но это была бы вторая копия того же списка — ровно та болезнь, что уже названа Q-158 про заголовки. Файл положен в корень. Расхождение мелкое, но дерево §2.1 объявлено «один в один», и следующий, кто сверит его с репозиторием, потратит время. Либо §2.1 переносит строку в корень, либо называет причину, по которой она там | [implementation-plan.md](implementation-plan.md) §2.1 × `infra/docker/Dockerfile` | закрыт |

**Взятие T-002 2026-09-10 показало, что D-20 п. 3 невыполним в названной форме.** Решение велит
занять на GitHub организацию `dokey`, но у пользователей и организаций общее пространство имён, а
логин `dokey` принадлежит чужому пользователю. Проверка D-20 его не видела: она искала проекты со
словом в имени, а не сам логин.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-233~~ | **Закрыт D-155 2026-09-10: организация `dokey-app`.** Пространство имён `dokey` на GitHub занято, а адрес `github.com/dokey/dokey` уже зашит в код. Логин `dokey` — личный аккаунт (id 1443132): создан 2012-02-16, последний пуш 2013-03-03, три тестовых репозитория, профиль пуст; `dokey/dokey` отдаёт 404. Вариант `dokey-dev` тоже занят, `dokey-app`, `dokeyapp`, `getdokey` свободны на 2026-09-10. От ответа зависят T-002 и все задачи, которым нужен remote: T-013…T-015, T-022, T-023. Адрес уже стоит в `src/pages/index.astro`, `README.md` (включая `--certificate-identity-regexp` для `cosign verify`: это identity подписи, её не сменить после первого релиза) и `.github/ISSUE_TEMPLATE/config.yml`, а `ghcr.io/<org>` в implementation-plan §2.4 и T-022 ждёт того же ответа. Имя необратимо после первого публичного упоминания (D-20), T0 по D-39 уже просрочен | D-20 п. 3 × GitHub, `src/pages/index.astro`, `README.md` | закрыт |

**/check задачи T-016 2026-09-11 нашёл, что гейт G-02 точнее буквы D-88, а состав шрифтов в
бюджете взят из строки, которую дизайн-план уже признал неверной.** Все три расхождения решены в
плане задачи или оставлены «вне этого документа», то есть решение принималось внутри задачи —
ровно то, что CLAUDE.md запрещает. Приоритет P2: от ответа зависят комментарий и строка шрифтов в
`.size-limit.json`, а по п. 2 — и сам счёт.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-234~~ | **Закрыт D-156 2026-09-11: каждый HTML сборки.** Порог G-02 по D-88 применяется к максимуму «по всем маршрутам реестра», а гейт берёт каждый `dist/**/*.html`: реестра до T-034 нет, а `404` в нём не будет. Какое множество маршрутов — предмет порога NFR-03? | D-88 × `scripts/gates/size.ts`, `.size-limit.json` (T-016) | закрыт |
| ~~Q-235~~ | **Закрыт D-156 2026-09-11: только исполняемые.** D-88 п. 1 считает «все инлайновые `<script>`», а гейт пропускает JSON-LD, `importmap` и `speculationrules`, которые браузер не исполняет. Входят ли блоки данных в initial JS? | D-88 п. 1 × `scripts/gates/size.ts` (T-016) | закрыт |
| ~~Q-236~~ | **Закрыт D-157 2026-09-11: Manrope 400/600 и Mono 400.** D-78 п. 2 и NFR-18 кладут на критический путь маршрута инструмента «Mono 400/600», сабсет D-79 п. 1 собирает Mono 400/700, design-plan §5.1 называет состав Manrope 400/600 плюс Mono 400; T-016 заложила глоб `jetbrains-mono-*`. Какие файлы входят в бюджет 48 KB? | D-78 п. 2 × D-79 п. 1 × design-plan §5.1, `.size-limit.json` (T-016) | закрыт |

**Четвёртый /check задачи T-016 2026-09-14 упёрся в предел разбора регуляркой.** G-02 ищет
статические импорты регулярным выражением, без лексера: текст в форме импорта внутри комментария,
JSDoc, строки или шаблонной строки модуля он принимает за ребро графа и валит сборку, если такой
файл не находится. Пропускать комментарии и строки целиком регулярка не может безопасно: `//` или
кавычка в регулярном выражении JS сдвигают разбор, и ложный провал превращается в тихий недосчёт.
План T-016 отверг `es-module-lexer` как новую зависимость (D-154). Приоритет P2: от ответа зависит,
остаётся ли у G-02 известный ложный провал.

| ID | Вопрос | Источник | Приоритет |
|---|---|---|---|
| ~~Q-237~~ | **Закрыт D-158 2026-09-15: `es-module-lexer`.** **Чем G-02 находит статические импорты: регуляркой с известным ложным провалом или `es-module-lexer`?** Регулярка T-016 валит правильную сборку на `// import { debug } from './debug.js'`, на `@example import … from 'x'` и на примере кода в шаблонной строке: сверка с `es-module-lexer` 2.3.2 по 5882 ESM-файлам `node_modules` нашла лишние спецификаторы в 160 файлах, недосчётов — ноль. Варианты: (а) оставить регулярку и записать провал как известное ограничение — автор переформулирует текст; (б) взять `es-module-lexer` — лексер самого Vite, уже в `node_modules` транзитивно — прямой devDependency со строкой tech-stack §3 (D-154) и заменить им `specifiersOf` | план T-016 «Отвергнуто» × D-154 × `scripts/gates/size.ts` | закрыт |

---

## Допущения, с которыми идём дальше

Каждое допущение — незакрытый риск. При опровержении любого из них перепроверить
связанные вопросы и решения.

| ID | Допущение | Основание | Приоритет проверки | Владелец |
|---|---|---|---|---|
| A-01 | **[ДОПУЩЕНИЕ]** Целевая аудитория: backend- и frontend-разработчики, QA-инженеры, системные и интеграционные аналитики, DevOps. Ядро — те, кто ежедневно работает с токенами, конфигами и API-ответами. На этом допущении держится правка D-06 «хэши важнее систем счисления». | Интервью, раунд 0 (описание идеи) | P1 | PO |
| A-02 | **[ДОПУЩЕНИЕ]** Основной сценарий — короткая разовая задача (5–30 секунд), вклинившаяся в другую работу, а не длинная сессия в продукте. Отсюда приоритет скорости входа над глубиной функций и урезанная редакция Regex Tester в D-06. | Вывод из выбранного рычага отличия (раунд 2) | P1 | PO |
| A-03 | **[ДОПУЩЕНИЕ]** Десктопный браузер даёт основную долю использования; мобильная версия нужна для полноты PWA-обещания, но не является драйвером ценности. | Характер задач целевой аудитории | P2 | PO |
| A-04 | **[ДОПУЩЕНИЕ]** Архитектура — статический фронтенд без бэкенда, хостинг статики. Прямое следствие красной линии «никакой серверной обработки данных». Конкретизировано в D-02. | Интервью, раунд 2 | P1 | PO |
| A-06 | **[ДОПУЩЕНИЕ]** Объём P0a после разбора 2026-09-03 — ориентировочно 35–40 человеко-дней: D-06 давал 34–38, плюс артефакты открытого кода (D-08), OpenSearch и страница «под рукой» (D-13), редактор (D-24), петля «продолжить с результатом» (D-26), минус контент, снятый D-17. Сверх этого D-30 добавил worker и виртуализацию в JSON Workbench, и эта надбавка не оценена. При 3–4 днях в неделю это 9–13 недель; P0b — ещё около 22 дней. Оценка по-прежнему не проверена фактической скоростью. | Расчёт в D-06 с поправками сессии 2026-09-03 | P1 | PO |
| A-08 | **[ДОПУЩЕНИЕ, опровергнуто в трёх местах 2026-09-07; одно из трёх снято 2026-09-08]** Продукт не имеет ограничений комплаенса и обработки ПДн. **Верно для ввода и неверно для аналитики:** IP и User-Agent доходят до edge, обезличивание происходит после получения. **Снято D-96:** приёмник IP больше не получает — прокси передаёт псевдоним, поэтому первичной записи ПДн граждан РФ у нас нет и локализация по 152-ФЗ неприменима. **Остаются в силе:** ст. 5(3) ePrivacy шире cookie и покрывает служебный ключ `localStorage` и чтение разрешения экрана (Q-178); контакта субъекта и имени оператора нет (Q-179); журналы edge и VPS содержат IP и не объявлены (Q-180). Допущение дописывается вместе с ответами на эти три | Следствие A-04 и D-01; опровержение — [security-and-access.md](security-and-access.md) §4.6 | **P1** | PO |
| A-10 | **[ДОПУЩЕНИЕ]** Оценки трудоёмкости в D-06 сделаны без прототипирования. Самые рискованные позиции — JSON Workbench (5 д) и Text & Code Diff (5 д): при недооценке вдвое волна P0a выходит за квартал. Первая же реально закрытая позиция должна использоваться для калибровки всех остальных. | Следствие D-06 | P2 | PO |
| A-11 | **[ДОПУЩЕНИЕ]** T0 = 2026-09-07. По D-39 это день, когда занят домен и создан публичный репозиторий, а не день первого коммита кода. От него производны все сроки [brd.md](brd.md): M-01 — до 2026-12-21 (вход в окно 2026-11-16), M-03 — до 2026-10-05, M-12 — до 2026-12-14. Остальные даты производны от M-01. При сдвиге старта всё пересчитывается одним действием. | Написание brd.md; определение уточнено D-39 | **P1** | PO |
| A-12 | **[ДОПУЩЕНИЕ, зона изменена D-175 2026-09-16]** Домен `.ru` стоит того же порядка, что закладывалось под `.app`, — около $15 в год. Вместе с VPS из D-23 даёт постоянные расходы проекта около $87 в год — число, на котором стоит вся экономика §8 brd.md и вывод о бессмысленности спора о монетизации. | Написание brd.md, §8 | P3 | PO |
| A-15 | **[ДОПУЩЕНИЕ, пересчитано D-146 2026-09-10]** Подготовка до начала разработки — **3,5–6,5 человеко-дня**, из них 1–2 — работы канала M-12 и подача TECH-14 (D-77 п. 2), внесённые в допущение, но не в его число. Состав прежних 2,5–4,5: домен и имя на GitHub (D-20) — 0,5 д; VPS, Umami, Postgres, прокси-rewrite, бэкапы и базовая защита (D-22, D-23) — 1,5–2,5 д; cosign, provenance и SBOM поверх пайплайна D-08 (D-28) — 0,5–1,5 д. Это работа, не входившая ни в одну оценку до D-39. **Складывается с A-06 явно:** полный путь до релиза — 37,5–44,5 человеко-дня плюс неоценённая надбавка D-30. **Дополнено 2026-09-07 (D-75 п. 4):** разбор открытых вопросов — три сессии, сорок семь закрытых вопросов и двадцать семь решений D-49…D-75; эта работа не входила ни в A-06, ни в первую редакцию A-15. Сверх неё решения сессии добавили ≈ 2,5 дня кода и контента: axe и статусные регионы (D-50), замер порогов (D-51), шрифты и спрайт (D-54), поисковые формулировки лендингов (D-56). | Разбор состава A-06 при закрытии Q-48 (D-39) | **P1** | PO |
| A-16 | **[ДОПУЩЕНИЕ, заведено 2026-09-09 решением D-102]** Сквозная инфраструктурная работа — **8–17 человеко-дней**: TECH-04…TECH-11 и TECH-15 (каркас приложения, конвейер CI/CD, четырнадцать гейтов, стенд замеров TTR, матрица браузеров, образ, цепочка подписи, инфраструктура фикстур, применение дизайн-системы). Не входит ни в A-06 (тот считает истории), ни в A-15 (та — подготовка до начала разработки). **Полный путь до релиза: A-06 + A-15 + A-16**, и это **49,75–80,25 человеко-дня** (после пересчёта A-15 решением D-146) снизу вверх по [implementation-plan.md](implementation-plan.md) §3.1 — число плана, а не дискавери. Оценка не проверена фактической скоростью, как и A-06 | [implementation-plan.md](implementation-plan.md) §6.4 п. 2, D-102 | **P1** | PO |

---

## Закрытые вопросы

| ID | Вопрос | Решение | Дата |
|---|---|---|---|
| Q-246 | Что значит «перегенерировать `openapi.yaml` из api-spec», если генератора нет | **D-182:** запрет `Edit`/`Write` на `docs/openapi.yaml` в `.claude/settings.json` остаётся и не обходится; генератор `scripts/gen-openapi.ts` и перегенерацию всего накопленного долга (ЭП-08 по D-93, ЭП-12 по D-164, корень по D-91, профиль ЭП-03 по D-163, путь ЭП-06) делает отдельная задача T-245, запись в файл человек разрешает единожды и явно как её USER_ACTION; T-239 сужается до api-spec §4.0 и §4.6 и в `блок:` на T-245 не ставится — api-spec источник и верен до генерации. Отвергнуты точечная перегенерация ЭП-06 внутри T-239 с разовым обходом запрета, ручная перегенерация файла целиком, снятие запрета совсем, разметка api-spec до T-239. Выбор PO. Правки — [tasks.md](tasks.md) T-239 и T-245, [decisions.md](decisions.md) D-182 | 2026-09-22 |
| Q-244 | Что делать с заголовками `Report-To` и `NEL`, которые Cloudflare добавляет к ответам `dokey.ru` | **D-180:** адрес отчётов, объявленный заголовком ответа, — сторонний origin по ИНВ-01, кто бы заголовок ни добавил; ROLE-03 выключает NEL в настройках зоны, как Managed robots.txt; RUN-16 падает на ответе с `nel`, `report-to`, `reporting-endpoints`; хвост до 7 суток у браузеров, получивших политику, принят. Отвергнуты снятие в Worker'е (скрипт `main` мимо ADR-13, край дописывает заголовки после Worker'а), правило Transform Rules в панели (вторая копия политики, ST-03), исключение в ИНВ-01, постоянный отказ через поддержку. PO передал выбор агенту. Правки — [architecture.md](architecture.md) ИНВ-01, [infra/dns/README.md](../infra/dns/README.md) п. 7, [tasks.md](tasks.md) T-235 | 2026-09-18 |
| Q-243 | Кто привязывает `dokey.ru` к Worker'у, если у токена деплоя одно право | **D-179:** ROLE-03 один раз в панели заводит Worker `dokey` и Custom Domain `dokey.ru` до тега `v0.0.0`; `wrangler.toml` повторяет привязку, токен остаётся с одним правом `Workers Scripts:Edit`; при отказе релиза на правах маршрута — новый Q-NN, а не расширение токена. Отвергнуты второе право `Workers Routes · Edit` и ручной `wrangler login`. Выбор PO. Правки — [tasks.md](tasks.md) T-011, [tech-stack.md](tech-stack.md) ADR-10, [infra/dns/README.md](../infra/dns/README.md) | 2026-09-17 |
| Q-242 | Каким тегом T-011 выкатывает первую заглушку на прод | **D-178:** тег `v0.0.0` на `main` после слияния PR T-011, конвейер целиком; `v0.0.0` — первая выкладка, конец Э-0 остаётся `v0.0.1` (T-032), правило `v0.N.0` действует с Э-1; образ `v0.0.0` в GHCR допустим. Отвергнуты приёмка тегом `v0.0.1` из T-015 и ручной деплой через `wrangler login`. Выбор PO. Правки — [tasks.md](tasks.md) T-011, [implementation-plan.md](implementation-plan.md) §4.2 | 2026-09-17 |
| Q-241 | Доступен ли прод на Cloudflare из России | **D-176:** T-011 идёт на Workers; после неё замер T-227 — ресурс ≥ 64 KB из пяти операторов, домашняя и мобильная сеть, по три попытки, целиком за ≤ 3 с. Не прошла хотя бы одна сеть — переезд по ST-03 до T-201; замер повторяется перед тегом (ЧЛ-93). Отвергнуты переезд на VPS сразу, риск без замера, российский CDN. Правки — [tasks.md](tasks.md) T-227, [launch-checklist.md](launch-checklist.md) ЧЛ-93, [tech-stack.md](tech-stack.md) ADR-10 | 2026-09-16 |
| Q-240 | Домен продукта: `dokey.app` не зарегистрирован, а у PO есть `dokey.ru` | **D-175:** прод-адрес — **`dokey.ru`**, регистратор сторонний, DNS — Cloudflare; алиасы и staging переезжают вместе с зоной. Зона `.ru` в HSTS-preload не входит, поэтому утверждение «HTTP невозможен по построению» снято в восьми документах, а HTTP закрывают редирект, заголовок HSTS и подача домена в `hstspreload.org` — новая задача **T-223** после T-011. Организация на GitHub остаётся `dokey-app` (D-155), домен продлевается на несколько лет (D-162 п. 4). Отвергнуты регистрация `.app`, редирект без подачи в preload, переименование организации. Правки — тридцать файлов, пометки у D-20 п. 2 и D-150 п. 1 | 2026-09-16 |
| Q-237 | Чем G-02 находит статические импорты | **D-158:** статические импорты и `export … from` модулей находит `es-module-lexer` — прямая devDependency со строкой tech-stack §3 (D-154); неразобранный файл — провал G-02. Регулярка с известным ложным провалом и пропуск комментариев регуляркой отвергнуты. Правка — [tech-stack.md](tech-stack.md) §3, план T-016; код — /fix T-016 | 2026-09-15 |
| Q-226 | `.dockerignore` в дереве скелета стоит не там, где его читает Docker | **D-174:** один файл в корне репозитория, дерево называет причину; `Dockerfile.dockerignore` отвергнут как вторая копия. Правка — [implementation-plan.md](implementation-plan.md) §2.1 | 2026-09-15 |
| Q-210 | Состав гейтов расходится между глоссарием и PRD | **D-173:** состав NFR, допускающих релиз, перечисляется только в NFR-17 — NFR-01…NFR-10, NFR-13 и NFR-18; остальные документы ссылаются на него (D-145 п. 2); NFR-07 держит статус через RUN-03. Правка — [prd.md](prd.md), [glossary.md](glossary.md), [user-stories.md](user-stories.md), [data-model.md](data-model.md), [architecture.md](architecture.md), [test-plan.md](test-plan.md), [launch-checklist.md](launch-checklist.md), [backlog.md](backlog.md) | 2026-09-15 |
| Q-209 | Пометки «Блокировано» пережили закрытие вопросов | **D-172:** сняты семь пометок (Q-72, Q-79, Q-68), критерии ссылаются на закрывшие их решения; US-001 крит. 5 — сообщение зоны D «Логика инструмента не загрузилась.», снимаемое оболочкой при старте; впредь решение снимает пометки в том же коммите. Правка — [user-stories.md](user-stories.md), [content-guide.md](content-guide.md) §5, [test-plan.md](test-plan.md), [launch-checklist.md](launch-checklist.md); задача T-219 | 2026-09-15 |
| Q-208 | NFR-18 не покрыт ни одной историей | **D-171:** шрифтовая половина NFR-18 принимается в US-061, precache — в US-004, критерии 6 ссылаются на бюджет NFR-18 и состав D-157 без чисел. Правка — [user-stories.md](user-stories.md), [test-plan.md](test-plan.md), [tasks.md](tasks.md) §14.2 | 2026-09-15 |
| Q-197 | Единица PM-02 и PM-09 названа по-разному | **D-170:** единица M-09 и CB-06 — визит (D-55 п. 2, D-130); сумма долей может превышать 100 %, топовая причина — максимум доли; перечень причин в BRD — ссылкой на EVT-04. Правка — [brd.md](brd.md), [prd.md](prd.md), [analytics-spec.md](analytics-spec.md), [launch-checklist.md](launch-checklist.md) | 2026-09-15 |
| Q-187 | Ретенция и шифрование дампов приёмника не назначены | **D-169:** дамп шифруется `age` до записи (закрытый ключ офлайн у ROLE-03), на VPS не больше одного регулярного и одного перед миграцией, копии вне VPS нет; ежемесячный прогон удаляет записи старше 12 месяцев и прежний дамп; потеря истории при гибели VPS принята. Выбор PO. Правка — [security-and-access.md](security-and-access.md), [architecture.md](architecture.md), [data-model.md](data-model.md), [launch-checklist.md](launch-checklist.md), `infra/vps/README.md`; задача T-216 | 2026-09-15 |
| Q-186 | Распознаватели ступеней не проверяются на катастрофический бэктрекинг | **D-168:** распознаватель ступени линеен по построению; «ограничение по времени» SEC-26 — бюджет на сборке (время на 4n ≤ 8× времени на n в RUN-01), а не таймер; определитель в воркере отвергнут. Правка — [security-and-access.md](security-and-access.md), [test-plan.md](test-plan.md), [data-model.md](data-model.md), [user-stories.md](user-stories.md) US-013; задача T-220 | 2026-09-15 |
| Q-170 | `Retry-After` при исчерпании лимита Worker | **D-167:** при исчерпании лимита Worker не исполняется и отвечает платформа, поэтому `Retry-After` и конверт ставить некому; D-121 п. 1 сужен до двух случаев; проверка D-108 распознаёт «Worker не исполняется» по отсутствию конверта. Правка — [api-spec.md](api-spec.md), [architecture.md](architecture.md), [tech-stack.md](tech-stack.md) ADR-10; T-027, T-031 | 2026-09-15 |
| Q-169 | Ответ образа на `/s/*` не определён | **D-166:** в `selfhosted` путей `/s/*` нет — любой метод получает ERR-01 `404`; различимость даёт страница развёртывания и системная проверка образа. Правка — [api-spec.md](api-spec.md); T-172, T-176 | 2026-09-15 |
| Q-168 | `Content-Type` события не выбран | **D-165:** тело ЭП-09 — `application/json`; разных origin у страницы и приёмника нет по ИНВ-01, preflight не возникает. Правка — [api-spec.md](api-spec.md); T-184 | 2026-09-15 |
| Q-163 | `robots.txt` не описан | **D-164:** `robots.txt` и `noindex` выводятся из одного признака индексации сборки; три состава (закрыто, открыто с `Disallow: /s/` и `Sitemap:`, `selfhosted` без `Sitemap`); отдельных правил для ботов нет — выбор PO; managed robots.txt Cloudflare выключен. Правка — [api-spec.md](api-spec.md) ЭП-12, [security-and-access.md](security-and-access.md), `infra/dns/README.md`; задача T-218, T-201 | 2026-09-15 |
| Q-161 | Нужен ли `sitemap.xml` в профиле `selfhosted` | **D-163:** в `selfhosted` `sitemap.xml` нет; ENT-26 перечисляет различия состава профилей исчерпывающе; G-07 учитывает профиль. Правка — [data-model.md](data-model.md), [glossary.md](glossary.md), [api-spec.md](api-spec.md), [architecture.md](architecture.md), [test-plan.md](test-plan.md); задача T-222 | 2026-09-15 |
| Q-150 | Владение DNS и хранение токена деплоя | **D-162:** секретов автоматики два; токен деплоя с одним правом и сроком 6 месяцев в Cloudflare, только в environment `production` для `v*`; регистратор — не Cloudflare; второго владельца нет, второй носитель — отложенный доступ доверенного лица к набору восстановления, назначаемый PO вне репозитория. Выбор PO. Правка — [security-and-access.md](security-and-access.md), [tech-stack.md](tech-stack.md), [architecture.md](architecture.md), [launch-checklist.md](launch-checklist.md), `infra/dns/README.md`; задача T-215, T-011 | 2026-09-15 |
| Q-142 | `platformClass` не покрывает мобильный Chrome | **D-161:** классы порогов — `desktop` и `mobile`, класс определяется по `(pointer: coarse)` без чтения UA; числа `mobile` меряются на эмуляции мобильного WebKit с `source = emulated`. Правка — [data-model.md](data-model.md), [prd.md](prd.md), [test-plan.md](test-plan.md), [user-stories.md](user-stories.md), [content-guide.md](content-guide.md); задача T-221, T-141 | 2026-09-15 |
| Q-141 | Есть ли у служебной страницы `description` | **D-160:** запись служебной страницы получает обязательное `description`; G-06 проверяет каждый HTML сборки. Правка — [data-model.md](data-model.md), [api-spec.md](api-spec.md), [test-plan.md](test-plan.md), [user-stories.md](user-stories.md); T-040, T-043 | 2026-09-15 |
| Q-140 | Текста страницы выведенной позиции не существует | **D-159:** «Инструмент ‹Tool.name› выведен из каталога.» и «Открыть палитру», экран целиком, без причин и дат, событий нет. Правка — [content-guide.md](content-guide.md) §5, [data-model.md](data-model.md), [api-spec.md](api-spec.md), SCR-15; задача T-217 | 2026-09-15 |
| Q-234 | Порог G-02 берёт каждый HTML сборки, а D-88 — маршруты реестра | **D-156 п. 1:** маршрут для G-02 — каждый HTML собранного артефакта, надмножество маршрутов реестра; после T-040 множества сходятся. Вариант «буквально реестр» отвергнут: гейт ждал бы T-034, а HTML вне реестра выпал бы из счёта. Код не меняется. Правка — [decisions.md](decisions.md) D-88 (пометка), [test-plan.md](test-plan.md) §4.1 NFR-03 | 2026-09-11 |
| Q-235 | D-88 считает все инлайновые `<script>`, а гейт — только исполняемые | **D-156 п. 2:** слагаемое 1 — исполняемые инлайновые `<script>` (без `type`, JavaScript MIME type, `module`); JSON-LD, `importmap`, `speculationrules` — данные. Счёт «всех» отвергнут: блок данных, растущий с каталогом, съедал бы бюджет JS. Код не меняется. Правка — [decisions.md](decisions.md) D-88 (пометка), [test-plan.md](test-plan.md) §4.1 NFR-03 | 2026-09-11 |
| Q-236 | Состав шрифтов критического пути расходится между D-78, D-79 и дизайн-планом | **D-157:** критический путь — Manrope 400, Manrope 600, JetBrains Mono 400, ≤ 48 KB gzip; Mono 700 не прелоадится и догружается с областью результата. Строка G-02 перечисляет три файла явно, имена — договор с T-028. Правка — [decisions.md](decisions.md) D-78 п. 2 (пометка), [prd.md](prd.md) NFR-18, [design-plan.md](design-plan.md) §5.1, [screens-prompt.md](screens-prompt.md); строку `.size-limit.json` правит /fix T-016 | 2026-09-11 |
| Q-233 | Пространство имён `dokey` на GitHub занято, а адрес `github.com/dokey/dokey` уже зашит в код | **D-155:** организация — **`dokey-app`**, имя репозитория и образа остаётся `dokey`: `github.com/dokey-app/dokey`, `ghcr.io/dokey-app/dokey`. Пространство имён повторяет домен, модификатор живёт только в нём. Занимается в T-002 сразу; до первого подписанного релиза организацию можно переименовать, если `dokey` освободится, после — только пересмотром D-20. Отвергнуты ожидание владельца, `dokeyapp`, личный `tmpapr/dokey` с переносом позже. Адреса в коде правит T-213. Правки — [decisions.md](decisions.md) D-20 п. 3, [backlog.md](backlog.md) TECH-13, [roadmap.md](roadmap.md) §4, [implementation-plan.md](implementation-plan.md) §2.4, [content-guide.md](content-guide.md) CG-23, SCR-13, [tasks.md](tasks.md) T-002, T-022 | 2026-09-10 |
| Q-219 | Одни и те же две позиции каталога стоят втрое по-разному | **D-151:** рычаг меряется той же шкалой, что и путь, — размерами backlog; оценки D-06 сделаны до декомпозиции и без прототипирования (A-10). Каталог как рычаг — волна В-7 целиком, **1,75–2,75 дня** (2–6 % пути), полный рычаг В-11 плюс В-7 — **3,25–5,25** вместо «шести–восьми». Вывод D-49 не меняется, а усиливается: каталог срок не спасает. Правка — [roadmap.md](roadmap.md) §2.5, [brd.md](brd.md) §3, [prd.md](prd.md) §9, пометки в D-49 п. 4 и D-77 п. 1 | 2026-09-10 |
| Q-222 | Процедуры синхронизации промта и кита не существует | **D-152:** промт — рецепт, а не живой документ; единственная процедура синхронизации — приёмка §3 при перепрогоне, выполняет Maintainer, шапка несёт штамп «сверено с китом». Правило направления разделено по классу: **значения токенов** — прав кит, правится промт; **механизм, назначенный решением D-NN**, — прав D-NN, и коммит решения правит все носители сразу (случай D-103). Генерируемый `_ds_bundle.js` ненормативен. Гейт отвергнут: разбор прозы хрупок и механизма не ловит. Правка — [design-system-prompt.md](design-system-prompt.md) §0.2, §3 | 2026-09-10 |
| Q-224 | Две сущности модели не имеют строки в глоссарии | **D-153:** заведены **ENT-31 «Пункт контракта» `ContractClause`** (§1.5, адресат D-21 и G-14) и **ENT-32 «Вопрос FAQ» `FaqItem`** (§1.1, от трёх до пяти на лендинг). Модель данных не меняется, заголовки data-model §2 получили номера. Вариант «части ENT-25 и ENT-06» отвергнут: запрет на смену `id` остался бы без адресата. Правка — [glossary.md](glossary.md) §1, [data-model.md](data-model.md) §1, §2 | 2026-09-10 |
| Q-225 | Две зависимости сборки не имеют строки в сводке версий | **D-154:** сверка манифеста нашла **шесть** таких пакетов, а не два (плюс `@vitest/browser`, `@playwright/test`, `@size-limit/file`, `@types/node`). Спутник — пакет, существующий только ради принятого инструмента, — называется с версией в строке этого инструмента и наследует её проверку и ADR, пин точный; §3 перечисляет весь манифест, зависимость без строки — дефект PR. Все шесть сверены с npm dist-tags 2026-09-10; `prettier-plugin-astro` 1.0.0 и `@types/node` 24.13.4 записаны как вышедшие после каркаса, пины не двигались. Правка — [tech-stack.md](tech-stack.md) §3 | 2026-09-10 |
| Q-229 | Аллоулист адресов Cloudflare не является границей доверия, а SEC-10 проверяет не с той стороны | **D-147:** плечо закрывается двумя рубежами вместо одного — **TLS-фронт VPS отвечает `404` на всё, кроме `POST /s/event`** (включая путь панели), и **Worker передаёт общий секрет** из своей конфигурации, которого в браузере нет (ADR-13 не нарушен). SEC-10 переформулирован: проверка идёт запросом **с адреса Cloudflare**, а не с произвольного. Cloudflare Tunnel отвергнут по эксплуатации, mTLS — по второй цепочке доверия | 2026-09-10 |
| Q-230 | Сверка подписи описана без пиннинга идентичности и потому ничего не доказывает | **D-148:** проверка обязана пиннить `--certificate-oidc-issuer https://token.actions.githubusercontent.com` и `--certificate-identity-regexp` на репозиторий и путь релизного workflow по тегу; оба значения публикуются на SCR-10 вместе с командой целиком. Правило распространяется на образ (SEC-13) и на манифест веб-сборки (SEC-05). Команда без пиннинга считается непройденной проверкой, а не упрощённой | 2026-09-10 |
| Q-231 | Предмет Q-185 сузился при закрытии: Trusted Types выпали | **D-149:** `require-trusted-types-for 'script'` добавлен в набор SEC-22 и в обе копии политики; политик Trusted Types не заводится ни одной — по контракту `text-only-dom` продукт в DOM-стоки не пишет. Директива ловит то, чего не видит линт по `src/**`: присваивание из зависимости или из чанка инструмента. Ограничение записано рядом: исполняется в Chromium, поэтому это второй рубеж, а не замена линта | 2026-09-10 |
| Q-232 | HSTS в self-hosted-образе ставится безусловно, а документ обещает обратное | **D-150:** HSTS ушёл из образа — заголовок ставит только прод, где зона `.app` и так в HSTS-preload (ИНТ-09). Это **первое объявленное различие двух копий политики**, и `gate:headers-parity` научен ему асимметрично: шесть общих заголовков сверяются на равенство, HSTS обязан быть в `_headers` и обязан отсутствовать в `nginx.conf` — гейт стал сильнее, потому что ловит и возвращение заголовка в образ | 2026-09-10 |
| Q-206 | Историю принимает тот же человек, который её реализовал | **D-143:** второй пары глаз нет и не появится; на её место поставлено правило §0 test-plan, поднятое до нормы приёмки — **критерий приёмки и есть тест**. Каждый критерий выражается исполняемым прогоном либо сверкой с документом, написанным раньше и не этим PR; приёмка = сверка отчёта со списком. Критерий, не выражаемый ни тем ни другим, — дефект истории. Риск назван в roadmap §5 как **РС-09**, а не спрятан | 2026-09-10 |
| Q-211 | US-007 и US-051 назначены двум волнам одновременно | **D-144:** верны §1.2 и §1.9 — US-007 в В-0, US-051 в В-9 (D-58, D-59), план реализации уже развёрнут по ним. Строка В-11 в backlog §1.1 — **список демонтажа первым по RISK-06** из §5.2, а не волна поставки; правится она, план работ не меняется | 2026-09-10 |
| Q-214 | Критерий готовности релиза требует семь гейтов, а их четырнадцать | **D-145:** пункт 11 roadmap §2.4 читается как **G-01…G-14 зелёные, без исключений**; число словом не называется — только диапазоном адресов, потому что именно словесное «семь» разошлось с составом TECH-06. Исход «нечего проверять» на релизе запрещён. **T-202 разблокирована**, задач с пометкой `⚠ Q-NN` не осталось | 2026-09-10 |
| Q-216 | A-15 не пересчитана после решения, которое её увеличило | **D-146:** A-15 — **3,5–6,5 человеко-дня** (2,5–4,5 подготовки плюс 1–2 на работы канала M-12 и подачу TECH-14, D-77 п. 2). Работы канала остаются внутри A-15, а не выносятся в четвёртое допущение. Полный путь A-06 + A-15 + A-16 пересчитан до **49,75–80,25 человеко-дня**; число обновлено в brd, backlog, roadmap и implementation-plan | 2026-09-10 |
| Q-201 | NFR-07 не входит ни в NFR-17, ни в TECH-06, а половина его величины не измерима в принципе | **D-139:** INP p75 снят как полевая величина, у которой у продукта нет источника (закрытый список по D-44 не растёт ради своих слепых пятен, Lighthouse даёт TBT). На его место — лабораторная **худшая задержка отклика на взаимодействие ≤ 200 мс** в прогоне RUN-03 (`PerformanceObserver` по `event`). Обе половины стали измеримыми до пользователей, и NFR-07 вошёл в NFR-17 | 2026-09-09 |
| Q-202 | «100 % сценариев P0a проходимы с клавиатуры» не имеет ни единицы, ни счётчика | **D-140:** единица та же, что у NFR-08 — сценарий FL-NN исполняемого прогона; счётчик другой: **доля сценариев, доводимых до конца только клавиатурой с первого действия, = 100 %**, порог провала — любое значение ниже ста. Проверяет **тот же гейт G-11** вторым числом; пятнадцатый гейт не заводится. Снятие «100 %» отвергнуто: клавиатурная достижимость — безусловный критерий WCAG 2.2 AA | 2026-09-09 |
| Q-203 | Состав чек-листа скринридера не выписан, и пара «скринридер × браузер» не названа | **D-141:** пара — **NVDA + Firefox на Windows** (машина мейнтейнера, бесплатный скринридер, самая распространённая связка). Пять пунктов выведены из уже написанных критериев: заголовок и `h1` при входе · имя и подсказка поля ввода · появление результата live-регионом без потери фокуса · «Скопировано» как статус · офлайн и новая версия. Результат идёт в релизную заметку включая провалы; вторая пара не заводится — машины Apple нет | 2026-09-09 |
| Q-204 | Матрица NFR-14 проверяется не тем, чем объявлена, и порог `mobileSafari` меряется на эмуляции | **D-142:** `ENT-22.source` получил третье значение **`emulated`**, и оговорка о происхождении печатается рядом с числом там, где оно публикуется как обещание — так же, как это делает `provisional`. NFR-14 переписан под то, что проверяется: движки Playwright; «последние две мажорные версии вендоров» остаётся намерением совместимости. Ферма реальных устройств отвергнута по экономике §8 BRD; условие пересмотра названо — первая жалоба, воспроизведённая на устройстве | 2026-09-09 |
| Q-194 | Приёмник хранит больше, чем отправляет браузер, и обещание описывает только вторую половину | **D-135:** производные приёмника — `browser`, страна, ОС, класс устройства, язык — заведены в реестр ПДн как **PD-09** со сроком 12 месяцев (D-113) и публикуются на SCR-10 **третьим списком**. Обещание переформулировано точно: ENT-29 описывает то, что покидает вкладку; ADR-13 уточнён. Обещать нехранение того, что чужой продукт считает по построению, отвергнуто — исполнение зависело бы не от нас | 2026-09-09 |
| Q-195 | Гейт закрытого списка проверяет поля и не проверяет имена | **D-136:** заведён закрытый список из пяти имён (`shell_opened`, `tool_opened`, `result_received`, `result_failed`, `type_switched`), и проверяет его **тот же гейт G-05**, что и поля: имя вне списка останавливает сборку. Новый гейт не заводится — предмет один. Список публикуется на SCR-10, расширение идёт процедурой «публикация → список → код» | 2026-09-09 |
| Q-196 | Счётчик ввода: точка успеха сценария и категория отказа — одно состояние | **D-137:** в `Result.status` заведено состояние **`counted`** (ввод получен, тип не распознан, показаны счётчик и чипсы кодирования), и **событий оно не порождает вовсе**. `result_failed(type_not_detected)` уходит только там, где человек явно запросил разбор. PM-02 перестал называть топовой причиной работающий по замыслу сценарий; цена — сценарий J-2 в метриках невидим, и это принято | 2026-09-09 |
| Q-199 | p90 не выводится из девяти прогонов | **D-138:** p90 снят из NFR-02. Величина — **медиана девяти ≤ 3 с** и **худший из девяти ≤ 6 с** на той же выборке: порог у максимума мягкий, потому что максимум шумен. Тридцать прогонов отвергнуты по О-01, скользящее окно — потому что число перестало бы относиться к останавливаемой сборке | 2026-09-09 |
| Q-190 | К какому `result_received` относится поле `ttr` | **D-131:** только к **первому результату маршрута** — тому, который отвечает определению TTR из D-03. На шагах петли и на копировании поле опускается (контракт ЭП-09 допускает: поле необязательное). Переход по петле открывает другой маршрут, там отсчёт начинается заново. PM-04 перестал зависеть от доли петли в поведении | 2026-09-09 |
| Q-191 | Отмена долгой операции отправляет событие, у которого нет категории | **D-132:** заведена четвёртая категория **`cancelled`**. Отмена остаётся событием: её частота — прямой сигнал «операция слишком долгая» и читается рядом с `limit_exceeded` («мы не дали» против «человек не стал ждать»). Правка перечня идёт процедурой публичного списка: SCR-10 публикует состав раньше, чем код его отправляет | 2026-09-09 |
| Q-192 | Экран «Инструмент недоступен» не порождает ни одного события | **D-133:** SCR-15 отправляет `result_failed` с одной из двух новых причин — **`tool_not_found`** (спрос на позицию, которого ждёт гипотеза P0b) и **`tool_unavailable`** (три технических повода D-63). Смешивать нельзя: первое ведёт к решению о составе каталога, второе — о сборке и кеше. Шестое событие не заводится, пять событий P0a остаются пятью | 2026-09-09 |
| Q-193 | M-14 не имеет ни прокси-метрики, ни правила распознавания канала | **D-134:** заведена **PM-10** «визиты в месяц из поискового канала» и закрытый список поисковых доменов в analytics-spec §1.1 — по образцу списка площадок M-12, составляется до релиза. Классификация происходит при чтении: ни нового события, ни нового поля. Реферер вне обоих списков считается «прочим» — метрика скорее занизит канал, чем присвоит чужие визиты | 2026-09-09 |
| Q-183 | Доступ ROLE-03 к отдельным записям аналитики не ограничен, хотя вся модель метрик агрегатная | **D-127:** панель приёмника **в интернет не публикуется** — фаервол пускает только адреса Cloudflare и SSH (D-94 п. 3), доступ через SSH-туннель; **выборка по отдельному визиту или посетителю запрещена явно**, в том числе ради отладки. Запрет держится дисциплиной, а не правами, и это сказано прямо: ценность записи в том, что нарушение отличимо от нормы | 2026-09-09 |
| Q-184 | Полный набор заголовков безопасности не назначен: объявлены две директивы CSP из необходимых восьми | **D-128, закрыт фактом:** T-010 (2026-09-08) положила полный набор SEC-22 в обе копии — двенадцать директив CSP, HSTS, `Referrer-Policy`, `X-Content-Type-Options`, `Permissions-Policy`, COOP и CORP. Открытым оставался не набор, а его описание: таблица api-spec §1.6 называла пять заголовков из девяти и приведена в соответствие с файлом | 2026-09-09 |
| Q-185 | Запрет `innerHTML` для пользовательских данных не назначен ничем | **D-129:** мера SEC-21 получила исполнителя из трёх частей — **одиннадцатый пункт контракта ENT-25 `text-only-dom`**, запрет `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `document.write` и `srcdoc` в `src/**` на шаге линта (T-013), враждебные образцы в корпусе фикстур (T-075). Пятнадцатый гейт отвергнут: число гейтов зашито в критерий релиза и в десяток документов | 2026-09-09 |
| Q-188 | У PM-03 нет знаменателя | **D-130:** `визиты с ≥ 1 type_switched / визиты с ≥ 1 (result_received ∨ result_failed)` — знаменатель есть визиты, где человек дошёл до операции. Третья единица счёта не заводится: «операция» потребовала бы нового события или правила склейки ради одной контрольной метрики. CB-04 читается как «автоопределение типа не стало ошибаться чаще» | 2026-09-09 |
| Q-176 | У прод-артефакта нет провенанса: подписан образ, а пользуются сайтом | **D-123:** релизный конвейер публикует **манифест `sha256` файлов веб-артефакта и аттестацию provenance на манифест** той же keyless-цепочкой, что и на образ; процедура сверки — на SCR-10 «Как это проверить». ROLE-01 получает возможность проверить, что прод собран из опубликованного исходника, и публичность кода перестаёт быть декларацией | 2026-09-09 |
| Q-178 | Ст. 5(3) ePrivacy шире, чем cookie, и под неё попадают две вещи из P0a | **D-124:** убраны обе. `screenResolution` **удалён из ENT-29 — список стал семью полями** (потребителя среди метрик не было); служебный флаг «подсказка показана» переехал в память оболочки, и REQ-ENTRY-05 читается как «один раз за сессию вкладки». На устройстве остаётся только precache, защитимый исключением «строго необходимо». Баннера согласия по-прежнему нет | 2026-09-09 |
| Q-179 | Контакта для обращения субъекта данных и имени оператора нет ни в одном документе | **D-125:** контролёр — **мейнтейнер как физическое лицо**, имя то же, что в `NOTICE`; контакт — **`privacy@dokey.app`**, алиас на своём домене, заводится в T-001. Обе строки — на SCR-10. Срок ответа не обещается: его назначает ст. 12 GDPR | 2026-09-09 |
| Q-180 | Журналы nginx на VPS и журналы edge содержат IP и не объявлены нигде | **D-126:** адрес не пишется. Журнал TLS-фронта задан явно без `$remote_addr` и User-Agent, журналы edge не включаются и не хранятся; PD-05 и PD-06 сняты из реестра ПДн. Хранилище персональных данных остаётся ровно одно — события в PostgreSQL (IP нет по D-96, срок по D-113). Диагностический журнал включается вручную и гасится, каждое включение — строка в `infra/vps/maintenance.md` | 2026-09-09 |
| Q-165 | Приёмник открыт и не подписан: чем детектируется набивка метрик, на которых стоят M-08, M-09, M-15 | **D-119:** ничем — вместо сторожа записана граница применимости. Все девять прокси-метрик читаются **как изменение к предыдущему релизу**, абсолютное значение решением не служит; M-08, M-09 и M-15 достигнуты знаком и величиной изменения, а не порогом. Признак аномалии отвергнут: набивка портит ряд целиком и видна в сравнении сама | 2026-09-09 |
| Q-166 | analytics-spec, на который ссылается контракт события, ещё не написан | **D-120:** документ написан 2026-09-07, имена закреплены реестром §3 — EVT-01…EVT-05, а не первым коммитом; api-spec §4.8 ссылается на реестр. Вторая половина вопроса — что гейт проверяет состав полей, но не значения `name`, — живёт отдельно как **Q-195** и этим решением не закрывается | 2026-09-09 |
| Q-167 | Конверт ошибки предполагает от прокси то, чего ADR-10 ему не поручал | **D-121:** обязанность назначена явно — Worker формирует ответ ровно в трёх случаях (отказ по аллоулисту ERR-03/ERR-09, недоступность апстрима ERR-05, исчерпание лимита ERR-04), и только там применяется конверт §3.2. **Ответ приёмника проходит насквозь** и не переупаковывается. NFR-11 не ослаблен: тело существует для проверки D-108, а не для вкладки | 2026-09-09 |
| Q-172 | Лицензия проекта не названа ни одним документом | **D-122:** **Apache-2.0** — одна на код, образ и документацию репозитория. `LICENSE` и `NOTICE` в корне положила T-003; идентификатор SPDX встал в `openapi.yaml` `info.license` (последнее замечание валидатора), лицензия названа в ADR-15. MIT отвергнут — теряет патентную оговорку, на которую смотрит ROLE-05 | 2026-09-09 |
| Q-145 | Объём администрирования VPS не назван в часах — у триггера ST-04 нет момента срабатывания | **D-115:** часы **не назначаются** (D-43 п. 3 — число живёт там, где его проверяет машина). Заведён журнал `infra/vps/maintenance.md`, строка на каждое ручное вмешательство; **ST-04 срабатывает при двух календарных месяцах подряд с непустой записью**. Момент срабатывания появился, самооценка в часах не понадобилась | 2026-09-09 |
| Q-148 | Совместимость проверки типов каркаса с TypeScript 7 | **D-116:** тип-чек — `astro sync && tsc --noEmit`, одна версия компилятора в дереве; `astro check` не ставится — вторая версия TS давала бы расхождение диагностик локально и в CI. Цена названа прямо: выражения внутри `.astro` типами не проверяются, поэтому **логика в шаблонах запрещена**, вся она в `.ts` | 2026-09-09 |
| Q-159 | Целевой путь, на который прокси переписывает `/s/event`, не закреплён ничем | **D-117:** константа Worker'а — `https://<vps>/s/event`, тот же путь, что и снаружи; переписывание на путь Umami делает TLS-фронт VPS, который и так стоит там под сертификат (D-94). Путь чужого продукта живёт в одной строке конфигурации `infra/vps/` и виден в диффе при смене мажора | 2026-09-09 |
| Q-160 | HTTP-статус маршрута выведенной позиции не выбран | **D-118:** `200` плюс `noindex`; запись вне sitemap, ссылок в сборке ноль. Сохранённый ярлык и `start_url` установленного PWA продолжают открываться. `410` отвергнут по устройству поставки: через Worker по D-97 идёт ровно `POST /s/event`, статус на статическом пути выдать нечем | 2026-09-09 |
| Q-153 | Срок хранения событий в PostgreSQL под Umami не назначен ничем | **D-113 п. 1:** **12 месяцев**, удаление ежемесячным заданием на VPS, число публикуется на SCR-10. Довод из D-96: псевдоним считается с суточной ротацией соли, поэтому срок — вопрос диска и позиции, а не приватности. 90 дней отвергнуты — не покрывают предыдущий релиз при квартальном темпе | 2026-09-09 |
| Q-157 | Поведение прокси при исчерпании суточного лимита Worker не выбрано | **D-113 п. 2:** *fail open*, выставленный **явно**, а не унаследованный умолчанием провайдера. NFR-11 требует нуля видимых ошибок, отправитель ответ игнорирует. После D-97 обход задевает только аналитику: проксируется один путь, сайт через Worker не идёт | 2026-09-09 |
| Q-152 | У расписания пересборки два выключателя, и второй не наш | **D-112 п. 1:** ежемесячная пересборка **записывает исход коммитом** — один коммит делает дату сборки видимой (D-59, T-179) и сбрасывает 60-дневный таймер GitHub. Самопроверкой это не закрывалось: правило срабатывает от неактивности репозитория, отключая все расписания разом, включая синтетическую проверку D-108. Плюс сверка `state` расписаний на релизе. Пустой коммит-пульс отвергнут | 2026-09-09 |
| Q-155 | CI тянет базовые образы с Docker Hub анонимно | **D-112 п. 2:** `node:24-alpine` и `nginx:1.30.4-alpine` зеркалятся в GHCR по digest и тянутся оттуда. Зеркало не добавляет поставщика, а убирает: `ИНТ-07` уходит из зависимостей конвейера и остаётся источником зеркала при обновлении версии. Аутентификация в Docker Hub отвергнута — поставщик остался бы, а секрет требует ротации при открытом Q-150 | 2026-09-09 |
| Q-154 | Окружения staging нет, а переход с прошлой прод-сборки на новую не проверяется | **D-111 п. 2:** миграция SW проверяется прогоном на **одном origin** в CI — адрес отдаёт предыдущий релизный артефакт, SW регистрируется, затем подмена на новый. Scope и кеши те же, что у пользователя, и проверка идёт до деплоя. Точнее staging (тот на другом origin) и дешевле прода (у ST-07 нет дешёвого отката) | 2026-09-09 |
| Q-228 | Критерий готовности T-001 требовал ответа прода, который поднимает T-011 | **D-114:** приёмка разведена — T-001 принимается делегированием зоны (`dig NS dokey.app +short`), `curl -sI https://dokey.app` и `strict-transport-security` уходят в готово T-011, где заголовок впервые существует. HSTS в панели Cloudflare **не включается**: ST-03 и D-107 не ослабляются ради формы приёмки одной задачи | 2026-09-09 |
| Q-215 | Заводить ли staging и что именно он проверяет | **D-111 п. 1:** `stage.dokey.app`, тот же артефакт, профиль без аналитики, `noindex` и `Disallow: /`. Предмет — три пункта RUN-17, не прогоняемые ни на dev, ни в CI. **Записано прямо: миграцию SW staging не проверяет** — другой origin, другой scope | 2026-09-09 |
| Q-205 | Генерация больших вводов не описана ничем | **D-110:** `scripts/gen-large-input.ts` — генератор из зерна, параметризованный числом порога **из реестра**; три формы по трём осям (вес строки, глубина вложенности, размер пакета). Вывод не коммитится, воспроизводимость даёт пара «ось + зерно». Образцы в репозитории и генерация без зерна отвергнуты | 2026-09-09 |
| Q-138 | `expectedChips` фикстуры зависит от состава сборки | **D-109 п. 1:** размечается один раз по полному каталогу, прогон пересекает с активными ступенями сборки. Состав при жёстком минимуме — не другой список, а тот же с выпавшими элементами, значит его выводят, а не размечают. Бесплатно: `expectedStep` обязан совпадать с головой `expectedChips` | 2026-09-09 |
| Q-139 | Где проходит граница между данными реестра и его кодом | **D-109 п. 2:** **оболочка — это `src/` минус `registry/` и `tools/<toolId>/`**; критерий читается как «diff не выходит за реестр, каталог новой позиции и контент и не трогает чужие позиции». Вынести предикат из реестра нельзя: определитель работает до загрузки чанков. Признано прямо: «только данные» было неточностью. Попутно исправлено расхождение `src/README.md` с моделью | 2026-09-09 |
| Q-156 | Прод не наблюдается ничем | **D-108:** синтетическая проверка в расписании Actions каждые 6 часов, пять утверждений одним прогоном (прод, метод прокси, два упразднённых пути, приём события). Issue после **двух** неуспехов подряд, повторный не заводится, пока открыт предыдущий. Пункты 2–4 закрывают заодно проверку аллоулиста на проде, отложенную D-97. Наследует Q-152 | 2026-09-09 |
| Q-158 | Заголовки ответа существуют в двух копиях, и их равенство не проверяется | **D-107:** единственный источник — `infra/headers/policy.ts`, из него выводятся `_headers` для Cloudflare и `headers.conf` для nginx (диалекты разные, и это довод за один источник, а не против). `gate:headers-parity` меняет предмет: перегенерировать и упасть на диффе — строго сильнее сравнения семи полей, покрывает кеш-правила бесплатно | 2026-09-09 |
| Q-207 | Гейты никем не проверены на срабатывание | **D-106:** гейт заводится или меняется только вместе с негативной проверкой в том же PR — пункт в `.github/pull_request_template.md`. Покрытие проверяет сам набор: `tests/gates/negative` берёт список из `GATES` в `run-all.ts` и падает, если у гейта нет случая. Прогон входит в конвейер PR. Пятнадцатый гейт и «правило без самопроверки» отклонены — второе прямо по прецеденту D-98 | 2026-09-09 |
| Q-147 | Модульные скрипты переживают клиентский переход, teardown — обязан не переживать | **D-105:** триггер — `astro:before-swap`, не выгрузка документа. Названо главное, чего не было ни в одном документе: **буфер, записанный инициатором текущего перехода, teardown не трогает** — иначе «Продолжить с этим» ломается на своём же механизме; обнуляется непрочитанный буфер прошлого. Записано в ENT-23 и ENT-30. Проверка на каркасе — критерий T-048, а не условие закрытия | 2026-09-09 |
| Q-227 | Идентификатор сайта покидает браузер и не назван полем | **D-104:** `website` подставляет прокси на edge, браузер его не отправляет вовсе. ENT-29 остаётся восемью полями и остаётся **исчерпывающим** — слово сохранено буквальным, потому что на нём стоит процедура опровержения SCR-10. Девятое поле и переопределение перечня отвергнуты | 2026-09-09 |
| Q-223 | Спецификация `Icon` предписывает CDN, запрещённый ИНВ-01, а design-plan считает правку внесённой | **D-103:** правка внесена в кит — четыре файла переведены на инлайн-спрайт `<use href="#icon-…">`, контракт пропсов не изменился (D-54 п. 3). Промт правки не потребовал: он уже предписывал спрайт и запрещал CDN — то есть правило §0.2 «правится промт, не кит» здесь дало бы обратный результат, что усиливает Q-222 | 2026-09-09 |
| Q-212 | Девять TECH-задач не входят ни в один бюджет | **D-102:** заведено допущение **A-16** «сквозная инфраструктурная работа, 8–17 д». A-06 остаётся объёмом историй, A-15 — подготовкой; оба сохраняют смысл. Полный путь до релиза = A-06 + A-15 + A-16 = 48,75–78,25 дня по implementation-plan §3.1 — числом плана, а не дискавери | 2026-09-09 |
| Q-213 | M-03 наступает позже точки, где она нужна | **D-102:** предохранителей два. **Ранний** — сверка каркаса в конце Э-1 (РС-05 уже назначил ему тот же вес), срабатывает на месяц раньше и до входа в окно релиза. **Поздний** — M-03 переносится с 2026-10-05 на реалистичную дату и становится подтверждающим, а не единственным. Опирается на D-75 п. 3, который уже разделил сверку каркаса и сверку позиции | 2026-09-09 |
| Q-200 | NFR-05 не имеет исполнителя, хотя NFR-17 объявляет его условием релиза | **D-101:** величины разнесены по природе — клиентский переход ≤ 300 мс в G-01 через `performance.measure` в E2E-08, глубина ≤ 2 нажатий в G-11 автотестом по позициям реестра (D-73 п. 4). Пятнадцатый гейт и снятие NFR-05 из NFR-17 отклонены. Гейтов остаётся четырнадцать | 2026-09-09 |
| Q-198 | Чем меряется лабораторный TTR: у Lighthouse такой величины нет | **D-100:** процедура D-75 п. 1 применяется к своей сборке и даёт приговор G-01 наравне с Lighthouse — от навигации до результата в DOM по явному селектору, медиана девяти прогонов, фиксированный троттлинг. RUN-09 получает гейт. Переформулировка NFR-01 и NFR-02 в навигационные метрики отклонена: TTR — north star D-03. Статистика p90 — Q-199 | 2026-09-09 |
| Q-189 | Событие входа введено решением и не имеет требования | **D-99:** заведено REQ-METR-09 (Should, M-16…M-19) и история US-068 в В-10, задача T-208 в Э-10. Дыра была шире вопроса — истории на событие тоже не было. Новых полей не появляется: маркер живёт внутри уже разрешённого `url`. Счёт историй 67 → 68, задач 207 → 208 | 2026-09-09 |
| Q-182 | Права ROLE-06 не защищены ничем: гейты правит тот же, кого они ограничивают | **D-98:** формулировка ROLE-06 приведена к правде — выпуск мимо гейтов предотвращён защитой ветки, снятие защиты предотвратить нельзя, но оно обнаруживается сверкой со слепком `infra/github/branch-protection.json` на релизе. G-14 расширен с `Tool.id` до всех пожизненных идентификаторов. Разделение ROLE-04 снялось само — прав записи у контрибьютора нет | 2026-09-08 |
| Q-220 | Обещание «мимо гейтов выпустить нельзя» держится дисциплиной, а не механизмом | **D-98:** критерий переписан на достижимый и получил механизм — релизный конвейер сверяет фактические настройки защиты со слепком в репозитории и падает при расхождении. Обойти можно, сделать это незаметно — нет | 2026-09-08 |
| Q-181 | Правило переписывания `/s/*` — маска, а не список путей | **D-97:** аллоулист из одного элемента `POST /s/event`, целевой адрес константой, отказ отвечает edge не доходя до VPS: ERR-03 `405` на чужой метод, новый **ERR-09** `404` на любой другой путь под `/s/`. Проверяется системным прогоном в CI против `wrangler dev` на каждой сборке — синтетическая проверка прода отклонена, чтобы не вешать P1 на открытый Q-156 | 2026-09-08 |
| Q-177 | Гео не подтверждено решением, местоположение VPS не названо, 152-ФЗ требует локализации | **D-96:** гео — RU + EN. Вилка «VPS в РФ против VPS в ЕС» снята устранением предмета: прокси хеширует адрес на edge с суточной солью и передаёт псевдоним, приёмник IP не получает и не пишет. Локализация к нему неприменима, площадка перестаёт быть юридически связанной. Запасной вариант при провале сверки в T-027 — VPS в РФ плюс уведомление. Остаток темы — Q-180 | 2026-09-08 |
| Q-175 | Гейт нулевого egress проверяет origin, а не содержимое | **D-95:** G-04 получает второе, положительное условие — в прогоне RUN-11 подстрока токена-фикстуры не встречается ни в одном URL, заголовке и теле запроса, независимо от origin. Заголовки добавлены к предложению вопроса. Отдельный гейт G-15 отклонён: четырнадцать объявлены в шести документах, а Q-210 и Q-214 уже про расхождения в составе | 2026-09-08 |
| Q-174 | Плечо «прокси → приёмник» описано двумя документами противоположно | **D-94:** только HTTPS. Сертификат Let's Encrypt на поддомене, TLS терминируется на VPS, HTTP не слушает, фаервол — только адреса Cloudflare и SSH. Cloudflare Tunnel отклонён по эксплуатации (Q-145 открыт), самоподписанный — как половина меры. ИНТ-03 приведён в соответствие | 2026-09-08 |
| Q-173 | Клиент приёмника аналитики отдаётся с чужой машины под нашим origin (THR-01) | **D-93:** `/s/script.js` не существует — ЭП-08 упразднён, номер выведен из обращения. КМП-07 шлёт `POST` на `/s/event` сам; прокси сужен до одного пути. Вендоринг в артефакт (предложение вопроса) отклонён: оставляет чужой код и обязанность перевендоривать без владельца. Поднял Q-227 | 2026-09-08 |
| Q-171 | Закрытый список полей назван семью, а перечислен восемью | **D-92:** полей восемь, `name` — восьмое. Довод — определение ENT-29 как **исчерпывающего** перечня покидающего браузер. Предложение analytics-spec §2 оставить семь отклонено. Приведены в соответствие одиннадцать документов; гейт REQ-METR-04 проверяет мощность восемь. Проверка значений `name` — по-прежнему Q-195 | 2026-09-08 |
| Q-164 | Целевой путь `start_url` манифеста PWA не определён | **D-91:** `/?src=pwa`. Корень становится пятой записью реестра (`pageId` `root`, маршрут `/`) и отдаёт оболочку с открытой палитрой. Не инструмент — потому что вывод позиции по D-82 п. 3 навсегда сломал бы все установленные копии. screen-map §1 и analytics-spec EVT-01 приведены в соответствие, заведена T-207 | 2026-09-08 |
| Q-162 | Шесть `Tool.id` и четыре `pageId` не названы ни одним документом, при том что пожизненны | **D-90:** `json`, `jwt`, `base64`, `url`, `unix-timestamp`, `uuid`; страницы — `how-to-verify`, `keep-handy`, `benchmark`, `self-hosting`. Правило: slug называет формат, а не глагол, потому что `id` неизменен навсегда, а инструменты растут. Таблицы — [api-spec.md](api-spec.md) §4.1, §4.2 | 2026-09-08 |
| Q-151 | Service worker требует защищённого контекста, а образ в интранете стоит на голом `http` | **D-89:** три части по образцу D-64 — NFR-12 получает условие «в защищённом контексте» ([prd.md](prd.md) §7), страница развёртывания US-045 — раздел про TLS внутри периметра, интерфейс — объявленное состояние ([content-guide.md](content-guide.md) §4). Подделка возможности по-прежнему запрещена | 2026-09-08 |
| Q-149 | «Initial JS» не определён машинно, а NFR-03 задаёт 40 KB gzip | **D-88:** инлайновые `<script>` собранного HTML плюс всё достижимое из `<script src>` по статическим `import`; `import()` исключён. Порог — максимум по маршрутам реестра, «не растёт» — относительно максимума предыдущего релиза. Строка NFR-03 в [test-plan.md](test-plan.md) §4 приведена в соответствие | 2026-09-08 |
| Q-146 | Umami 3 умеет больше, чем разрешает закрытый список полей ENT-29 | **D-87:** проверено по документации — replay, heatmaps и Core Web Vitals выключены по умолчанию, а сбор реплея требует отдельного `recorder.js`. `script.js` подключается с `data-auto-track="false"`, рекордер не подключается никогда. Отключение структурное: три независимых барьера. Таблица — [tech-stack.md](tech-stack.md) ADR-11. Побочно поднял Q-181 до P1 | 2026-09-08 |
| Q-143 | Опыт мейнтейнера с Astro, островами и ванильным TypeScript неизвестен | **Закрыт фактом, не решением:** мейнтейнер работал с Astro — острова и границы чанков знакомы на практике. Надбавка на изучение в оценку не вносится, A-06 остаётся 35–40 человеко-дней, ADR-01 перестаёт читаться как рекомендация под допущением. Записано в [tech-stack.md](tech-stack.md) §0.1 | 2026-09-08 |
| Q-144 | Хостер не выбран ни одним решением, а требования к нему жёсткие | **D-86:** Cloudflare Workers со статическими ассетами, wrangler из Actions; вторая внешняя зависимость поставки принята, цена переезда — часы (ST-03). Аккаунта не было — заведение вошло в T-001 первым шагом. Разбор альтернатив — [tech-stack.md](tech-stack.md) ADR-10 | 2026-09-08 |
| Q-218 | Публичность прода до тега релиза: индексация, состав заглушки, смысл «первого публичного упоминания» | **D-85:** индексация закрыта целиком до `v1.0.0` (T-012 → T-201); заглушка показывает имя, категорию и ссылку на репозиторий, обещаний не даёт (CB-08); открытие домена — не «первое публичное упоминание» D-20 п. 2, первое впечатление расходуется объявлением (D-36). Тексты — [content-guide.md](content-guide.md) §5 | 2026-09-08 |
| Q-217 | Перенос образа, подписи, provenance и SBOM из В-9 в нулевой этап требовал подтверждения PO | **D-84:** перенос подтверждён — TECH-09, TECH-10 и инфраструктурная часть US-048 принадлежат Э-0; в В-9 остаются US-049 и US-050. Приведён в соответствие [backlog.md](backlog.md) — единственный документ, который расходился с планом, дорожной картой и задачами | 2026-09-08 |
| Q-134 | Число «недавних» и правило их вытеснения не заданы | **D-83 п. 2:** до пяти записей, вытеснение по давности открытия, повторное открытие поднимает наверх. Живут в памяти оболочки и перезагрузку не переживают: в закрытом списке служебных ключей (D-71) им места не выделяется — «недавние» это удобство внутри сессии, а не механизм входа, для входа есть три пути D-62. Пустое состояние из content-guide §5 остаётся верным дословно | 2026-09-07 |
| Q-129 | Имя и состав записи единичной фикстуры не закреплены | **D-83 п. 1:** в глоссарий заводится ENT-21a `Fixture` — имя, которым модель уже пользовалась как рабочим. Состав: `id`, синтетическое значение, ожидаемая ступень, ожидаемый состав и порядок чипсов (требование D-73), признак неоднозначности. Правило одного слова восстановлено: коллекция — `FixtureCorpus`, запись — `Fixture`, оба имени в нормативном документе | 2026-09-07 |
| Q-137 | Разобранный пример первого экрана — данные, у которых нет владельца | **D-82 п. 5:** поле `example` принадлежит записи реестра: пример уникален для инструмента, синтетичен (REQ-TRUST-06) и рендерится на сборке (REQ-CORE-02). У генератора поле пустое — там роль примера играет сгенерированное значение (D-81), и это единственное исключение, названное явно | 2026-09-07 |
| Q-136 | Ищет ли палитра по синонимам и русской раскладке | **D-82 п. 4:** да. Запись реестра получает поле `synonyms` (русские и обиходные написания), палитра нормализует русскую раскладку (`жсон` → `json`, таблица на 33 символа) и ищет по трём источникам: продуктовое имя, поисковое имя, синонимы. Причина не в удобстве: интерфейс русский, имена английские, а палитра не инструментована — точка потери невидима в метриках и сама себя не проявит | 2026-09-07 |
| Q-132 | Судьба маршрута выведенной из каталога позиции | **D-82 п. 3:** маршрут пожизненный. Выведенная позиция отдаёт страницу с объяснением и входом в палитру, из sitemap запись убирается, редиректа нет; сохранённый ярлык и `start_url` установленного PWA продолжают открываться. 301 на ближайший инструмент отвергнут: человек, пришедший за конкретным инструментом, молча попадал бы в другой — тот же класс молчаливого отказа, что запрещает REQ-LIMIT-04 | 2026-09-07 |
| Q-131 | `Tool.id` — публичное неизменяемое значение без правила неизменности | **D-82 п. 2:** `id` — ASCII-slug, **совпадает с сегментом RU-маршрута** и не меняется после релиза никогда; переименование имён его не трогает — для этого они и разведены D-56. Гейт: множество id релизной сборки — надмножество предыдущей. Если переименование неизбежно, ряды PM-01…PM-03 разрываются **объявленно**: разрыв, выглядящий как изменение показателя, хуже разрыва названного | 2026-09-07 |
| Q-130 | Состав полей записи реестра не описан ничем | **D-82 п. 1:** состав собран из решений сессии — `id`, продуктовое имя, синонимы, чанк, `usesEditor`, `sensitiveInput`, ступень лестницы с предикатом осмысленности (D-49, D-73), таблица порогов по тройке ключей (D-73), маршруты, лендинг с поисковым именем (D-56), пример первого экрана. `sensitiveInput` подтверждён как обязательное поле: teardown принадлежит маршруту, состав обнуляемого — инструменту, и без признака маршрут не знает, что охраняет | 2026-09-07 |
| Q-125 | Маршруты служебных страниц не описаны ничем | **D-82 п. 1:** реестр держит два типа записей — инструмент и служебная страница, — поэтому маршруты, sitemap и `hreflang` работают по общим правилам, добавление страницы не правит оболочку (US-002 остаётся верным), а в P0b EN-ветка заводится им так же, как инструментам. «Об обязательствах» остаётся разделом внутри страницы развёртывания (REQ-CONTENT-05): страниц четыре и один раздел | 2026-09-07 |
| Q-128 | Палитра ищет шесть инструментов или восемь позиций каталога | **D-81 п. 4:** палитра ищет то, что можно открыть, — шесть инструментов с маршрутами; палитра и определитель входят в восемь позиций M-02, но записей в палитре не имеют, потому что «открыть определитель» бессмысленно. US-014 крит. 2 правится на «любой из шести инструментов», готовая строка гайда остаётся верной. Путаница возникала оттого, что оба числа называли «каталогом»: M-02 считает состав, палитра показывает открываемое. Служебные страницы в палитру в P0a не попадают — вход к ним даёт обвязка оболочки | 2026-09-07 |
| Q-127 | «Недавние в сайдбаре» — сайдбара в продукте не существует | **D-81 п. 3:** сайдбар вычеркнут из §4 глоссария и из правил контента кита; «Недавние» живут только в палитре. Сайдбара нет ни в сетке, ни в макетах, и появиться он мог бы только за счёт меры ленты — то есть за счёт данных | 2026-09-07 |
| Q-126 | Счётчик внешних запросов — один на экране или два | **D-81 п. 2:** один, в шапке, виден на всех маршрутах. Строка доказательства первого экрана число **не дублирует, а объясняет**: что считается, куда идёт аналитика (D-80) и ссылка на «Как это проверить». Двух чисел на экране не бывает: при разной методике они разойдутся, и механизм предъявления станет поводом не верить продукту | 2026-09-07 |
| Q-123 | Первый экран генератора: значение сразу или пустое состояние | **D-81 п. 1:** значение сгенерировано и показано на первом кадре, как требуют FL-12 и US-066; пустого состояния у генератора не существует, строка из content-guide §5 удалена. Сцепка с PM-01, делавшая вопрос дорогим, снята ещё D-52 — автозначение событий не порождает; осталось соображение DP-01 про лишнее нажатие там, где ввода не требуется вовсе | 2026-09-07 |
| Q-122 | Где стоят чипсы типа | **D-80 п. 4:** **рядом с полем ввода**, до области результата; порядок первого экрана становится H1 → поле → чипсы → результат → строка доказательства → FAQ. Правятся D-18 в части порядка, сетка design-plan, SCR-03 и промт экранов. Основание не в старшинстве документов: промах определителя виден там же, где смотрит человек сразу после вставки, а чипс после результата предлагает исправление тому, кто уже решил, что продукт ошибся, — то есть тому, кто уже уходит | 2026-09-07 |
| Q-121 | Рядом со счётчиком внешних запросов нет ни слова про аналитику | **D-80 п. 3:** добавляется **одна строка** — `аналитика — через свой origin, содержимое ввода не отправляется` со ссылкой на «Как это проверить». Не панель и не блок: строка `--type-caption`, поле ввода не отодвигается. Довод перевесил цену места: канал D-36 проверяет именно это, и умолчание там обходится дороже самой аналитики (RISK-12) | 2026-09-07 |
| Q-120 | Единицы и сокращения не определены ни одним документом | **D-80 п. 2:** пороги публикуются в **десятичных МБ** (10⁶), и код считает так же — опубликованное число обязано совпадать с поведением, на этом стоят CB-06 и PM-09. Единица байта в русском интерфейсе — `Б`, не латинская `B` (кит правится). Счётчик пишет `знаков` полностью: сокращение `симв.` экономит три знака и стоит вопроса «символов чего». Правило действует во всех четырёх местах сразу | 2026-09-07 |
| Q-119 | Нужен ли тост как класс | **D-80 п. 1:** нет, и это принимается сознательно. Все сообщения привязаны к месту: подтверждение копирования — подпись кнопки `Скопировано` на 1,2 с, ошибки и пороги — в области результата, сообщения оболочки — в статусной строке шапки (REQ-CORE-09). Сообщений, не привязанных ни к элементу, ни к оболочке, у продукта не будет; если такое понадобится, сначала пересматривается решение, а не добавляется слой. Правило одной тени сохраняется | 2026-09-07 |
| Q-118 | Правило «кнопка — один глагол» против четырёх подписей | **D-79 п. 4:** принята редакция гайда — глагол из §3 плюс объект, когда без него действие неоднозначно, предел 20 знаков; формулировка кита правится под неё. Список исключений не заводится: после D-60 и D-72 в нём было бы уже шесть подписей, и он рос бы с каждым новым действием | 2026-09-07 |
| Q-117 | На каком языке ведутся публичные тексты репозитория | **D-79 п. 3:** английский — основной и единственный язык README, `CONTRIBUTING`, шаблона issue, релизных заметок, описания образа и ответов контрибьюторам: канал D-36 англоязычен и заявки подаются на английском. Русский остаётся языком интерфейса и лендингов; в README — одна строка по-русски со ссылкой на продукт. Полный двуязычный README отвергнут: поддержка вдвое на весь срок жизни у одного человека и два расходящихся текста при первой правке | 2026-09-07 |
| Q-116 | Знака нет, а он нужен раньше продукта | **D-79 п. 2:** отрисовка не заказывается. Вордмарк — `dokey` набором с акцентным слогом `key`, компактная форма — `dk` на акцентном квадрате (favicon, PWA, аватар): знак совпадает с ярлыком REQ-ENTRY-03 и обучает механизму входа. OG-изображение — тот же вордмарк со строкой `внешних запросов: 0`, то есть показывает предъявляемый механизм, а не украшение; изображений в репозитории не появляется | 2026-09-07 |
| Q-112 | Кириллица в выбранных начертаниях не проверена | **D-79 п. 1:** сабсеттинг четырёх woff2 и ручная проверка на подготовительной неделе — покрытие, ширина моноширинных кириллических глифов, метрики фолбэка на Windows. Сверх разовой проверки заведён **гейт**: сборка сверяет все символы текстов продукта с составом сабсета, отсутствующий символ роняет сборку. Это ловит то, чего разовая проверка не поймает — строку с тире, «ёлочками» или «ё», добавленную через полгода | 2026-09-07 |
| Q-115 | Минимальный размер цели не проверяется ничем | **D-78 п. 4:** в кит добавлен токен `--target-min: 24px`, всё нажимаемое обязано его соблюдать, проверка — правило target-size внутри существующего гейта axe (D-50). Нового гейта не появляется, чипсы с 27 px проходят, а сократить отступ незамеченным больше нельзя | 2026-09-07 |
| Q-114 | У акцентной подложки два смысла сразу | **D-78 п. 3:** предупреждение получает собственный носитель — `--amber-100` и `--text-warning`; акцентная подложка остаётся только за распознаванием. На экране JWT Inspector три состояния становятся тремя цветами: акцент, янтарь, красный, и каждое по-прежнему дублируется словом. Один носитель с различением словом отвергнут: рядом оказались бы два одинаковых пятна с противоположным смыслом — ровно случай, который вопрос и называл | 2026-09-07 |
| Q-111 | Байтового бюджета на шрифты нет ни в одном NFR | **D-78 п. 2:** заведено **NFR-18** с двумя числами: шрифты на критическом пути ≤ 48 KB gzip и сумма чанков каталога в precache ≤ 120 KB gzip (без чанка редактора). Вторая статья появилась в ходе сессии — precache всего каталога из D-63 растёт с каждым инструментом и не был ограничен ничем. Оба числа — оценка до В-2 и уточняются вместе с M-03; проверяются существующим бюджетным гейтом | 2026-09-07 |
| Q-108 | Четыре релиза названы тремя системами имён | **D-78 п. 1:** остаётся система BRD, PRD и backlog — **P0a / P0b / P1 / R2**. Сводный слой R0/P0b из роадмапа и имена D-06 (`R1`, `R1.1`, `R1.2`) выводятся из обращения одним проходом. «Человеческая» нумерация читается лучше, но P0a стоит десятками мест, включая определение M-02, и цена переименования — риск разъехавшихся ссылок в уже согласованных документах | 2026-09-07 |
| Q-107 | У CB-08 нет механизма проверки, хотя BRD называет его гейтом CI | **D-77 п. 4:** гейт добавлен — поиск по списку слов-маркеров («SLA», «гарантируем», «поддержка 24/7», «время ответа», «срок жизни», «исправим в течение», «обязуемся») по всем релизным текстам, README и описанию образа; совпадение роняет сборку. Ложное срабатывание в цитате снимается явным исключением в конфиге, с комментарием, а не молча. Ручной пункт чек-листа отвергнут: проверял бы тот же человек, который пишет тексты | 2026-09-07 |
| Q-106 | P0b выпускается раньше данных, определяющих его состав | **D-77 п. 3:** бюджет P0b делится на ≈ 17 дней фиксированной части (состав D-06) и ≈ 5 дней **резерва**, не назначенного ни на что до 2027-01-20 и распределяемого в день снятия baseline строго по топовой причине M-09. Если причина требует больше пяти дней, она переносится в R2 явным решением, а не размывается. Сдвиг P0b отвергнут: даёт простой ровно в месяц первой обратной связи | 2026-09-07 |
| Q-105 | M-12 и TECH-14 стоят в неделе, где слака нет | **D-77 п. 2:** M-12 перенесена на **2026-11-16** — вход в окно релиза, где она и была задумана по смыслу («составляется, когда времени ещё много»). Подача заявок TECH-14 остаётся на релизной неделе: раньше публикации она невозможна. Обе работы впервые внесены в оценку — 1–2 дня в A-15 | 2026-09-07 |
| Q-104 | Жёсткий минимум RISK-06 записан диапазоном волн | **D-77 п. 1:** принята редакция роадмапа §2.5 — минимум это В-0…В-6 и В-8…В-10 без В-7 и В-11, демонтаж идёт по MoSCoW и вкладу в M-NN. Запись «В-0…В-4» отменена: буквально она выносила из релиза образ GHCR, всю M-07, опубликованные пороги и инструментовку. Рядом записана честная цена рычага: после D-58 и D-59 в Could осталось три требования, полный демонтаж даёт шесть–восемь дней из 37,5–44,5 — при отклонении вдвое этого не хватит, и это сказано прямо | 2026-09-07 |
| Q-103 | Порядок «гейт раньше приёмника» в инструментовке | **D-76 п. 4:** приёмник поднимается **до первой строки кода** — TECH-03 остаётся в подготовке A-15 (1,5–2,5 дня уже посчитаны). Семь историй EP-09 проверяются против живого приёмника в день написания, а гейт закрытого списка полей в В-0 перестаёт быть гейтом без предмета. Отвергнут перенос за релиз: истории закрывались бы непроверенными, а baseline M-08 снимался бы с данных, качество которых никто не подтверждал | 2026-09-07 |
| Q-102 | Архитектурные истории волны каркаса не имеют предмета приёмки | **D-76 п. 3:** критерии переписываются под проверку **на синтетике** тем же приёмом, что гейт-инвариант D-48: воркер — тестовая операция и запрет блокировки дольше 50 мс, виртуализация — дерево на 100 000 узлов, teardown — синтетический маршрут с токеном-фикстурой. Истории закрываются в В-0 честно, а фикстуры потом используются гейтами CI — работа не выбрасывается | 2026-09-07 |
| Q-101 | У сквозных историй нет момента, в который отмечается чекбокс | **D-76 п. 2:** сквозная история разбирается на **пункты контракта позиции** (D-21) — единая форма и фикстуры проверяются при приёмке каждого инструмента, там же и отмечаются, — а сама история становится гейтом релиза и закрывается вместе с последней позицией. Разрезание на шесть историй отвергнуто: бэклог вырос бы с 67 до 79 ради учёта, а не ради работы | 2026-09-07 |
| Q-100 | Считается ли требование покрытым историей, которую нельзя взять в спринт | **D-76 п. 1:** нет. Матрица получает третий столбец «покрыто проходимой историей», а требование, у которого все покрывающие заблокированы, попадает в дыры §4. Матрица существует как проверка на полноту, и «история написана» — не то же, что «работа может начаться». На 2026-09-07 столбец зелёный целиком: правило вводится ради следующей волны вопросов, а не ради текущего состояния | 2026-09-07 |
| Q-99 | Порядок и даты закрытия блокирующих вопросов | **D-75 п. 4:** правило — блокирующий вопрос закрывается **до начала волны**, в которой нужен; дат не назначается, назначается место в очереди, а счётчик блокировок входит в чек-лист готовности волны. Острота вопроса снята фактом: на 2026-09-07 из сорока двух заблокированных историй не осталось ни одной, сорок три вопроса закрыты за три сессии до начала разработки. Время PO на ответы записывается в A-15 отдельной строкой — впервые измеренное | 2026-09-07 |
| Q-98 | Что именно калибрует M-03: позицию или позицию вместе с каркасом | **D-75 п. 3:** две сверки вместо одной. Каркас В-0 получает собственную оценку (выделяется из строки «оболочка 10–12 д» в A-06) и собственную сверку; M-03 переносит на остальные семь позиций **только коэффициент позиции**. Обе сверки публикуются вместе — иначе «уложились по позиции» скроет перерасход каркаса, съевший те же дни. Отклонение вдвое по любой из двух включает план RISK-06 | 2026-09-07 |
| Q-97 | Что такое «сценарий-фикстура» в NFR-08 | **D-75 п. 2:** единица — **исполняемый прогон сценария FL-NN** в браузере, а не образец корпуса ENT-21: корпус про данные, прогон про путь. Доля считается автоматически, и NFR-08 **включается в гейты NFR-17** — у M-06 есть порог провала, а бюджет без гейта ничего не запрещает, тогда как D-38 вводил его именно как запрет | 2026-09-07 |
| Q-94 | Состав пяти порталов бенчмарка и определение TTR для чужой страницы | **D-75 п. 1:** площадки берутся из выдачи research §4 и называются поимённо (`jsonformatter.org`, `codebeautify.org`, `jwt.io` плюс две по запросам Base64 и URL), список живёт в скрипте, а не в тексте. TTR чужой страницы определён процедурно: от навигации до появления результата в DOM по **явному селектору**, объявленному для каждой площадки, медиана девяти прогонов, фиксированный троттлинг, холодный кеш. Публикуются скрипт, конфиг селекторов, сырые прогоны и дата — включая проигранные замеры. Оговорка про рекламу на чужих страницах пишется прямо | 2026-09-07 |
| Q-93 | Событие открытия инструмента отсутствует в модуле METR | **D-74, против рекомендации бэклога:** заведено **REQ-METR-08**. Довод бэклога («ни одна PM-NN его не потребляет») перевешен тем, что событие не добавляет ни одного поля к закрытому списку, а без распределения по каталогу состав P0b выбирается вслепую. Событий стало девять, полей — столько же, сколько было | 2026-09-07 |
| Q-92 | Обвязка оболочки: статус «Офлайн» и ссылки шапки и подвала | **D-74:** заведено **REQ-CORE-09** — одно требование на весь состав шапки и подвала: вордмарк (открывает палитру), кнопка палитры, счётчик внешних запросов, статус сети, ссылки на служебные страницы, версия и дата сборки. Статус сети отдельным требованием не заводится: разводить их значило бы описывать шапку в двух местах | 2026-09-07 |
| Q-91 | Действие «Очистить» не описано ни одним требованием | **D-74:** заведено **REQ-INPUT-08**: поле обнуляется, область результата возвращает пример (D-65), ручной выбор чипса и буфер возврата сбрасываются. На маршруте с `sensitiveInput` очистка обнуляет **тот же перечень, что teardown** (ENT-25) — иначе действие выглядит как обнуление, а секрет остаётся в памяти | 2026-09-07 |
| Q-90 | Копирование результата не описано ни одним требованием | **D-74:** заведено **REQ-INPUT-07** (Must): у любого непустого результата есть «Скопировать», копируется ровно показанное без форматирования, отказ буфера — по правилу D-64. Приоритет Must не обсуждался: копирование стало вторым основанием события METR-01 (D-52), то есть на нём стоит метрика | 2026-09-07 |
| Q-95 | Что считается «нажатием» в правиле двух нажатий | **D-73 п. 4:** нажатие — действие, продвигающее к цели: открыть палитру (одно, любым из трёх входов D-62) плюс выбрать позицию (одно). Набранные символы не считаются — они сужают список, а не навигируют. Отсчёт от состояния «оболочка открыта, фокус в поле» на любом маршруте; гейт — автотест по каждой позиции каталога, и NFR-05 в этой части становится измеримым | 2026-09-07 |
| Q-89 | Что такое «осмысленный результат» | **D-73 п. 3:** каждая ступень лестницы объявляет **свой предикат** рядом с распознавателем в записи реестра; базовая редакция задана таблицей на восемь ступеней (пустой JSON-объект осмыслен, base64 обязан декодироваться в непустой валидный UTF-8 без управляющих символов, timestamp — диапазон 1970–2100 и так далее). Корпус фикстур размечает **ожидаемый порядок чипсов**, а не единственный ответ — иначе фикстура проверяет вкус разметчика. Общее правило отвергнуто: `1234567890` проходило бы как base64 и как timestamp одинаково | 2026-09-07 |
| Q-88 | Слово «сценарий» | **D-73 п. 2:** §6 глоссария разводит два значения — слово разрешено как «пользовательский сценарий FL-NN» и в производных вроде «сценарий-фикстура» (NFR-08), запрещено как синоним петли и как элемент интерфейса. Запрет вводился против одного злоупотребления, а не против слова; переименование единицы user-flows отвергнуто как правка в шести документах ради терминологической чистоты | 2026-09-07 |
| Q-135 | Порог — не одно число на инструмент, а таблица | **D-73 п. 1:** подтверждено и расширено — размерность не только по классу платформы, но и по оси. `SizeThreshold` получает поле `axis`, индекс становится тройкой | 2026-09-07 |
| Q-87 | Оси порога, кроме размера ввода | **D-73 п. 1:** ключ порога — тройка «инструмент × класс платформы × ось». Оси P0a: `inputSize` у всех, `jsonDepth` у JSON Workbench, `batchSize` у генератора; в P0b добавляется `hashInput`. Числа по каждой оси назначаются процедурой D-51, публикуется таблица целиком. Довод против одной оси: документ в три килобайта с тысячей уровней вложенности проходит порог по размеру и валит вкладку — предохранитель не срабатывает там, где нужен | 2026-09-07 |
| Q-85 | Контракт требует обе локали раньше, чем они существуют | **D-72 п. 4:** пункт требует **структуру**, а не текст — наполненную текущую локаль и заведённый маршрут второй в статусе `draft`, ровно то, что делает US-006. Английский текст до P0b не требуется и не принимается; ненаполненная ветка не публикуется, поэтому вклад не создаёт видимой дыры | 2026-09-07 |
| Q-84 | Честность счётчика внешних запросов | **D-72 п. 3:** счётчик считает всё, что видит вкладка, включая запросы расширений — фильтрация отвергнута, потому что число обязано совпадать с DevTools, ради сверки с которым оно и существует. При ненулевом значении одним нажатием раскрывается список origin: ложное срабатывание превращается в объяснение. Оба краевых случая (расширения и молчащая при DNT/GPC аналитика) описаны на странице «Как это проверить», где для них заводится раздел «границы измерения» — четвёртое место, где обещание слабее механизма | 2026-09-07 |
| Q-83 | Обратное направление и сохранение файлом | **D-72 п. 2:** обратное направление поддерживается, и в §3 глоссария заводится глагол `Сохранить` (`save`) — локально из `Blob`, ноль сетевых обращений; «загрузить», «скачать» и «выгрузить» остаются запрещёнными, потому что обещают сеть. Действие показывается у бинарного результата и у результата больше порога копирования — этим же закрыта вторая половина вопроса: многомегабайтный результат забирается файлом, а не буфером | 2026-09-07 |
| Q-81 | Предмет поиска в JSON Workbench | **D-72 п. 1:** поиск идёт по ключам **и** значениям одновременно — П-2 чаще знает значение, а не имя поля. Все совпадения подсвечиваются (объявленное исключение из правила «одно акцентное пятно»), рядом счётчик и переход между ними; регистр игнорируется, поиск по подстроке. Переключатель режимов отвергнут: он требует выбора до того, как человек увидел результат | 2026-09-07 |
| Q-78 | Слово «продолжить» на пороге | **D-71 п. 4:** переименовывается порог, а не петля: «Продолжить с этим» закреплено D-26 и опознаётся как имя механизма, а кнопка порога имени не несёт. Подпись мягкого порога — `Обработать`; глагол добавлен в §3 глоссария и больше нигде не употребляется | 2026-09-07 |
| Q-75 | Петля и порог | **D-71 п. 3:** петля не имеет привилегий — поданное значение проходит пороги как обычная вставка. За жёстким порогом значение во ввод **не подаётся** (иначе петля оставляет человека с полем, которое нельзя обработать), исходный результат остаётся на экране и его можно скопировать. На пустом результате действия «Продолжить с этим» на экране нет вовсе | 2026-09-07 |
| Q-74 | Состояние после «Отменить» | **D-71 п. 2:** ввод сохраняется всегда, область результата возвращается **ровно к состоянию до запуска** (прежний результат или разобранный пример), частичный результат не показывается никогда и в буфер обмена не попадает — незаконченный Base64 выглядит как законченный и уходит в чужой конфиг. Отмена объявляется статусным регионом и отправляет METR-02: это «результат не получен» по выбору человека | 2026-09-07 |
| Q-73 | Где живёт признак «повторный визит» | **D-71 п. 1:** противоречия не было — NFR-10 запрещает хранить пользовательский **ввод**, а не служебные флаги. Заводится закрытый список служебных ключей по образцу закрытого списка полей аналитики: в P0a в нём ровно один ключ (показана ли подсказка), список публикуется на «Как это проверить», гейт CI запрещает ключи вне списка и значения, производные от ввода. В P0b туда же ложится выбранная тема (D-58) | 2026-09-07 |
| Q-86 | Хвост, набранный после `dk` в адресной строке | **D-70:** разбор сместил предмет — утечка происходит на уровне браузера, а не продукта: строка омнибокса `dk <токен>` попадает в историю и подсказки независимо от нашего шаблона (D-13 п. 2 это уже признавал). Значит, выбор был не «утечёт или нет», а «утечёт молча или с предупреждением». Ярлык остаётся: хвост не читается, не показывается, не уходит в аналитику, URL очищается до первой отрисовки; предупреждение стоит заранее на «Держать под рукой» и в подсказке. Проверка описания в четырёх браузерах — критерий US-016, делается на подготовительной неделе; при принятом шаблоне без `{searchTerms}` хвоста в адресе нет вовсе | 2026-09-07 |
| Q-82 | Возврата на шаг назад в петле нет | **D-69:** половину ответа дал D-65 — петля выполняется через буфер отмены поля, поэтому Ctrl + Z возвращает предыдущий шаг без единой новой сущности и без персистентности. Добавлено видимое действие «Вернуть», живущее только пока возврат возможен: клавиатурный путь невидим ровно для тех, кто читает исчезновение исходного значения как потерю. Глубина — один шаг (многошаговая дорожка возвращала бы историю D-13 и конструктор рецептов D-26). Буфер возврата входит в teardown ENT-25. Противоречие REQ-INPUT-03 и REQ-INPUT-05 снято формулировкой: ввод не заменяется **сам**, замена по явному действию обратима одним действием | 2026-09-07 |
| Q-80 | JWT Inspector вне HS256 | **D-68:** состояний подписи три, а не два — «не проверялась» это не «не совпала», и двухцветная логика в инструменте про безопасность ведёт к неверному прочтению. Разбор работает при любом `alg`; `alg: none` получает **предупреждающий** вердикт, а не нейтральное молчание; RS256 и ES256 объявляются проверкой следующего релиза (назван релиз, не дата — CB-08) и уходят в состав P0b. Разбор добавил случай, которого в вопросе не было: подмена алгоритма — при `alg`, отличном от HS256, введённый ключ не используется ни при каких условиях | 2026-09-07 |
| Q-133 | Что происходит с ленивым чанком старой сборки после релиза | **D-67:** решающим оказался не `skipWaiting`, а срок жизни файлов — причина поломки в том, что старых чанков больше нет на хостинге. Файлы предыдущих сборок хранятся **30 дней**, поэтому вкладка, открытая до релиза, догружает свои чанки независимо от активного service worker. Имя кеша несёт идентификатор сборки, старые кеши удаляются на `activate`, то есть тогда, когда новая оболочка реально вступила в силу, а не под работающей вкладкой | 2026-09-06 |
| Q-79 | Обновление: установленный PWA и образ | **D-67:** `skipWaiting` не вызывается автоматически; новая оболочка ждёт, а человек видит тихую строку `Вышла новая версия` + «Обновить» (проверка при запуске и раз в 6 часов). При непустом поле рядом стоит «введённое не сохранится» — предупреждение вместо модального подтверждения, которых в P0a ноль. Для образа выбора почти не было: REQ-DIST-03 запрещает сетевые обращения в рантайме, поэтому версия и дата сборки показываются в подвале, а организация сверяет их с публичной датой пересборки (D-59) | 2026-09-06 |
| Q-71 | Чипсы типа: сколько, в каком порядке и что при промахе | **D-66:** показываются все кандидаты с осмысленным результатом плюс «Текст» (выход из режима декодирования по D-16) — практически это два-четыре чипса, поэтому ограничение числа решало бы несуществующую задачу и создавало новую. Порядок — по лестнице специфичности и **не меняется** при переключении: применённый помечается состоянием, а не позицией. Ручной выбор побеждает определитель всегда, включая случай без осмысленного результата — тогда показывается объявленная ошибка инструмента, иначе переключение необратимо. Переполнение прокручивается горизонтально, меню «ещё» нет | 2026-09-06 |
| Q-68 | Повторная вставка и правка ввода | **D-65:** третье из трёх состояний оказалось уже решённым в content-guide §5 (после «Очистить» возвращается пример) — расхождения не было, была неучтённая строка. Вставка в непустое поле **заменяет содержимое целиком** и запускает определитель заново; предыдущее значение возвращает Ctrl + Z, поэтому замена идёт через буфер отмены поля, а не присваиванием `value`. Ручной выбор чипса переживает правку и сбрасывается вставкой: правка — уточнение тех же данных, вставка — новые данные. Побочно сузился Q-82: петля перестала быть полностью необратимой в пределах буфера отмены | 2026-09-06 |
| Q-76 | Отказ браузерного API: буфер обмена, WebCrypto, `File` | **D-64:** одно правило на каталог — возможность проверяется **до** показа действия, при отказе называется причина и остающийся путь, подделка возможности запрещена навсегда (никакого `Math.random` вместо `getRandomValues`, никакого «Скопировано» без копирования, никакой своей криптографии ради работы на http). Разбор нашёл, что один из трёх случаев не гипотетический: `crypto.subtle` не существует в незащищённом контексте, а self-hosted-образ в интранете обычно стоит на голом http — то есть проверка подписи JWT отваливается штатно у П-3. Отсюда раздел про TLS на странице развёртывания и третье место, где обещание слабее механизма | 2026-09-06 |
| Q-124 | Куда ведёт «путь в каталог» и куда нажимается вордмарк | **D-62:** каталог у продукта существует как поиск, а не как витрина, поэтому «путь в каталог» — палитра, и вордмарк `dokey` открывает её же. Ожидание «логотип ведёт куда-то главное» выполняется без изобретения главной страницы, отменённой D-18; корневой маршрут не заводится | 2026-09-06 |
| Q-77 | Мобильный путь к палитре и перехваченное сочетание | **D-62:** постоянная кнопка вызова палитры в шапке на всех ширинах, с подписью `Ctrl K` на десктопе, плюс второе сочетание `/` вне поля ввода. Один элемент закрыл три дыры сразу: мобильный путь, обход перехваченного Ctrl + K и «путь в каталог» для SCR-15 | 2026-09-06 |
| Q-72 | Что видит человек, когда логика недоступна | **D-63:** первое из трёх состояний устранено, а не объяснено — service worker кеширует чанки **всего каталога** фоном после первой отрисовки (чанк редактора не входит), и NFR-12 усилена до «100 % инструментов каталога работают без сети после первого визита». Обещание офлайна перестало быть условным. Оставшиеся состояния разведены текстами и действиями: чанк не загрузился — «Повторить»; чанка нет из-за новой сборки — «Обновить» (повторять нечего); маршрута нет — «Открыть палитру». Во всех случаях лендинг и поле ввода остаются доступными, отправляется METR-02 с причиной. Цена названа: трафик первого визита растёт, а байтового бюджета на чанки каталога не назначает ни один NFR | 2026-09-06 |
| Q-67 | Английские формулировки интерфейса не зафиксированы | **D-61:** сейчас фиксируется правило вывода, а не строки: EN-имя инструмента выбирается по англоязычному спросу (параллель D-56), подписи действий переводят смысл, а не слово, правило объекта из D-60 действует и в EN (`Verify signature`), кодовые имена не меняются никогда, соответствие один к одному. Колонка заполняется в P0b одним заходом до первой английской строки; недописанное не протечёт в релиз, потому что EN-маршрут в статусе `draft` не публикуется (US-006) | 2026-09-04 |
| Q-64 | «Проверить» — один глагол на два действия | **D-60:** объект обязателен всегда: `Проверить JSON` и `Проверить подпись`, ни в кнопке, ни в FAQ, ни в контракте, ни в issue глагол в одиночку не употребляется. Разбор уточнил, где двусмысленность кусает: в текстах ошибок её нет вовсе (CG-16 и CG-18 запрещают там глаголы), зато оба смысла соседствуют на экране JWT, а после D-52 граница между ними решает, METR-01 или METR-02 уходит в аналитику. Найдено и объявлено исключение — имя страницы «Как это проверить» (D-15): это не действие интерфейса, а название, и объект в нём — обещание. Отвергнуто «Сверить подпись»: не отраслевая формулировка | 2026-09-04 |
| Q-57 | REQ-DIST-05 (расписание пересборки образа) не служит ни одной M-NN | **D-59:** остаётся, ⚠ снимается — основание не метрика, а исполнение D-28, который выбрал пересборку **вместо** сканера уязвимостей; без расписания у безопасности образа не остаётся механизма. Настоящая цена оказалась не в сборке (тридцать строк YAML поверх готового пайплайна), а в её отказе — это RISK-11 без предохранителя после D-43. Отсюда три предохранителя: провал заводит issue, а не письмо; три провала подряд отключают расписание; на странице образа стоит **дата последней успешной сборки**, а не обещанная периодичность — факт вместо обязательства (CB-08) | 2026-09-04 |
| Q-110 | Тёмная тема Could, а ручному переключателю негде хранить выбор | **D-58:** переключатель уходит в P0b, поэтому в P0a хранить нечего — и это записано как свойство, а не как отсутствие функции. Разбор уточнил природу конфликта: NFR-10 запрещает хранить **пользовательский ввод**, а выбор темы вводом не является; ломается не требование, а демонстрация «откройте DevTools — хранилище пусто» (D-15). Правило для P0b зафиксировано заранее: один явно названный ключ `localStorage`, перечисленный на странице «Как это проверить», и переформулировка обещания на «ни байта ваших данных; единственная запись — тема» | 2026-09-04 |
| Q-56 | REQ-CORE-07 (светлая и тёмная темы) не служит ни одной M-NN | **D-58:** требование расщеплено, потому что собрало две вещи разной цены. Следование системной настройке остаётся в P0a как Should: это CSS кита, ноль JS и ноль байт NFR-03, и оно служит читаемости контентных страниц (M-14) и NFR-13 — принудительная светлая тема на тёмной ОС даёт контраст, которого человек не выбирал. Пометка ⚠ снята. Полдня, которые предлагалось вернуть в бюджет, оказались ценой переключателя, а не второй темы | 2026-09-04 |
| Q-65 | Является ли счётчик ввода позицией каталога | **D-57:** в P0a — функция оболочки: нет маршрута, лендинга и записи реестра, контракт D-21 не применяется, в палитре не виден, событий не порождает (это уже дал D-52). Формулировка D-16 «фактически нулевая позиция каталога» исправлена как оценочная. Разбор нашёл то, чего не было в постановке: у счётчика есть собственный частотный кластер запросов, а позиция каталога — это лендинг, то есть вход при единственном канале D-07. Поэтому спрос не теряется: «Счётчик символов» вносится в состав P0b. Заодно задано общее правило: позиция — то, у чего есть собственный маршрут и лендинг; всё остальное — оболочка | 2026-09-04 |
| Q-62 | Имя инструмента и заголовок лендинга — одна сущность или две | **D-56:** две, с правилом соответствия. Разбор снял ложную развилку в постановке: лендинг и экран инструмента — одна страница, поэтому H1 на ней один. `Tool.name` — продуктовое имя (палитра, хлебные крошки, `Tool.id`, справка); `ToolLanding.h1` и `title` — поисковая формулировка кластера, потому что канал по D-07 единственный, а запросов «json workbench» в русской выдаче нет. Мост между ними: продуктовое имя обязано стоять в первом предложении вводного абзаца, палитра ищет по обоим полям. CG-12 и CG-13 переписаны; гейт уникальности расширен на `Tool.name` и непустой `h1` | 2026-09-03 |
| Q-66 | Что считается «визитом» | **D-55:** единиц две, потому что двум семьям метрик нужны разные. **Вход** — доставленное событие открытия оболочки с маркером источника — единица PM-05…PM-08; **визит** — цепочка событий одной вкладки до 30 минут бездействия — единица PM-01, PM-02, PM-09. Повторный заход по ярлыку внутри окна: новый вход, тот же визит. Ключевой пункт, без которого первые два бессмысленны: **автотрекинг смены URL выключается** — после D-47 определитель меняет маршрут `replaceState`-ом, и Umami считал бы внутреннюю подмену инструмента просмотром. Обе единицы считаются по доставленным событиям (офлайновой части в данных нет), версия Umami пиннится как пресет Lighthouse в D-08: её смена — событие уровня baseline | 2026-09-03 |
| Q-113 | Подсветка JSON без цветов типов | **D-53:** закрыт как утративший предмет. Трёхролевая подсветка без цветов типов была частью собственного слоя токенов плана дизайна; вместе с ним предложение отменено. Подсветка берётся из палитры кита — `--code-key`, `--code-string`, `--code-number`, `--code-boolean`, `--code-punct`, `--code-comment`; проверка контраста на `--surface-code` пройдена, `--code-comment` подтянут с gray-600 до gray-500 (2,91:1 → 4,91:1) | 2026-09-03 |
| Q-109 | Готовая дизайн-система и план дизайна расходятся на уровне токенов | **D-53:** источник истины — кит `docs/Design System/`; план дизайна понижен до подчинённого документа, его §§3–6 приведены к киту, §8 снят, §7 (доступность) остаётся в силе по D-50. Граница: кит отвечает «как это выглядит», решения D-NN и screen-map — «что на экране есть», поэтому плитки каталога и сайдбар кита не возвращают отменённых D-18 экранов. Бренд ToolBox → DoKey. Гейт D-50 применён к токенам кита сразу: девять значений его не проходили (`--text-link` 4,10:1, `--focus-ring` 2,61:1, `--text-muted` 3,88:1, белая подпись на `--action-primary` 4,10:1, `--code-comment` 2,91:1 и другие) — все починены сдвигом на ступень собственной шкалы, правки внесены в `tokens/colors.css`. Узнаваемость видом не преследуется: принятый риск, названный прямо | 2026-09-03 |
| Q-63 | Дизайн-система предполагает стек и поставку, не проходящие бюджеты | **D-54:** заменяются механизмы, а не язык. Стек — Astro с островами на ванильном TS, React в сборку не входит вовсе, компоненты кита переносятся как спецификация (контракт `.d.ts`, состояния, разметка), что согласуется с запретом headless-библиотек из D-50. Шрифты самохостятся: четыре woff2, сабсет latin + cyrillic, `@font-face` вместо `@import` — `tokens/fonts.css` переписан. Иконки — инлайн-спрайт из восьми глифов, зависимость Lucide не берётся (ISC, атрибуция). Отвергнут Preact в островах: 4 KB из 40 за удобство переноса восьми экранов, остаётся резервом. Возврат любого из трёх механизмов ловят NFR-03 и NFR-09 | 2026-09-03 |
| Q-61 | Что считается «корректным результатом» в REQ-METR-01 | **D-52:** правило двух оснований — операция инструмента над вводом человека с непустым результатом **либо** копирование показанного значения. Второе основание закрывает генератор и счётчик одним правилом вместо двух исключений. Разбор нашёл третий случай, дороже двух названных в вопросе: разобранный пример первого экрана стоит на каждом лендинге до ввода, и при буквальном чтении D-34 успешным становился каждый визит вообще. Найден и снят конфликт с METR-02: отрицательный вердикт **явно запрошенной** валидации — результат, а не отказ; граница проходит между «инструмент ответил» и «инструмент не смог». Цена названа прямо: сценарий «посмотрел счётчик и ушёл без копирования» метрика занижает | 2026-09-03 |
| Q-59 | Числа порогов D-30 не назначены, а REQ-LIMIT-03 требует опубликовать их до релиза | **D-51:** момент замера роадмап назначил и раньше (В-2, вместе с M-03) — решение добавило то, без чего замер даёт данные, а не пороги. Правило вывода: мягкий = наибольший размер в границе p50 ≤ 1,5 с (NFR-01), жёсткий = половина наименьшего размера отказа, округлённая вниз. Корпус синтетический и лежит в репозитории, классы платформ — десктоп и мобильный Safari, меряются сразу оба. До замера в реестре стоят предварительные числа с `provisional = true`, и десятый гейт CI не выпускает сборку с ними: разработка идёт немедленно, публичным обещание становится после замера. Довод против кабинетных чисел — PM-09: предохранитель против подкрутки порогов работает только если число выводится процедурой, а не суждением того, кому оно мешает | 2026-09-03 |
| Q-60 | Доступность нигде не решена, а NFR-13 вводит числа без опоры на решение | **D-50:** обязательство — WCAG 2.2 AA целиком; плоский контраст 4,5:1 заменён формулой стандарта (4,5:1 текст, 3:1 крупный текст и нетекстовые элементы), иначе гейт запрещал бы принятые токены design-plan §5.1. Гейт разложен на три слоя: axe-core в CI (0 нарушений A и AA, до и после JS) плюс контраст из значений токенов плюс клавиатурный прогон корпуса M-06; ревью по design-plan §7; один ручной smoke со скринридером перед релизом. Найдена дыра, которой не было ни в одном документе: ядро продукта — применение инструмента без действия человека — для скринридера не происходит вовсе, отсюда REQ-CORE-08 (два статусных региона, 4.1.3). Конфликт с CB-01 снят способом реализации: паттерны пишутся руками, headless-библиотека в initial не берётся ни при каком исходе Q-63 и Q-109 | 2026-09-03 |
| Q-58 | Какие четыре инструмента входят в жёсткий минимум M-02 | **D-49:** четвёрка PRD подтверждена как обязательство — JSON Workbench, JWT Inspector, Base64, URL плюс палитра и определитель; JWT не заменяется более дешёвым инструментом ни при каких условиях. Главное в решении не состав, а три следствия: демонтаж каталога стоит 3 дня из 37,5–44,5 и потому планом реакции не является (вход в Q-104); состав обязывает порядок работ — Timestamp и UUID строятся последними, волной В-7; ступень лестницы D-14 переезжает из константы определителя в запись реестра, активны только ступени инструментов, которые есть в сборке, и связность проверяется гейтом CI | 2026-09-03 |
| Q-96 | Перечень «производных значений» в teardown маршрута | **D-48:** два артефакта вместо одного, потому что перечень всегда неполон, а инвариант — нет. Перечень из семи пунктов (токен, ключ, header и payload, байты WebCrypto, модель редактора и буфер отмены, воркер маршрута через `terminate()`, буфер переноса ENT-30) живёт в контракте инструмента ENT-25 и адресован автору. Предмет гейта — инвариант: подстрока токена-фикстуры не встречается на четырёх достижимых поверхностях. Граница обещания названа прямо: полный обход кучи JS недостижим | 2026-09-03 |
| Q-69 | Меняется ли маршрут, когда определитель выбрал другой инструмент | **D-47:** меняется — маршрут следует за инструментом, но не за типом и не за направлением операции; подмена делается `replaceState`, поэтому история не растёт и «назад» ведёт на выдачу. Главный довод не про адрес: единица teardown — маршрут (ENT-05), и без его смены чувствительный ввод остаётся в памяти вкладки без механизма обнуления — дыра в D-25 п. 3. Попутно уточнён D-46 п. 4: при подмене определителем буфер наполняется значением вставки и переносится всегда | 2026-09-03 |
| Q-70 | REQ-CORE-03 и NFR-10 противоречат друг другу | **D-46:** противоречие оказалось неполным — NFR-10 состоит из двух утверждений, и конфликтует только второе, потому что «перенос значения» и «выживание поля» — разные события. Переносится одно значение (поле ввода из палитры, чипса и определителя; результат из петли REQ-INPUT-05) через однократный буфер в памяти роутера, обнуляемый чтением; состояние оставленного инструмента не сохраняется; из инструмента с `sensitiveInput = true` не переносится ничего; «назад» возвращает маршрут, но не значение. Вторая часть NFR-10 переписана, первая — про носители — осталась дословно. Гейт NFR-17 проверяет три вещи: поля A пусты, буфер пуст, носители чисты | 2026-09-03 |
| Q-55 | Релизное окно упирается в предновогодние недели | **D-45:** закрыт как надуманный, ничего не меняется. Трилемма в постановке оказалась ложной: D-36 уже развёл релиз и объявление своей очерёдностью (сначала накопительные заявки, потом Show HN), поэтому «перенести объявление» — не компромисс, а то, что и так написано. Премиса «канал спит» верна частично: слой 2 и накопительная часть слоя 3 к дате безразличны, чувствительны только Show HN и русскоязычные сообщества (мертвы примерно с 25 декабря по 11 января). 2026-12-21 — верхняя граница, а не план; при работе по оценке релиз ложится в конец ноября. Календарного правила не вводится — решение о дне объявления принимается в момент релиза, что последовательно с принципом D-43. Отвергнуты: условное правило с отсечкой 10 декабря (рекомендованный вариант), прицел в ноябрь с урезанием состава (конфликт с D-31 п. 3) и сдвиг M-01 на январь (сделал бы единственное обязательство проекта предметом переговоров). | 2026-09-03 |
| Q-54 | Что происходит с событиями, полученными офлайн | **D-44:** теряются; на устройстве не копится ни очередь, ни счётчики. Разбор нашёл две ошибки в постановке вопроса. Первая: установленный PWA — не синоним офлайна, теряются не события лояльной аудитории, а события в редкие моменты отсутствия сети (A-02, A-03; изолированный контур и так без аналитики по D-22 п. 6). Вторая, важнее: M-08 — отношение, и без сети пропадают и числитель, и знаменатель, поэтому доля визитов с результатом не смещается, падает только объём. Реальное смещение — у M-17 (систематическое занижение) и слабее у M-15 (пессимистичное); оба записаны прямо у метрик. Факт «офлайн не отправляется ничего» раскрывается на странице проверки и обещание усиливает. Правило к D-40: список полей не растёт ради измерения собственного слепого пятна. Триггер переоткрытия — M-17 около нуля при заметной M-18. | 2026-09-03 |
| Q-53 | Как фиксируется время на эксплуатацию (CB-07) | **D-43:** никак — контр-баланс удалён, допущение A-13 выведено, триггер D-23 остаётся в исходном неформальном виде. Решение PO при озвученной рекомендации заменить часы на счётчик вмешательств (issues и прогоны пересборки берутся из GitHub API без дисциплины, VPS — отметка «да/нет» раз в месяц) с триггером «два месяца подряд с ручным вмешательством». Разбор дал принцип, объясняющий это решение вместе с D-42: **число остаётся там, где его проверяет машина** — CI, Lighthouse, фикстуры; там, где проверяющим был бы сам мейнтейнер, чисел не назначается, поскольку проверяющий и проверяемый совпадают. Уточнён D-38: второй признак бюджета («проверяется в лаборатории или CI») оказался действующим фильтром, а не сопутствующим свойством. Цена: RISK-11 без предохранителя. | 2026-09-03 |
| Q-52 | Что считается принятой заявкой на площадку и сколько их означает «канал заведён» | **D-42:** числа «≥ 5 заявок» и «≥ 2 русскоязычных» сняты, допущение A-14 выведено, «канал заведён» определяется суждением PO. Решение PO при озвученной рекомендации сохранить оба числа как бюджет работы: по тесту D-38 они запрещают два уже названных провала — один Show HN вместо канала (очерёдность D-36) и англоязычный перекос, который D-36 сам диагностировал. M-13 переведена в наблюдение с правилом счёта: запись опубликована и через 7 дней на месте, только площадки из списка M-12, Show HN не засчитывается. Разбор нашёл дыру: у основного канала D-36 не было ни одной метрики трафика — введена M-19 (визиты с реферерами площадок, разрез RU/EN), новых полей ноль. Цена: риск «выпустил и затих» остался без числового предохранителя. | 2026-09-03 |
| Q-50 | Измеримы ли доля возвратов за 30 дней и доля установок PWA среди вернувшихся | **D-41:** нет, и это не главное. Umami различает посетителя хешем с суточной ротацией соли — тридцатидневного окна не существует по построению; купить его можно только персистентным идентификатором, а тот равен cookie в ePrivacy, то есть баннер согласия, удар по TTR, отмена D-22 п. 5 и прямое нарушение vision §5. Но решающим оказалось третье: обе метрики пришли из D-03 от 2 сентября, а D-13 в тот же день переопределил удержание как становление точкой входа — метрики остались артефактами отменённой рамки. Выведены из обращения и заменены тремя наблюдениями по механизмам D-13: доля прямых визитов (пустой реферер), доля визитов из установленного PWA и доля визитов через ярлык (статические маркеры в `start_url` и шаблоне OpenSearch). Новых полей — ноль. Побочно стало проверяемым допущение D-13 об активации ярлыка. | 2026-09-03 |
| Q-49 | Как измерять TTR первого визита из поиска на 4G у живых пользователей | **D-40:** никак — и это правильный ответ. Разбор нашёл, что D-38 уже отнёс обе метрики TTR к бюджетам, а бюджет проверяется до пользователей; BRD при этом оставил полевой замер, то есть противоречил сам себе. M-04 и M-05 переведены в Lighthouse CI с запиннутыми пресетами (тёплый кеш и холодный старт с троттлингом slow 4G), пресет публикуется вместе с методикой D-19. Тип соединения в события не добавляется: `effectiveType` доступен только в Chromium, срез «первый визит с мобильного на сотовой сети» — сотни в месяц, а главное, это энтропия поверх разрешения экрана и браузера из D-22 п. 3. Полевой TTR сохраняется одним несегментированным числом как наблюдение (M-15). Введено правило: метрику, которую можно купить только расширением списка отправляемых полей, меряем в лаборатории. | 2026-09-03 |
| Q-48 | Что является T0, от которого отсчитываются все сроки BRD | **D-39:** T0 — день, когда занят домен `dokey.app` и создан публичный репозиторий; внутри счётчика подготовка, инфраструктура, разработка и релизная неделя. Разбор состава A-06 нашёл две дыры: настройка VPS с Umami и прокси (D-22, D-23) и обвязка образа подписью и SBOM (D-28) не входили ни в одну оценку — итого 2,5–4,5 человеко-дня подготовки (A-15). Полный путь до релиза — 37,5–44,5 человеко-дня, 9,5–15 недель; M-01 взята по верхней границе и сдвинута с 2026-12-07 на 2026-12-21. Вариант «два счётчика» отпал: калибровка A-06 меряется на одной закрытой позиции и от T0 не зависит. D-20 выполняется по построению. Побочно открыт Q-55. | 2026-09-03 |
| Q-47 | Какие метрики получают абсолютные целевые значения, а какие остаются сравнением версий | **D-38:** число даётся метрике только там, где можно назвать проектное решение, которое оно запрещает. Три вида метрик: **бюджет** (число + порог провала, проверка в лаборатории или CI), **наблюдение** (базовое значение и Δ ≥ 0, режим D-14 п. 6 и D-34 п. 4), **ориентир-прогноз** (число есть, целью является известное отклонение факта от него). Проверка показала, что признак воспроизводит существующее разделение точно, кроме двух строк: ≥ 20 % возвратов и ≥ 8 % установок PWA из D-03 не ограничили ни одного решения — числа сняты, метрики переведены в наблюдение. Доля операций без клика сохраняет ≥ 60 % как лабораторный бюджет на фикстурах. | 2026-09-03 |
| Q-51 | Является ли «доля операций без клика мышью» допустимым событием продукта | **D-38:** утратил предмет. Метрика стала лабораторным бюджетом, проверяемым на сценариях-фикстурах в CI, поэтому инструментовка способа ввода в проде не требуется: закрытый список отправляемых полей из D-34 п. 3 не расширяется, страница «Как это проверить» не усложняется. | 2026-09-03 |
| Q-46 | Что происходит с обещаниями vision, если мейнтейнер останавливается | **D-37:** отсутствие обязательств произносится вслух — нет SLA, поддержки, гарантии срока жизни и срока выпуска исправлений безопасности, — и рядом перечисляется, что работает без мейнтейнера: отсутствие обращений в сеть в рантайме (запиннутый образ и установленный PWA продолжают работать), Apache-2.0, подпись и SBOM из D-28, отсутствие серверной части. Условия передачи (объявление, репозиторий не удаляется, домен ведёт на последнюю сборку) записываются в README без дат и порогов, запрещённых D-11. Раздел 3 vision не правится: он держится на свойствах, а не на обязательстве. | 2026-09-03 |
| Q-45 | Достаёт ли основной канал из D-08 до целевого пользователя | **D-36:** не достаёт в том слое, который был проработан. Канал разобран на три слоя с разной аудиторией и сроком жизни: событие (Show HN, 48 часов, расходуется однократно), self-hosted-каталоги (годы, но пересечение с A-01 слабое — понижены до поддерживающего) и площадки под A-01, включая русскоязычные, которых в D-08 не было вовсе — назначены основным слоем. Введён критерий отбора площадки из четырёх пунктов по образцу D-21; очерёдность обратна привычной — Show HN после накопительных заявок без порога. Обнаружено расхождение: по языку поиска мы русские (D-07), по языку сообществ были английские. | 2026-09-03 |
| Q-44 | Не является ли реальной альтернативой AI-ассистент, а не сайт-конкурент | **D-35:** является — и это подтверждено данными: опорные домены ниши теряют двузначные проценты трафика помесячно (JSONFormatter.org −14,21 % и −21,24 %, Code Beautify −22,63 %), а ассистент снимает ровно ту боль, что записана в research первой. Но позиционирование, north star и состав каталога не меняются — решение PO при озвученной рекомендации сменить главный аргумент на «самый быстрый из путей, не отправляющих ввод наружу». Ассистент внесён в конкурентную рамку, вывод «ниша живая» исправлен, заведён RISK-13, зафиксированы два триггера переоткрытия. | 2026-09-03 |
| Q-01 | Конфликт рекламной монетизации с приватностью и офлайном | **D-01:** donation-first, ad-light. Только cookieless-сети (EthicalAds, Carbon), AdSense отвергнут; реклама изолирована от рабочей области и полностью отсутствует в установленном PWA; DoKey Supporter — разовый офлайн-ключ без аккаунта. Зафиксировано: реклама не является бизнес-моделью на горизонте MVP. | 2026-09-02 |
| Q-02 | Как совместить «страница на инструмент» (SEO) с «всё в одном табе» | **D-02:** SSG-пререндер уникального URL на каждый инструмент плюс клиентская навигация после первой загрузки (Astro, islands, ленивые чанки, service worker). Первый экран лендинга — поле ввода, SEO-текст ниже. | 2026-09-02 |
| Q-05 | Чем измеряется «скорость и низкое трение» | **D-03:** north star — Time-to-Result. Цели: p50 ≤ 1.5 с при повторном визите и ≤ 3 с из поиска на 4G; ≥ 60 % операций без клика мышью; точность автоопределения типа ≥ 95 %. Проверка — Lighthouse CI как гейт плюс публикуемый сравнительный бенчмарк против пяти конкурентов. | 2026-09-02 |
| Q-09 | Что считается «инструментом» | **D-04:** четыре обязательных условия — полная вычислимость в браузере, ввод «текст или небольшой файл», решение задачи за секунды, а не за минуты, и принадлежность аудитории A-01. | 2026-09-02 |
| A-07 | Действуют ли красные линии по AI, аккаунтам, шерингу и нативным приложениям | **D-05:** абсолютны — серверная обработка, аккаунты, сетевые AI-вызовы, шеринг ссылкой, командная работа. Ограничены горизонтом — локальный AI в браузере (не в MVP, дверь открыта) и нативные приложения (отклонены на 12 месяцев). Браузерное расширение красной линией не является и остаётся кандидатом R2. | 2026-09-02 |
| Q-03 | Реалистичен ли SEO-прорыв в занятой нише | **D-07:** выдача не заперта (по `jwt decoder online` нет даже jwt.io, по `json formatter online` есть молодые домены; KD головы 18–32), но конкурент — вендорские tool-страницы с чужой бизнес-моделью, а позиционирование по приватности уже занято в сниппетах. Цель — RU-выдача, EN-модификаторы отличия и страницы сравнения; голова EN не является целью первого года. Ориентир 1 000–5 000 визитов/мес к 12-му месяцу, из чего следует обязательность второго канала (Q-32). | 2026-09-02 |
| Q-32 | Какой второй канал привлечения | **D-08:** открытый код, Docker-образ и каталоги self-hosted — единственный доказанный в категории накопительный канал (IT-Tools: 39 569 звёзд через self-hosting). Всплеск на HN отвергнут как основной путь: питч отработан DevUtils, DevToys и DevTools-X, а всплеск не лечит нулевой retention. Show HN — один раз, углом бенчмарка TTR. | 2026-09-02 |
| Q-12 | Открывать ли исходный код | **D-08:** да, AGPL-3.0. Оказался тем же вопросом, что и Q-32: накопительный канал без открытого кода недоступен. MIT отвергнут (разрешает закрытый форк), GPLv3 отвергнут (не покрывает сетевое использование). Барьер копирования признан не главным ещё в D-07. | 2026-09-02 |
| Q-27 | Self-hosted-дистрибуция как канал | **D-08:** да, и не «вторым годом», а с первого публичного релиза: Docker-образ в GHCR — артефакт первого класса, листинги (awesome-selfhosted, selfh.st, AlternativeTo) — отдельная задача релиза. Конфликта с D-05 нет: это статика, а не бэкенд. Открытый вопрос ценности такой сборки при наличии PWA вынесен в Q-34. | 2026-09-02 |
| Q-34 | Зачем self-hosted, если PWA уже офлайновый | **D-09:** для отдельного разработчика — незачем, и так и говорим. Образ адресован организации: air-gapped-контур, один URL на команду, фиксация версии силами ИБ, нулевой внешний след. Предпосылка D-08 уточнена: в README IT-Tools нет ни слова про PWA и офлайн, там Docker — единственный путь к локальной копии, у DoKey он не единственный. Профиль сборки `selfhosted` — ноль внешних доменов. | 2026-09-02 |
| Q-36 | Пересмотр лицензии из D-08 | **D-10:** Apache-2.0. AGPL отменён: от реального сценария (клон с рекламой в выдаче) он не защищает — клон просто публикует наши же исходники, — а платой был корпоративный контур из D-09, где AGPL прямо запрещён политиками уровня Google. Барьер копирования принят равным нулю сознательно; защита — скорость, каталог и позиции. CLA не вводится. | 2026-09-02 |
| Q-06 | Метрика успеха MVP и порог остановки | **D-11:** количественных гейтов нет — проект личный и обучающий, критерий успеха не числовой. Порог трезвости 12 месяцев из D-01 отменён. Метрики D-03 остаются как инструмент качества, бенчмарк TTR — как проектная проверка, а не право продолжать. Единственное зафиксированное обязательство — выпустить P0a публично. | 2026-09-02 |
| Q-37 | Нужна ли реклама вообще | **D-12:** слот проектируется, но не включается. В релизной сборке ноль сторонних доменов, `script-src`/`connect-src` — `'self'` без исключений; пустое место под баннер не резервируется; кода интеграции в P0a нет. Supporter-ключ не реализуется, поддержка — статическая ссылка. Расчёт выручки D-01 пересчитан на реальный трафик: около $10/мес против $80–250. | 2026-09-02 |
| Q-07 | Что удерживает после первой задачи | **D-13:** возврат переопределён как становление точкой входа, а не вовлечение. В P0a: установка PWA, OpenSearch-ярлык (пусковой, без передачи данных в URL — иначе токен утекает в синхронизируемую историю браузера) и командная палитра. История операций отвергнута и переведена в красные линии: её вход — это секреты. Расширение остаётся в R2. | 2026-09-02 |
| Q-16 | Неоднозначность автоопределения типа | **D-14:** определитель ранжирует, а не классифицирует. Лестница специфичности JWT → JSON → UUID → URL → percent → timestamp → hex → base64 → текст; побеждает самый специфичный кандидат с осмысленным результатом. Лучший применяется сразу, альтернативы — чипсами в одно нажатие. Неизмеримая «точность ≥ 95 %» из D-03 заменена на долю ручных переключений типа. Корпус фикстур ведётся с первого дня. | 2026-09-02 |
| Q-23 | Доля трафика ниши, принадлежащая ядру A-01 | **Закрыт без решения как утративший смысл.** Вопрос обслуживал план по трафику и план по деньгам; D-11 отменил второй, D-07 понизил первый до вторичного канала, D-08 сделал основным каналом листинги, где аудитория самоотбирается в ядро A-01 по построению. Ни один возможный ответ не меняет ни одного решения. Полезный остаток — оценка русскоязычного спроса — перенесён в Q-08. | 2026-09-02 |
| Q-25 | Zero-egress — критерий выбора или гигиена | **D-15:** обещание — гигиена (его заявляют все, включая новичков в выдаче), проверяемость — отличие, но проверяет её не пришедший из поиска, а техническая аудитория листингов и HN. Значит, zero-egress — аргумент канала, а не привлечения: в сниппетах на него не давим, в README и листингах ставим в центр вместе с инструкцией проверки. RISK-03 реализовался и переписан в риск концентрации канала. | 2026-09-02 |
| Q-39 | Поведение определителя на простом тексте | **D-16:** случая «нет кандидата» не существует — есть «нет кандидата на декодирование», а текст это направление кодирования. Всегда показывается счётчик (символы, байты UTF-8, строки, слова) плюс чипсы обратного направления; автоматически не применяется ничего. Текстовые инструменты остаются в P1. | 2026-09-02 |
| Q-08 | Достаточно ли RU-выдачи и нужен ли полный контент | **D-17:** RU-выдача не тоньше, а мельче — плотность мелких доменов выше английской, а обещание приватности коммодитизировано сильнее (сниппеты конкурентов буквально «данные не отправляются на сервер»). Лендинги сводятся к уникальным title/description/H1, короткому вступлению и FAQ; длинный SEO-текст из P0a убран, экономия 2-3 дня. Порядок языков не меняется. | 2026-09-02 |
| Q-40 | Что несёт первый экран лендинга | **D-18:** пустое поле в фокусе (вставка работает сразу), разобранный пример в области результата до первого ввода, строка «внешних запросов: 0» как измерение через `performance.getEntriesByType`, а не слоган. Предзаполненное поле в стиле jwt.io отвергнуто — лишнее движение там, где обещана скорость. | 2026-09-02 |
| Q-30 | Страницы сравнения «alternative to» | **D-19:** одна страница бенчмарка вместо набора — методика, таблица TTR, воспроизводимый скрипт, честные оговорки. Сравнение только с рекламными порталами; с OSS-соседями (IT-Tools, DevToys, CyberChef) не сравниваемся, их сообщества и есть наш основной канал. Проигранный замер публикуется тоже. | 2026-09-02 |
| Q-10 | Свободны ли имя и домен DoKey | **D-20:** имя сохраняется, риск принят осознанно. `dokey.io` занят действующей компанией Octopulse SAS (SEO-сервис, другая категория), на GitHub есть заброшенный `federico-terzi/dokey-desktop`, `dokey.dev` зарегистрирован. Берём `dokey.app` и занимаем имя на GitHub — до первого публичного упоминания. Зафиксированы триггеры пересмотра. | 2026-09-02 |
| Q-33 | Как открытый код не размывает каталог | **D-21:** риск переназван — вредит не ширина (IT-Tools с 86 инструментами не проиграл, ленивые острова не влияют на TTR), а разнобой формы и вечная поддержка. Введён публичный контракт инструмента: четыре критерия D-04 + единая форма + бюджет чанка + фикстуры в корпус D-14 + обе локали. Процесс «сначала issue, потом PR». Количество не лимитируется. | 2026-09-02 |
| Q-17 | Сбор TTR-метрик без нарушения обещания | **D-22:** self-hosted Plausible/Umami, но проксируемые через собственный origin (`dokey.app/s/…`), поэтому CSP D-12 не меняется и счётчик D-18 остаётся нулевым. Обязательно явное раскрытие прокси на странице проверки: что отправляется, что не отправляется никогда, куда идёт. Содержимое инструментов не покидает браузер; уважаются DNT и GPC; в self-hosted-сборке аналитики нет. Обещание уточнено с «ни одного стороннего запроса» на «ни одного запроса с вашими данными». | 2026-09-02 |
| Q-42 | Где держать аналитику из D-22 | **D-23:** Umami на собственном VPS за $5-6/мес, за прокси D-22. Plausible CE отвергнут — ClickHouse требует 2-4 GB ради 5 тыс. визитов; Plausible Cloud ($9/мес) отвергнут по цене при отсутствии дохода; бесплатный serverless — из-за засыпающей базы. Постоянные расходы проекта: ~$72/год плюс домен. Аналитика обязана деградировать молча. | 2026-09-02 |
| Q-19 | Редактор ввода в бюджете 40 KB | **D-24:** CodeMirror 6 в ленивом чанке поверх SSG-textarea, только для JSON Workbench. Первый кадр — обычная textarea в фокусе, ноль JS; редактор подменяет её после первой отрисовки, не двигая каретку. Сборка тонкая, `basic-setup` не используется (это и есть 124 KB). Остальные инструменты остаются на textarea. Абстракция одна — адаптер ввода, нужный и для определителя D-14. | 2026-09-02 |
| Q-20 | Ввод секрета при верификации JWT | **D-25:** верификация — необязательный свёрнутый шаг; ноль персистентности для ключа и токена (токен тоже секретен); очистка при смене маршрута, но не по потере фокуса вкладки; визуальное маскирование без `type=password`, чтобы не провоцировать менеджеры паролей; HS256 остаётся как флагманский сценарий D-04. Остаточный риск чтения DOM расширениями назван открыто. | 2026-09-02 |
| Q-31 | Нужны ли цепочки операций | **D-26:** петля «продолжить с результатом» вместо конструктора рецептов — значение уходит во ввод, определитель D-14 отрабатывает заново. Только явным действием пользователя, с очисткой по D-25; ключи и секреты не передаются никогда. Полноценные цепочки отвергнуты как территория CyberChef и нарушение критерия D-04. | 2026-09-02 |
| Q-43 | Бюджет ленивого чанка редактора | **D-24, пункт 8:** ≤ 90 KB gzip, проверяется в CI. Лестница отката при превышении: подсветка скобок и сворачивание → языковой пакет JSON → возврат к textarea для JSON Workbench. Чанк не запрашивается на инструментах, которые его не используют. | 2026-09-02 |
| Q-41 | Свободен ли dokey.app и запасные варианты | **Закрыт данными.** Проверка NS: `dokey.app` — NXDOMAIN, с высокой вероятностью свободен; `dokey.com` делегирован на AWS, занят; `dokey.tools` и `dokey.sh` свободны и служат запасными в этом порядке. Побочный плюс зоны `.app` — она целиком в HSTS-preload, HTTP невозможен в принципе. Окончательно подтверждает регистратор при покупке. | 2026-09-02 |
| Q-11 | Примут ли EthicalAds / Carbon Ads | **D-27:** закрыт как утративший предмет — D-12 спроектировал слот, но не включил, кода интеграции в P0a нет. Переоткрытие обязательно при любом решении включить монетизацию. | 2026-09-02 |
| Q-18 | Доставка ключа Supporter без бэкенда | **D-27:** закрыт как утративший предмет — D-12 не реализует ключ в P0a, поддержка сведена к статической ссылке. Переоткрытие обязательно при включении монетизации. | 2026-09-02 |
| Q-24 | Существует ли платящий сегмент | **D-27:** закрыт как утративший предмет — D-11 отменил доход как цель. Собранные ориентиры (DevUtils $29/$24, Diffchecker $15-40, ограничение бесплатного тарифа OnlineTools) сохранены как входные данные для будущего разбора. | 2026-09-02 |
| Q-28 | Ценовая точка Supporter | **D-27:** закрыт как утративший предмет вместе с самим ключом. Переоткрытие обязательно при включении монетизации. | 2026-09-02 |
| Q-35 | Что требует ИБ от стороннего образа | **D-28:** keyless-подпись Sigstore, provenance-аттестация и SBOM на каждой сборке — всё в GitHub Actions, где образ и так публикуется. Сканирование уязвимостей не заводится: его заменяет регулярная пересборка на минимальном базовом образе, а потребитель прогоняет свой сканер сам. Плюс инструкция по сверке содержимого образа с репозиторием. Воспроизводимая сборка отвергнута как отдельный проект. | 2026-09-02 |
| Q-26 | Есть ли корпоративный запрет на вставку данных | **D-29:** политика существует, но нацелена на AI-сервисы (Samsung, апрель-май 2023: три утечки кода через ChatGPT за 20 дней, затем запрет); прямых свидетельств запрета на dev-утилиты не найдено. Важнее другое — запрет не сработал, теневое использование продолжалось три года. Корпоративное предложение формулируется как ответ на теневое использование, а не как соответствие политике. | 2026-09-03 |
| Q-38 | Доля активации OpenSearch-ярлыка | **D-13, пункт 6:** заведена страница «Держать под рукой» с обоими механизмами входа; одноразовая подсказка на повторном визите ведёт на неё, а не объясняет путь в настройках Chrome одной строкой (перейти на `chrome://settings` со страницы всё равно нельзя). Доля активации принята как единицы процентов — механизм берётся ради дошедших, а не ради массовости. | 2026-09-03 |
| Q-13 | Лимиты обработки в браузере | **D-30:** ломается отрисовка, а не парсинг. Разбор, форматирование, кодирование и хэширование — в Web Worker; дерево и вывод виртуализируются; пороги объявляются в двух ступенях (мягкий с предупреждением, жёсткий с отказом), числа назначаются после замера. Отказ формулируется через обещание («считается у вас в браузере»), молчаливой смерти вкладки быть не должно. | 2026-09-03 |
| Q-14 | Нужна ли валидация на пользователях до разработки | **D-31:** нет. Повестка интервью разошлась по решениям (Q-26 → D-29, Q-31 → D-26, Q-39 → D-16, Q-24 → D-27), автор входит в целевую аудиторию, а выбранный канал (r/selfhosted, HN) сам даёт обратную связь от A-01 — быстрее и на работающем продукте. Цена решения: первое впечатление расходуется один раз, значит релиз должен быть законченным. | 2026-09-03 |
| Q-21 | Возможна ли минификация в бюджете | **D-32:** минификация JS выводится из каталога навсегда — esbuild-wasm и swc многомегабайтны, terser сотни килобайт, регулярками нельзя. CSS и HTML остаются кандидатами в P1 при условии бюджета чанка. Beautify без изменений. | 2026-09-03 |
| Q-22 | Пипетка без EyeDropper API | **D-33:** развилка не в деградации кнопки — универсальный путь нужен в любом случае. Основной способ: выбор пикселя с загруженного изображения через canvas, работает везде. Кнопка пипетки показывается только там, где API поддержан, и является ускорителем. Неактивных кнопок с пояснением не делается. | 2026-09-03 |
| Q-29 | Норма отказов и длительности визита | **D-34:** стандартные метрики для DoKey непригодны — короткая сессия у нас успех, и чем лучше работает продукт, тем хуже выглядят цифры. Измеряется доля визитов с полученным результатом плюс события неудачи с категорией причины. Целевых чисел нет (D-11), метрика служит сравнению версий. | 2026-09-03 |
| A-09 | Оценка рекламной выручки $80–250/мес | **D-12:** допущение снято пересчётом. Оценка считалась при 100 000 показов, а D-07 установил реальный потолок в 1 000–5 000 визитов; при доле блокировщиков 40–60 % это около $10 в месяц. Реклама покрывает домен и ничего сверх того, поэтому слот спроектирован, но не включён. | 2026-09-03 |
| Q-04 | Состав MVP и волны релиза | **D-06** (заменяет состав из D-04): принят список PO из 20 инструментов с четырьмя правками — хэши подняты, системы счисления опущены, Regex Tester урезан, механизмы ядра внесены отдельными строками, Unit Converter сокращён до единиц данных. Разбит на P0a (6 инструментов + палитра + автоопределение, RU-контент, 9–12 недель), P0b (5 инструментов + EN-контент, 6–7 недель) и P1 (roadmap). | 2026-09-02 |
