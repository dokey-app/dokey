# DoKey — Очередь задач

**Дата:** 2026-09-07 · **Статус:** черновик к согласованию
**Опирается на:** [implementation-plan.md](implementation-plan.md) (этапы Э-0…Э-11, §1.1 чек-лист
вертикали, §4 ветвление), [backlog.md](backlog.md) (US-001…US-067, TECH-01…TECH-15, §1.12
зависимости), [user-stories.md](user-stories.md) (критерии приёмки),
[architecture.md](architecture.md) (КМП-01…КМП-20, ИНВ-01…ИНВ-12),
[tech-stack.md](tech-stack.md) (ADR-01…ADR-18), [test-plan.md](test-plan.md) (RUN-01…RUN-18,
G-01…G-14, §7.2 чек-лист приёмки истории), [data-model.md](data-model.md) (ENT-03, ENT-19,
ENT-21…ENT-30).
**Не содержит:** критериев приёмки (они в user-stories — задача ссылается на историю, а не
пересказывает её), оценок в днях (они в implementation-plan §6), обоснований решений (decisions).
**Новые вопросы:** Q-211…Q-220 в [open-questions.md](open-questions.md).

---

## 0. Соглашения

### 0.1. Форма задачи

```
- [ ] T-042 — что сделать (US-014): границы — какие файлы и модули; готово — какая команда проходит
```

**Что сделать** — одно действие в повелительном наклонении, без «и»: два действия через «и» — это
две задачи. **US-NNN** в скобках — история, чей чекбокс двинется; `TECH-NN` — работа без истории;
прочерк — работа этапа Э-0. **Границы** — файлы и каталоги, которые задача имеет право трогать;
всё, что за ними, — предмет другой задачи. **Готово** — команда, чей зелёный статус закрывает
задачу; «посмотрел глазами» готовым не является нигде, кроме пунктов ручного чек-листа RUN-17.

### 0.2. Размер

**Один план, одна сессия, один коммит.** Три признака того, что задачу нужно резать:

1. План задачи не помещается на экран.
2. Задача трогает больше 5–7 файлов.
3. Внутри задачи нужно принять решение, которого нет ни в decisions, ни в этом документе.

Третий признак — самый дорогой: решение, принятое посреди задачи, не проходит ни через чей обзор и
всплывает через месяц как «почему так». Такие решения выносятся **заранее** в
[open-questions.md](open-questions.md) как Q-NN, а задача помечается `⚠ Q-NN` и не берётся, пока
вопрос открыт. Правило закрытия задано D-75 п. 4: вопрос закрывается **до начала этапа**, где он
нужен, а счётчик блокировок входит в чек-лист готовности этапа.

### 0.3. Порядок и блокировки

Порядок задач следует этапам [implementation-plan §3](implementation-plan.md). Внутри этапа
порядок — сверху вниз: задача ниже может зависеть от задачи выше, обратное запрещено. Зависимости,
пересекающие этап, помечены строкой `блок: T-NNN` — это те же зависимости backlog §1.12, где
нарушение порядка означает переписывание, а не неудобство.

**Запись в реестр (`src/registry/tools.ts`) — всегда последняя задача среза.** Незаконченная
позиция не имеет маршрута, не попадает в sitemap и не показывается палитрой; попавший в релиз
`Tool.id` необратим (ИНВ-07, G-14).

### 0.4. Когда задача закрыта

Задача закрыта, когда команда из графы «готово» проходит **в CI**, а не только локально, и коммит
влит в `main` через PR. История в [backlog.md](backlog.md) отмечается, когда закрыты все её задачи
— не раньше (backlog §0). Приёмка истории идёт по чек-листу test-plan §7.2.

### 0.5. Команды

Гейты и прогоны названы адресами test-plan; сокращения ниже используются в графе «готово».

| Команда | Что запускает |
|---|---|
| `pnpm lint`, `pnpm format:check`, `pnpm typecheck` | oxlint, Prettier, tsc |
| `pnpm test:unit` / `test:int` / `test:e2e` | RUN-01 / RUN-02 / RUN-03 |
| `pnpm gate:lh` · `gate:size` | G-01 · G-02 |
| `pnpm gate:persist` · `gate:egress` · `gate:fields` | G-03 · G-04 · G-05 |
| `pnpm gate:meta` · `gate:sitemap` · `gate:ladder` | G-06 · G-07 · G-08 |
| `pnpm gate:a11y` · `gate:provisional` · `gate:nomouse` | G-09 · G-10 · G-11 |
| `pnpm gate:words` · `gate:subset` · `gate:toolids` | G-12 · G-13 · G-14 |
| `pnpm gates` | все четырнадцать подряд |
| `pnpm test:gates:negative` | негативные проверки гейтов (D-106 п. 1); самопроверку покрытия по `GATES` (п. 2) заводит T-261 |
| `pnpm test:matrix` · `test:image` · `verify:supply` | RUN-04 · RUN-14 · RUN-15 |
| `pnpm bench:ttr` · `bench:compare` · `check:prod` | RUN-09 · RUN-09 против площадок · RUN-16 |

### 0.6. Вопросы, закрываемые до старта этапа

| Этап | Обязаны быть закрыты | Почему до, а не по ходу |
|---|---|---|
| **Э-0** | ~~Q-217, Q-218~~ — закрыты 2026-09-08 (D-84, D-85) | Оба про состав самого этапа: где стоит образ и открыт ли прод наружу |
| **Э-1** | ~~Q-149~~ (D-88), ~~Q-220~~ (D-98), ~~Q-147~~ (D-105) — все закрыты | Без определения «initial JS» бюджет NFR-03 проверяется на глаз, а он — первый гейт каркаса |
| **Э-2** | ~~Q-212, Q-213~~ (D-102), ~~Q-138, Q-139~~ (D-109), ~~Q-186~~ (D-168) — все закрыты | Оба P1 были про бюджет и дату; Э-2 — первая встреча плана с календарём |
| **Э-5** | ~~Q-205~~ закрыт 2026-09-09 (D-110) | Ввод «за порогом» нечем породить |
| **Э-6** | ~~Q-151~~ (D-89) и ~~Q-215~~ (D-111) закрыты | Service worker в незащищённом контексте и предмет staging |
| **Э-9** | ~~Q-152, Q-155~~ закрыты 2026-09-09 (D-112) | Два выключателя расписания и 429 от Docker Hub |
| **Э-10** | ~~Q-153, Q-157~~ закрыты 2026-09-09 (D-113) | Ретенция событий и поведение прокси при исчерпании лимита |
| **Релиз** | ~~Q-206~~ (D-143), ~~Q-209~~ (D-172), ~~Q-210~~ (D-173), ~~Q-214~~ (D-145) — все закрыты | Все четыре — про допуск релиза: что блокирует приёмку и какой состав NFR её держит |

---

## 1. Э-0 — труба

**Готово этапа:** шесть пунктов демонстрации implementation-plan §2.4. Функционального кода в
этапе нет.

- [x] T-001 — завести аккаунт Cloudflare, оформить `dokey.ru` у регистратора, делегировать зону, проверить TLS (TECH-01, D-86, D-114, D-125, D-175): границы — `infra/dns/README.md`; готово — `dig NS dokey.ru +short` отдаёт NS Cloudflare, зона активна, сертификат выдан, почтовый алиас `privacy@dokey.ru` принимает письмо
- [x] T-225 — завести алиасы `security@dokey.ru` и `conduct@dokey.ru`, уже названные в `SECURITY.md` и `CODE_OF_CONDUCT.md` (D-175 п. 1; найдено в T-001 2026-09-16): границы — `infra/dns/README.md`; готово — оба адреса в Email Routing, письмо на каждый доходит до ящика пересылки
- [x] ~~T-226 — продлить `dokey.ru` на несколько лет вперёд (D-162 п. 4, SEC-12; T-001 закрыта с оплатой на год решением PO 2026-09-16): границы — `infra/dns/README.md`, строка «Дата истечения»; готово — `paid-till` в whois `whois.tcinet.ru` не раньше 2028-08-28~~ — **снята D-177** 2026-09-16: домен продлевается по году
- [x] T-002 — занять на GitHub организацию `dokey-app` и создать в ней публичный репозиторий `dokey` (TECH-13, D-155): границы — настройки репозитория, `README.md` в одну строку; готово — `github.com/dokey-app/dokey` открывается анонимно
- [x] T-003 — положить LICENSE Apache-2.0, NOTICE, CONTRIBUTING, CODE_OF_CONDUCT (TECH-12): границы — четыре файла в корне; готово — GitHub показывает лицензию Apache-2.0 в шапке репозитория
- [x] T-004 — завести шаблоны issue и PR по контракту D-21 (TECH-12, готовит US-050): границы — `.github/ISSUE_TEMPLATE/*`, `.github/pull_request_template.md`; готово — форма нового issue предлагает шаблон «предложить инструмент» с семью пунктами контракта
- [x] T-005 — поднять каркас Astro 7.3.1, `output: 'static'`, ноль интеграций фреймворков (TECH-04, ADR-01): границы — `package.json`, `astro.config.mjs`, `tsconfig.json`, `src/pages/index.astro`; готово — `pnpm build` даёт статический каталог
- [x] T-006 — разложить дерево каталогов по компонентам architecture §2.1 (TECH-04): границы — `src/README.md`, пустые каталоги с `.gitkeep`; готово — `pnpm typecheck`, каждый КМП-NN имеет строку назначения
- [x] T-007 — настроить oxlint 1.81.0 и Prettier 3.9.6 (ADR-16): границы — `.oxlintrc.json`, `.prettierrc`, `package.json`; готово — `pnpm lint && pnpm format:check`
- [x] T-008 — настроить Vitest 5.0.0 двумя проектами: node и browser mode (RUN-01, RUN-02): границы — `vitest.config.ts`, `tests/unit/smoke.test.ts`, `tests/integration/smoke.test.ts`; готово — `pnpm test:unit && pnpm test:int`
- [x] T-009 — настроить Playwright 1.63.0 и первый сценарий (RUN-03): границы — `playwright.config.ts`, `tests/e2e/smoke.spec.ts`; готово — `pnpm test:e2e`
- [x] T-010 — описать заголовки ответа единым источником: CSP `'self'` без исключений, HSTS, правила кеша (TECH-02, ИНВ-01): границы — `infra/headers/_headers`; готово — `pnpm check:headers` на `wrangler dev` печатает три заголовка ожидаемого вида
- [x] T-215 — описать рунбук доступа к четырём учётным записям и двум секретам автоматики (TECH-01, D-162): границы — `infra/access/README.md`, `docs/security-and-access.md` §5.3; готово — в реестре секретов §5.3 нет строки без срока ротации, у каждой из четырёх учётных записей записаны второй фактор и порядок восстановления, `gh api repos/dokey-app/dokey/environments/production` показывает развёртывание только для тегов `v*`
- [x] T-230 — привести текст заглушки к content-guide §5 (D-85, D-155): на `index.astro` сейчас свой текст с обещаниями и ссылка на чужую организацию `github.com/dokey/dokey` (найдено в T-011 2026-09-17): границы — `src/pages/index.astro`; готово — `pnpm build` даёт страницу ровно с тремя текстами `DoKey` · `Утилиты разработчика в браузере` · `Исходный код на GitHub`, ссылка ведёт на `https://github.com/dokey-app/dokey`, `pnpm gates` зелёный
- [x] T-011 — задеплоить заглушку на `dokey.ru` через wrangler (TECH-02, ADR-10, D-114, D-162, D-178, D-179): границы — `wrangler.toml`, `.github/workflows/release.yml`; готово — ROLE-03 привязал `dokey.ru` к Worker'у `dokey` в панели, после тега `v0.0.0` на `main` страница открывается, `curl -sI https://dokey.ru` возвращает ответ хостинга и `strict-transport-security`, заголовки совпадают с `_headers`, токен деплоя — со сроком ≤ 6 месяцев и одним правом `Workers Scripts:Edit`; блок: T-230
- [x] T-235 — выключить NEL в зоне `dokey.ru` и научить RUN-16 падать на заголовках отчётов (ИНВ-01, D-180): Cloudflare дописывает к каждому ответу прода `Report-To` и `NEL` с адресом `a.nel.cloudflare.com` (найдено в T-011 2026-09-18): границы — настройка зоны (ROLE-03), `infra/dns/README.md`, `scripts/check-headers.ts`; готово — `curl -sI https://dokey.ru` не содержит ни `nel`, ни `report-to`, ни `reporting-endpoints`, `DOKEY_TARGET=https://dokey.ru pnpm check:headers` зелёный и красный на ответе с любым из трёх, в таблице результата `infra/dns/README.md` есть строка «Network Error Logging Cloudflare — выключен»
- [x] T-234 — развести кеш-правила `_headers` так, чтобы частное правило заменяло `Cache-Control` из `/*`, а не дописывало: Cloudflare склеивает одноимённые заголовки подходящих правил через запятую, и прод отдаёт на `/_astro/*` `public, max-age=0, must-revalidate, public, max-age=31536000, immutable`, а комментарий в файле утверждает обратное (найдено в T-011 2026-09-18): границы — `infra/headers/_headers`, `scripts/gates/headers-parity.ts`; готово — после релиза `curl -sI https://dokey.ru/_astro/<файл>` отдаёт ровно `cache-control: public, max-age=31536000, immutable`, `/sw.js` — ровно `no-cache`, `pnpm gate:headers-parity` зелёный
- [x] T-236 — добавить негативную проверку RUN-16 на склейку заголовков `_headers` (D-106, T-234): блок: T-259; сборка, где частное правило задаёт `Cache-Control` без `! Cache-Control`, валит конвейер (найдено в T-234 2026-09-22 — проверка склейки в гейте заведена без негативной сборки): границы — `tests/gates/negative/*`; готово — `pnpm test:gates:negative` красный на `_headers` без любой строки `! Cache-Control`
- [x] T-238 — уложить `tests/unit/docs-structure.test.ts` в таймаут Vitest: каждый из двух случаев заново читает все 80 файлов `docs/**/*.md` (4,3 МБ) и идёт 7–8 с при пороге 5 с, поэтому `pnpm test:unit` красный на чистом дереве, и падает то один случай, то другой (найдено в T-235 2026-09-18, Windows): границы — `tests/unit/docs-structure.test.ts`; готово — `pnpm test:unit` зелёный три прогона подряд; блок: T-241
- [x] T-241 — уложить в таймаут Vitest два остальных случая `tests/unit`: `size-gate.test.ts` и `check-headers.test.ts` поднимают на случай по дочернему процессу и при загруженной машине выходят за 5 с — падает то один случай, то другой, а на свободной машине оба зелёные (найдено в T-238 2026-09-20): границы — `tests/unit/size-gate.test.ts`, `tests/unit/check-headers.test.ts`; готово — `pnpm test:unit` зелёный три прогона подряд при занятых ядрах
- [ ] T-263 — свести два стенда запуска гейта в один и закрыть класс сторожей дочернего процесса: `tests/unit/size-gate.test.ts` зовёт `execFile` без `timeout` и без `maxBuffer` и приводит ошибку к `{ code: number }`, то есть процесс, убитый сигналом, потолком или переполнением буфера, даёт `status: undefined`, и случай засчитает это за исход гейта; в `tests/gates/negative/harness.ts` тот же стенд уже со сторожами, и две копии разъезжаются (найдено в /check T-019 2026-09-23; стенд скопирован из T-241, границы T-019 `tests/unit/*` не включают): границы — `tests/unit/size-gate.test.ts`, `tests/gates/negative/harness.ts`; готово — стенд один, оба набора зовут его, и случай, чей гейт убит сигналом или буфером, краснеет названной причиной, а не исходом гейта
- [ ] T-262 — уложить сбор G-01 в бюджет попыток на загруженной машине: на свободной машине `pnpm gate:lh` зелёный (иногда со второго сбора — «сборов 2»), а под нагрузкой обе попытки кончаются провалом в двух формах — набор не сходится с планом сбора («`/health/index.html`: отчётов 0, а не 3 — сбор до маршрута не дошёл») и `lhci collect` не оставляет ни одного отчёта; следующий прогон на том же дереве при этом зелёный, то есть предмет — устойчивость сбора, а не пороги (найдено в T-019 2026-09-23, Windows: пять прогонов, два красных под нагрузкой, три зелёных в одиночку; полнота набора введена T-229, значения аудита — T-255): границы — `scripts/gates/lighthouse.ts`, `lighthouserc.json`; готово — `pnpm gates` зелёный три прогона подряд при занятых ядрах, и неполный сбор по-прежнему валит гейт, а не досчитывается по оставшимся отчётам
- [ ] T-237 — подтвердить на проде кеш-правила T-234 после выкладки `v0.0.1` (ST-03, D-178): блок: T-032; PR #9 слит, но своего тега у выкладки нет, и прод до `v0.0.1` отдаёт на `/_astro/*` `public, max-age=0, must-revalidate, public, max-age=31536000, immutable` (найдено в T-234 2026-09-22): границы — только проверка, кода нет; готово — `curl -sI https://dokey.ru/_astro/<файл>` отдаёт ровно `cache-control: public, max-age=31536000, immutable`, `curl -sI https://dokey.ru/sw.js` — ровно `no-cache`, `/` — `public, max-age=0, must-revalidate`
- [x] T-239 — привести путь ЭП-06 к факту (D-181, D-182): api-spec §4.0 и §4.6 называют хешированные ассеты `/assets/{name}.{hash}.{ext}`, а сборка Astro и оба файла заголовков отдают их из `/_astro/*` и `/fonts/*` (найдено в /check T-234 2026-09-22): границы — `docs/api-spec.md` (§4.0 и §4.6); `docs/openapi.yaml` вне границ — его перегенерирует T-245 (D-182 п. 2–3); готово — `grep -rn "/assets/" docs/api-spec.md` ничего не находит, путь ЭП-06 в §4.0 и §4.6 — `/_astro/{name}.{hash}.{ext}` и `/fonts/{семейство}-{начертание}.{hash}.woff2`, то есть ровно правила `/_astro/*` и `/fonts/*` из `infra/headers/_headers`, `pnpm test:unit` зелёный
- [ ] T-245 — вывести `docs/openapi.yaml` из api-spec скриптом и закрыть накопленное расхождение (D-182): генератора нет ни в `scripts/`, ни в `package.json`, файл объявлен выводимым (docs/README) и правился точечно, поэтому в нём стоит упразднённый ЭП-08 `/s/script.js` (D-93), нет ЭП-12 `/robots.txt` (D-164), корень не заведён записью реестра (D-91), профиль ЭП-03 не сужен (D-163), а ассеты ЭП-06 живут на `/assets/` вместо `/_astro/*` и `/fonts/*` — часть расхождения накоплена откладыванием «подхватит при генерации», а не сломана задачей (найдено при планировании T-239 2026-09-22): границы — `scripts/gen-openapi.ts`, `package.json`, `docs/openapi.yaml` (только выводом скрипта), при необходимости разметка `docs/api-spec.md`; **USER_ACTION: человек единожды разрешает запись в `docs/openapi.yaml` на время задачи — генератором, не руками; запрет `Edit`/`Write` в `.claude/settings.json` задача не правит и не обходит (D-182 п. 1)**; шаблон пути `/fonts/{семейство}-{начертание}.{hash}.woff2` в OpenAPI напрямую не выражается (сейчас в `openapi.yaml` один параметр `{assetFile}`) — как отобразить, решает план T-245; готово — `pnpm gen:openapi` перезаписывает `docs/openapi.yaml`, повторный прогон не даёт диффа, `docs/openapi.yaml` проходит валидацию OpenAPI (инструмент выбирает план T-245: в `tech-stack.md` и `package.json` валидатора сейчас нет), в файле нет ни `/assets/`, ни `/s/script.js`, есть `/robots.txt`, и список путей совпадает с таблицей api-spec §4.0
- [ ] T-246 — обновить строку «Принятые решения» в шапке `docs/open-questions.md`: она называет диапазон `D-01…D-179`, а в `decisions.md` уже есть D-180, D-181 и D-182 — ссылка отстаёт от файла и будет отставать после каждого нового решения (найдено в T-239 2026-09-22): границы — `docs/open-questions.md` (шапка), при необходимости проверка в `tests/unit/docs-structure.test.ts`; готово — диапазон в шапке кончается последним `D-NNN` из `decisions.md`, и расхождение ловит проверка, а не глаз (какая именно — выбирает план задачи)
- [ ] T-247 — распространить условие ERR-06 в `docs/api-spec.md` §3.3 на оба префикса ЭП-06 (D-181): §3.3 описывает причину как «ЭП-06: **чанк** сборки старше 30 дней», а §4.6 — как «**Файл** сборки старше 30 дней», хотя после D-181 в ЭП-06 входят и шрифты `/fonts/{семейство}-{начертание}.{hash}.woff2`, которые чанком не являются, — узкая формулировка §3.3 неверна для второго префикса (найдено в /check контрактов T-239 2026-09-22): границы — `docs/api-spec.md` §3.3; готово — условие ERR-06 в §3.3 и §4.6 совпадают и покрывают оба префикса, `/_astro/*` и `/fonts/*`
- [ ] T-248 — убрать имя шрифта без хеша из гейта размера и токенов (D-181): `.size-limit.json` и `tests/unit/size-gate.test.ts` перечисляют `dist/fonts/manrope-400.woff2`, а `docs/Design System/tokens/fonts.css` ссылается на `/fonts/manrope-400.woff2` — это расходится с D-181 (хеш содержимого в имени файла шрифта) и с api-spec §4.6; имена шрифтов — ответственность T-028, но её «готово» об этих файлах молчит (найдено в /check контрактов T-239 2026-09-22): границы — `.size-limit.json`, `tests/unit/size-gate.test.ts`, `docs/Design System/tokens/fonts.css` либо дополнение «готово» T-028; готово — ни один из трёх файлов не называет шрифт без хеша, `pnpm gate:size` зелёный
- [x] T-240 — не выдавать «пусто» за «проверено» в проверке склейки `gate:headers-parity`: если в `infra/headers/_headers` нет правила `/*`, `concatenated()` возвращает пустой список, и гейт печатает «частные правила … заменяют заголовки /*» (найдено в /check T-234 2026-09-22): границы — `scripts/gates/headers-parity.ts`; готово — `_headers` без правила `/*` даёт `fail` с объяснением, на текущем файле гейт зелёный
- [x] T-242 — баг: сверять заголовки `gate:headers-parity` только в правиле `/*` (`scripts/gates/headers-parity.ts`): `fromHeadersFile()` берёт первую строку с нужным заголовком в любом правиле `infra/headers/_headers`, не глядя на путь правила — если `Content-Security-Policy` есть только в блоке `/sw.js`, а в `/*` её нет, `pnpm gate:headers-parity` зелёный, хотя остальные страницы идут без CSP (ИНВ-01); воспроизведение — перенести строку CSP из блока `/*` в блок `/sw.js`, `pnpm gate:headers-parity` → OK (найдено в /fix T-240 2026-09-22): границы — `scripts/gates/headers-parity.ts`, `tests/unit/headers-parity.test.ts`; готово — CSP и прочие сверяемые заголовки вне правила `/*` дают `fail`, на текущем файле гейт зелёный
- [x] T-243 — баг: сверять заголовки `gate:headers-parity` только на уровне server в `infra/docker/nginx.conf` (`scripts/gates/headers-parity.ts`): `fromNginxConf()` берёт первый `add_header` с нужным заголовком в любом месте файла, не глядя на блок — если CSP стоит только в одном `location` или `add_header` внутри `location` снимает набор уровня server, `pnpm gate:headers-parity` зелёный, хотя остальные пути идут без CSP (ИНВ-01, D-107); воспроизведение — перенести `add_header Content-Security-Policy …` из server в новый `location = /sw.js { … }`, `pnpm gate:headers-parity` → OK (найдено в /fix T-242 2026-09-22): границы — `scripts/gates/headers-parity.ts`, `tests/unit/headers-parity.test.ts`; готово — сверяемый заголовок вне уровня server и любой `add_header` внутри `location` дают `fail`, на текущем файле гейт зелёный
- [x] T-224 — привести `src/README.md` и `scripts/gates/README.md` к Prettier: таблицы в них разошлись с форматтером после 63ab142, и `pnpm format:check` красный на чистом дереве (найдено в T-001 2026-09-16): границы — два файла; готово — `pnpm format:check` зелёный без правки чего-либо ещё
- [x] T-228 — устранить зависание RUN-03 на раннере GitHub: `pnpm test:e2e` молчит после `$ playwright test` до отмены, на `main` и в PR #1 одинаково, а у job нет `timeout-minutes`, и GitHub ждёт 6 ч (найдено в T-002 2026-09-17): границы — `tests/e2e/preview-server.ts`, `tests/e2e/preview-teardown.ts`, `playwright.config.ts`, job `e2e` в `.github/workflows/pr.yml`; готово — RUN-03 в PR зелёный за ≤ 10 мин, а зависший прогон job обрывает по таймауту
- [x] T-244 — баг: задать `timeout-minutes` остальным job'ам `.github/workflows/pr.yml`: у `lint`, `typecheck`, `unit`, `integration` и `build` (сборка и гейты) потолка нет, и зависание любого держит раннер 6 ч, как было с RUN-03 (найдено в /fix T-228 2026-09-22): границы — `.github/workflows/pr.yml`; готово — у каждого job pr.yml есть `timeout-minutes` с обоснованием значения по фактической длительности зелёного прогона, как в `release.yml`
- [x] T-249 — баг: задать `timeout-minutes` job'ам `.github/workflows/release.yml`: у `gates` (сборка, `playwright install`, гейты) и `image` (docker build/push, cosign, SBOM, системный прогон) потолка нет — он стоит только у `deploy` (15 мин), и зависший релизный прогон держит раннер 6 ч умолчания GitHub, тот же дефект, что T-228 и T-244 (найдено в /fix T-244 2026-09-22): границы — `.github/workflows/release.yml`, `tests/unit/workflow-timeouts.test.ts` (проверка сейчас намеренно смотрит только `pr.yml`); готово — у каждого job `release.yml` есть `timeout-minutes` с обоснованием значения по фактической длительности прогона, проверка RUN-01 ловит job без потолка в обоих workflow
- [x] T-250 — баг: применить правило «пустое значение `timeout-minutes` — не потолок» во всех формах в `tests/unit/workflow-timeouts.test.ts`: `timeout-minutes:` без значения считается непокрытием, а явные `null`, `~`, `''` и `""` — покрытием, хотя потолка не дают ни в одной из четырёх форм; воспроизведение — заменить в `pr.yml` значение потолка любого job на `null`, `pnpm test:unit` зелёный (найдено в /check T-244 2026-09-22): границы — `tests/unit/workflow-timeouts.test.ts`; готово — все пять форм пустого значения дают проблему, на текущем `pr.yml` проверка зелёная
- [x] T-251 — баг: задать `timeout-minutes` job'ам `.github/workflows/rebuild.yml` и `.github/workflows/synthetic.yml`: у `rebuild` и `probe` потолка нет — тот же дефект, что T-228, T-244 и T-249; у `synthetic.yml` он опаснее: запуск идёт по расписанию `17 */6 * * *`, зависший прогон держит раннер 6 ч умолчания GitHub и пересекается со следующим по расписанию, а тревога наружу (issue по `if: failure()`) при зависании не заводится вовсе (найдено в /fix T-249 2026-09-23): границы — оба workflow и строка исключений в `tests/unit/workflow-timeouts.test.ts`; предложено — `rebuild` 5 мин (сейчас заглушка `echo`, предмет в Э-9), `probe` 10 мин; готово — у каждого job обоих workflow есть `timeout-minutes` с обоснованием, список исключений в тесте пуст, `pnpm test:unit` зелёный
- [x] T-252 — баг: `isCeiling` в `tests/unit/workflow-timeouts.test.ts` засчитывает дробное значение за потолок: `timeout-minutes: 5.5` проходит по белому списку `/^\d+(\.\d+)?$/`, а документация GitHub для `steps[*].timeout-minutes` говорит «Fractional values are not supported, must be a positive integer» — проверка зелёная там, где GitHub потолка не даёт, то есть ложная зелень против правила самого T-250; воспроизведение — заменить в `pr.yml` значение потолка любого job на `5.5`, `pnpm test:unit` зелёный (найдено в /check T-251 2026-09-23): границы — `tests/unit/workflow-timeouts.test.ts`; готово — дробные и нецелые значения дают проблему, на четырёх workflow проверка зелёная
- [x] T-254 — баг: `isCeiling` в `tests/unit/workflow-timeouts.test.ts` засчитывает `0` за потолок: белый список `/^\d+$/` пропускает `0` и `00`, а документация GitHub о `timeout-minutes` требует «positive integer», и собственная реализация GitHub того же ключа у шага при нуле потолка не ставит вовсе (`actions/runner`, `src/Runner.Worker/StepsRunner.cs`: `if (timeoutMinutes > 0) { step.ExecutionContext.SetTimeout(timeout); }`), то есть `0` означает «потолка нет» — ровно та ложная зелень, которую закрывал T-252, а комментарий теста утверждает обратное («job с ним не висит шесть часов умолчания, а обрывается сразу») без подтверждения; воспроизведение — заменить в `pr.yml` значение потолка любого job на `0`, `pnpm test:unit` зелёный (найдено в /check T-252 2026-09-23): границы — `tests/unit/workflow-timeouts.test.ts`; готово — `0` и `00` дают проблему, утверждение о поведении GitHub в комментарии либо подтверждено ссылкой на первоисточник, либо снято, на четырёх workflow проверка зелёная
- [x] T-253 — баг: `pnpm format:check` красный на чистом дереве: Prettier обходит неотслеживаемые каталоги обвязки (`orchestrator/`, `.orchestrator/`, `.claude/commands/orchestrate.md`), исключённые через `.git/info/exclude`, но не через `.prettierignore`, и пункт 1 чек-листа `.github/pull_request_template.md` не проходит ни у одной задачи, хотя в CI и по `git ls-files -z | xargs -0 -n 80 prettier --check --ignore-unknown` всё зелёное (найдено в T-224, T-244, T-249, T-251): границы — предмет проверки формата: `package.json` (`format`, `format:check`), `scripts/format.ts`, `tests/unit/format-subject.test.ts`; `.prettierignore` остаётся списком отслеживаемых файлов, которые решено не форматировать, и локальные исключения в него не переезжают; готово — `pnpm format:check` зелёный на чистом дереве с этими каталогами, предмет берётся из списка git (`--cached --others --exclude-standard`), а не обходом рабочего каталога, набор проверяемых файлов в CI после checkout прежний и состав проверок CI не меняется, RUN-01 краснеет и при возврате к обходу каталога, и при заплатке в `.prettierignore`
- [x] T-229 — сделать приговор G-01 по Lighthouse mobile устойчивым на раннере GitHub: одна сборка PR #1 дала `categories: 0.88 против 0.95`, а при перезапуске прошла (найдено в T-002 2026-09-17): границы — `lighthouserc.json`, `scripts/gates/lighthouse.ts`; готово — пять подряд перезапусков job «Сборка и гейты» на одном коммите дают один и тот же вердикт G-01; если для этого нужно менять число прогонов или порог (D-100, NFR-04) — сначала Q-NN
- [x] T-255 — баг: агрегат аудита в G-01 считается по тем прогонам, где аудит дал число: `@lhci/utils/src/assertions.js:77-82` берёт `values.filter(isFiniteNumber)`, и если сверяемый аудит дал число не во всех прогонах маршрута, медиана берётся по оставшимся одному-двум без всякого сигнала, а полнота набора отчётов, введённая T-229, этого не закрывает — считает отчёты, а не значения аудита в них (найдено в /check T-229 2026-09-23): границы — `scripts/gates/lighthouse.ts`, `tests/unit/lighthouse-gate.test.ts`; готово — прогон, в котором сверяемый аудит дал число не во всех отчётах маршрута, валит G-01 с указанием аудита и числа значений, на текущей сборке гейт зелёный
- [ ] T-227 — замерить доступность прода из российских сетей (D-176, Q-241): границы — ресурс-зонд ≥ 64 KB через Worker, удаляется тем же PR; итог — строка в [tech-stack.md](tech-stack.md) ADR-10; готово — пять операторов (Ростелеком, МТС, МегаФон, Билайн, Т2), домашняя и мобильная сеть, по три попытки: ресурс целиком за ≤ 3 с везде, либо записан провал и заведены задачи переезда по ST-03 до T-201; блок: T-011
- [ ] T-223 — подать `dokey.ru` в HSTS-preload: редирект `http`→`https` включён, домен отправлен на `hstspreload.org` (D-175): границы — `infra/dns/README.md`; готово — форма на `hstspreload.org` принимает домен без замечаний, статус домена там — `pending` или выше; блок: T-011
- [ ] T-232 — завести `www.dokey.ru` редиректом на apex: `infra/dns/README.md` п. 2–3 требует записи и редиректа, а T-011 привязывает только `dokey.ru` (найдено в T-011 2026-09-17): границы — `infra/dns/README.md`; готово — `curl -sI https://www.dokey.ru` отдаёт `301` с `location: https://dokey.ru/`; блок: T-011
- [ ] T-012 — закрыть индексацию до тега релиза: `Disallow: /` и `noindex` (D-85): границы — `public/robots.txt`, `src/layouts/Base.astro`; готово — `curl https://dokey.ru/robots.txt` отдаёт запрет обхода
- [x] T-013 — собрать конвейер PR: линт, типы, RUN-01…RUN-03, сборка (TECH-05, US-047, D-129): границы — `.github/workflows/pr.yml`, `.oxlintrc.json`; готово — PR показывает шесть зелёных проверок, а присваивание `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `srcdoc` или вызов `document.write` в `src/**` останавливает `pnpm lint` (SEC-21)
- [ ] T-257 — привести состав запрета SEC-21 в документах к факту: правило в `.oxlintrc.json` после T-013 называет шесть стоков (`innerHTML`, `outerHTML`, `insertAdjacentHTML`, `srcdoc`, `document.write`, `document.writeln`), а D-129 п. 2, графа «чем проверяется» SEC-21 в `docs/security-and-access.md`, Q-185 и строка T-013 — пять: `document.writeln` не назван нигде (найдено в /check T-013 2026-09-23): границы — `docs/decisions.md`, `docs/security-and-access.md`; готово — перечень стоков в документах и в `.oxlintrc.json` совпадает по составу, расхождение ловится сторожем из T-256
- [ ] T-258 — баг: `el.setAttribute('srcdoc', …)` проходит мимо запрета SEC-21: правило линта T-013 ловит обращение к свойству, а задать контентный атрибут через `setAttribute` — обычный способ, а не обфускация, и правило на нём молчит (найдено в /fix T-013 2026-09-23): воспроизведение — положить `el.setAttribute('srcdoc', s)` в любой файл `src/**`, `pnpm lint` → 0 проблем; границы — `.oxlintrc.json` и, если инструмента не хватит, `package.json`: в oxlint 1.81.0 нет ни `no-restricted-syntax`, ни `no-unsanitized`, поэтому потребуется второй инструмент линта или свой плагин — первым шагом задачи может быть Q-NN; готово — `setAttribute` с контентным атрибутом в `src/**` останавливает `pnpm lint` или равноценную проверку, а на текущем дереве она зелёная
- [ ] T-014 — включить защиту `main` и зафиксировать её слепком: PR обязателен, статусы обязательны, история линейная (US-047, D-98): границы — настройки репозитория, `infra/github/branch-protection.json`, `scripts/check-protection.ts`, раздел в `CONTRIBUTING.md` и шапка `.github/workflows/pr.yml` (только комментарий: T-013 вписал туда датированный факт «на 2026-09-23 `main` не защищён», и включение защиты — ровно момент, когда факт протухает); готово — прямой пуш в `main` отклоняется, `pnpm check:protection` зелёный и красный при расхождении со слепком, а шапка `pr.yml` больше не утверждает, что `main` не защищён
- [ ] T-015 — собрать релизный конвейер: тег → сверка защиты ветки → сборка → гейты → деплой → манифест хешей и аттестация provenance на веб-артефакт (TECH-05, US-047, D-98, D-123): границы — `.github/workflows/release.yml`; готово — тег `v0.0.1` доезжает до прода одним шагом, а тег при расхождении настроек со слепком — не доезжает; в релизе лежат `sha256` файлов сборки и аттестация на манифест, `cosign verify-attestation` с `--certificate-oidc-issuer` и `--certificate-identity-regexp` проходит, а без пиннинга проверка не засчитывается (D-148)
- [x] T-016 — завести size-limit и G-02 на initial JS, чанк, шрифты, precache (US-047, D-88): границы — `.size-limit.json`, `scripts/gates/size.ts`, `package.json`; готово — `pnpm gate:size` зелёный, определение «initial JS» записано машинно
- [x] T-017 — завести Lighthouse CI и G-01 на запиннутом пресете (US-047, NFR-01): границы — `lighthouserc.json`, `.github/workflows/release.yml`; готово — `pnpm gate:lh`
- [x] T-018 — завести адреса двенадцати оставшихся гейтов заглушками и общий раннер: границы — `scripts/gates/*.ts`, `scripts/gates/README.md`, `package.json`; готово — `pnpm gates` зелёный и печатает список выключенных с пометкой «включается в Э-N»
- [x] T-019 — завести прогон `test:gates:negative` и негативные сборки для G-02 и G-06 (US-047, D-106): границы — `tests/gates/negative/*`, `package.json`, `vitest.config.ts`, `.github/workflows/pr.yml`; готово — `pnpm test:gates:negative`: сборка, нарушающая бюджет initial JS, и сборка с повтором `title` или пустым `description` дают исход `fail` своего гейта и код возврата 1, а прогон идёт в `pr.yml` отдельным job с потолком времени
- [x] T-259 — написать негативную пару копий заголовков для RUN-16: обе половины правила профиля (US-047, D-106, D-150): блок: T-019; границы — `tests/gates/negative/*`; готово — `pnpm test:gates:negative` красный и на паре, где `strict-transport-security` вернулся в `infra/docker/nginx.conf`, и на паре, где он пропал из `infra/headers/_headers`, — обе с исходом `fail` гейта, а не падением процесса
- [ ] T-264 — написать негативный случай RUN-16 на равенство шести общих заголовков (US-047, D-106 п. 1, D-107, SEC-22): блок: T-259; главный предмет гейта негативного случая не имеет вовсе — ни одна из трёх веток цикла `CHECKED` в `scripts/gates/headers-parity.ts` не поднята прогоном: «нет в `_headers`», «нет в `nginx.conf`» и «`«a»` против `«b»`»; `tests/unit/headers-parity.test.ts` судит разборщики, а не исход гейта, а T-259 развела только `strict-transport-security` (найдено в /check T-259 2026-09-24 — правило профиля закрыто, равенство нет): границы — `tests/gates/negative/*`; готово — `pnpm test:gates:negative` красный на трёх парах — значение сверяемого заголовка разошлось, заголовок пропал из `nginx.conf`, заголовок пропал из `_headers`, — все с исходом `fail` гейта, а не падением процесса; пара печатается теми же `headersFile()` и `nginxConf()` из одной таблицы, что и в T-259
- [ ] T-260 — написать негативную сборку для G-01: пороги скорости (US-047, D-106, D-100): блок: T-019; фикстура заведомо не проходит `categories:performance` и `largest-contentful-paint`, но её приговор — три сбора Chrome, поэтому у случая свой потолок времени, а у прогона в CI — свой (T-241, T-251): границы — `tests/gates/negative/*`, `.github/workflows/pr.yml`; готово — `pnpm test:gates:negative` красный на медленной сборке с исходом `fail` гейта G-01, и зелёный прогон укладывается в объявленный потолок
- [ ] T-261 — завести самопроверку покрытия негативных случаев по `GATES` (US-047, D-106 п. 2): блок: T-259, T-260; список гейтов лежит в массиве `GATES` в `scripts/gates/run-all.ts` — единственном месте, где он есть, — но он не экспортирован, а сам файл исполняет прогон всех четырнадцати гейтов при импорте: задача выносит список в экспорт и закрывает исполнение условием `import.meta.filename === process.argv[1]`, как у самих гейтов: границы — `scripts/gates/run-all.ts`, `tests/gates/negative/*`; готово — `pnpm test:gates:negative` падает, если у гейта, чей предмет уже есть, нет негативного случая, и `pnpm gates` при этом по-прежнему зелёный
- [x] T-020 — собрать образ multi-stage на nginx 1.30.4-alpine, non-root (TECH-09, ADR-14): границы — `infra/docker/Dockerfile`, `infra/docker/nginx.conf`, `.dockerignore`; готово — `docker build && docker run` отдаёт заглушку
- [ ] T-021 — вывести обе копии заголовков из единого источника (TECH-09, D-107, D-149, D-150): границы — `infra/headers/policy.ts`, `scripts/build-headers.ts`, `infra/headers/_headers`, `infra/docker/headers.conf`, `infra/docker/nginx.conf`, `scripts/gates/headers-parity.ts`, `package.json`; готово — `pnpm build:headers` выводит оба артефакта из `policy.ts` (включая кеш-правила), nginx подключает `headers.conf` через `include`, `pnpm gate:headers-parity` красный, если перегенерация даёт дифф; профиль — параметр `policy.ts`, а не второй список: HSTS выводится только в `_headers` (D-150), `require-trusted-types-for` — в оба артефакта (D-149)
- [ ] T-022 — публиковать образ в GHCR из релизного конвейера (US-048): границы — `.github/workflows/release.yml`; готово — `docker pull ghcr.io/dokey-app/dokey:v0.0.1` анонимно
- [ ] T-233 — перевести установку cosign в `release.yml` на `sigstore/cosign-installer`, поддерживающий cosign 3: `@v3` ищет отдельную подпись `.sig`, которой у релизов cosign 3.1.3 нет, и в релизе `v0.0.0` задание образа упало после публикации — образ `v0.0.0` и `latest` лежат в GHCR без подписи и SBOM, системный прогон не шёл (найдено в T-011 2026-09-18): границы — `.github/workflows/release.yml`, задание образа; готово — перезапуск задания образа на теге проходит целиком, `cosign verify` на образе с пиннингом издателя и идентичности проходит
- [ ] T-023 — подписывать образ cosign keyless, прикладывать provenance и SBOM (TECH-10, US-048, ADR-15, D-148): границы — `.github/workflows/release.yml`, `docs/content/how-to-verify`; готово — `cosign verify` и проверка аттестации проходят на свежем теге **с `--certificate-oidc-issuer https://token.actions.githubusercontent.com` и `--certificate-identity-regexp` на релизный workflow**, а та же команда без пиннинга признаётся непройденной проверкой
- [ ] T-024 — написать инструкцию сверки образа для ROLE-05 (US-048): границы — `README.md`, `SECURITY.md`; готово — три команды из инструкции выполняются как есть на чистой машине
- [ ] T-025 — проверить образ системным прогоном: ноль сетевых обращений, первый ответ ≤ 5 с (RUN-14, NFR-16): границы — `tests/system/image.spec.ts`, `.github/workflows/release.yml`; готово — `pnpm test:image`
- [ ] T-026 — поднять VPS за TLS: Umami 3.3.1 и PostgreSQL 18.6 по digest, сертификат на поддомене, фаервол только на адреса Cloudflare и SSH, дамп базы в процедуре (TECH-03, ADR-11, D-94, D-115, D-117, D-126, D-147): границы — `infra/vps/docker-compose.umami.yml`, `infra/vps/README.md`, `infra/vps/maintenance.md`; готово — панель Umami отвечает, дамп снимается одной командой, журнал фронта не содержит ни адреса, ни User-Agent (D-126), TLS-фронт принимает `POST /s/event` с секретом плеча и отвечает `404` на любой другой путь, включая путь панели (D-147) и переписывает его на путь приёмника (D-117), журнал вмешательств заведён и первая запись в нём — сам подъём (D-115)
- [ ] T-216 — завести ежемесячное задание приёмника: удаление событий старше 12 месяцев, зашифрованный дамп, удаление предыдущего (TECH-03, D-113 п. 1, D-169): границы — `infra/vps/monthly.sh`, `infra/vps/dump-recipient.txt`, `infra/vps/README.md`; готово — ручной запуск оставляет на VPS ровно один `*.dump.age`, проверка потока `pg_restore --list` до шифрования проходит, записей старше 12 месяцев — 0
- [ ] T-027 — завести прокси одного пути `/s/event` на своём origin и отправитель без клиента Umami (TECH-03, КМП-18, D-93, D-167): границы — `wrangler.toml`, `src/pages/s/event.ts`, `src/analytics/send.ts`, `docs/api-spec.md` §3.3; готово — `POST /s/event` доезжает до Umami без `script.js`, состав тела сверен с Umami 3.3.1, прокси делает две подстановки из своей конфигурации — псевдоним вместо адреса (D-96) и `website` (D-104), — различение посетителей сохраняется, в теле, доехавшем до приёмника, IP отсутствует, а в перехвате запроса из вкладки нет ни IP, ни `website`, в артефакте нет ссылки ни на один скрипт под `/s/`, прокси добавляет секрет плеча из своей конфигурации (D-147), негативный набор (`/s/`, `/s/script.js`, `/s/recorder.js`, `/s/../`, `GET /s/event`) получает отказ на edge против `wrangler dev` (D-97), а запрос к пути панели с адреса Cloudflare получает `404` от фронта, панель приёмника снаружи недостижима, поведение платформы при исчерпании суточного лимита в этой схеме записано фактом в строке ERR-04 api-spec §3.3 (D-167)
- [ ] T-028 — собрать сабсеты четырёх woff2 с хешем содержимого в имени и отчёт покрытия символов (TECH-15, ADR-17, D-181): границы — `scripts/subset-fonts.ts`, `src/styles/fonts.css`, `public/fonts/*`; готово — `pnpm build:fonts` кладёт в `public/fonts/` только файлы `{семейство}-{начертание}.{hash8}.woff2` и генерирует `fonts.css` с этими именами, повторный запуск не даёт диффа, шрифты ≤ 48 KB gzip в `pnpm gate:size`
- [ ] T-029 — собрать SVG-спрайт из восьми глифов (TECH-15, D-54 п. 3): границы — `scripts/build-sprite.ts`, `src/ui/sprite.svg`; готово — `pnpm build:sprite`, зависимость иконочной библиотеки отсутствует в `package.json`
- [ ] T-030 — перенести токены дизайн-системы и контрастные правки (TECH-15, D-53): границы — `src/styles/tokens.css`, `src/styles/base.css`; готово — `pnpm gate:a11y` по контрасту на заглушке в обеих темах
- [ ] T-031 — завести синтетическую проверку прода каждые 6 часов (D-108, D-167): границы — `.github/workflows/synthetic.yml`, `scripts/check-prod.ts`; состояние — адрес workflow заведён в Э-0, но в GitHub он выключен вручную (`state: disabled_manually` с 2026-09-17, через 26 секунд после заведения; остальные три workflow `active`): ни один триггер не срабатывает, прогонов нет ни одного, и прод не наблюдается ничем (Q-156, ЧЛ-37; найдено в /check T-251 2026-09-23). Включение — действие человека в GitHub, коммитом не делается, и делается здесь, вместе с составом проверок; готово — `pnpm check:prod` проверяет пять утверждений (прод отвечает `200` с заголовками из `policy.ts`; `GET /s/event` — `405`; `GET /s/script.js` — `404`; `/s/recorder.js` — `404`; приёмник принимает `POST`), два неуспеха подряд заводят issue, третий issue не заводится, пока открыт предыдущий, ответ `/s/event` без конверта §3.2 при живом проде заводит issue «Worker не исполняется: суточный лимит (сброс в 00:00 UTC) или снят маршрут», а сам workflow включён — `gh workflow list --all` показывает `synthetic.yml` как `active`, и в `gh run list --workflow synthetic.yml` есть прогон по расписанию
- [ ] T-231 — исправить организацию в команде `cosign verify` раздела «Как проверить сборку»: `--certificate-identity-regexp` указывает на `github.com/dokey/dokey`, а репозиторий — `dokey-app/dokey` (D-155; найдено в T-011 2026-09-17): границы — `README.md`; готово — `grep -c 'github.com/dokey/' README.md` отдаёт 0
- [ ] T-032 — выпустить тег `v0.0.1` и пройти демонстрацию Э-0 по шести пунктам: границы — `README.md` раздел «Как проверить сборку», `CHANGELOG.md`; готово — шесть пунктов повторены по README посторонним человеком
- [x] T-209 — довести правки решений D-124, D-126, D-132, D-133, D-135 и D-136 по всем документам: мощность ENT-29, состав `failureReason`, пять имён событий, снятые PD-05, PD-06, PD-08 и служебный ключ, состав SCR-10 из трёх списков (найдено /check 2026-09-10): границы — `docs/api-spec.md`, `docs/data-model.md`, `docs/architecture.md`, `docs/security-and-access.md`, `docs/analytics-spec.md`, `docs/screen-map.md`, `docs/screens/*.md`, `docs/tech-stack.md`, `docs/test-plan.md`, `docs/user-stories.md`, `docs/openapi.yaml`, `docs/tasks.md` (T-196), `scripts/gates/fields.ts`; готово — `pnpm test:unit` зелёный, а grep по «восемь полей», «служебн», «PD-05», «PD-06», «Q-180», «три события» в `docs/` не находит ни одного действующего утверждения
- [ ] T-210 — привести `src/README.md` и `scripts/gates/README.md` к формату Prettier (найдено T-001 2026-09-10): границы — два файла; готово — `pnpm format:check` зелёный
- [ ] T-211 — довести пересчёт полного пути D-146 до 49,75–80,25: итог таблицы §6.2 и три строки календаря §6.3 с датами, РС-02, О-02 (найдено /questions 2026-09-10): границы — `docs/implementation-plan.md` §6.2, §6.3, `docs/roadmap.md` §5, `docs/tech-stack.md` О-02; готово — grep по «48,75» и «78,25» в `docs/` находит их только в истории решений (`decisions.md`)
- [ ] T-212 — снять с Q-223 пометку открытого P1 в таблице новых вопросов промта: вопрос закрыт D-103 (найдено /questions 2026-09-10): границы — `docs/design-system-prompt.md` §4; готово — строка Q-223 зачёркнута и ссылается на D-103, как соседняя Q-222
- [ ] T-213 — заменить адреса `github.com/dokey/dokey` и `ghcr.io/dokey/dokey` на `dokey-app` (D-155, найдено /questions 2026-09-10): границы — `src/pages/index.astro`, `README.md`, `.github/ISSUE_TEMPLATE/config.yml`; готово — `git grep -nE "(github\.com|ghcr\.io)/dokey/" -- src README.md .github` пуст, `pnpm build` проходит
- [ ] T-256 — закрыть `set:html` в `src/**`: правило линта T-013 держит SEC-21 только в JS (`innerHTML`, `outerHTML`, `insertAdjacentHTML`, `srcdoc`, `document.write`/`writeln`), а `set:html` — атрибут шаблона `.astro`, oxlint его не видит, и запрет D-129 п. 4 обходится одной директивой (найдено в T-013 2026-09-23): границы — `tests/unit/set-html.test.ts` и, если выбранному парсеру нужна зависимость, `package.json` — из корня не резолвится ни `@astrojs/compiler`, ни `@astrojs/compiler-rs` (оба лежат в store транзитивами, проверено в T-013 2026-09-23), поэтому задача без права на `package.json` невыполнима; готово — `pnpm test:unit` красный на `set:html`, подсаженном в любой файл `src/**/*.astro`, и зелёный на текущем дереве; `.astro` разбирается парсером, а не регуляркой, и файл, не разобравшийся по грамматике, — провал проверки, а не молчаливый пропуск (какой парсер — решается в самой T-256, на живом инструменте); тот же тест сторожит и сам запрет — в `.oxlintrc.json` есть правило SEC-21 на шесть имён, и его удаление краснит `pnpm test:unit`, причём конфиг после T-013 — JSONC (в нём комментарий с причиной запрета): `JSON.parse` на файле падает, а из корня не резолвится ни `jsonc-parser`, ни `strip-json-comments`, ни `comment-json` (проверено в T-013 2026-09-23), поэтому сторожу нужен либо JSONC-парсер — и тогда та же графа `package.json`, — либо строгий JSON от самого линтера: `oxlint --print-config` печатает разобранный конфиг вместе с этим `overrides`, и `JSON.parse` его берёт (проверено там же); чем именно — решается в T-256; цена запрета названа заранее: инлайн-спрайт D-54 п. 3 / D-103 (`src/ui/sprite.svg` от T-029) типовым способом вставляется ровно `set:html`, а исключений D-129 п. 4 не заводит — значит спрайту нужен другой способ вставки, вплоть до генерации `.astro` вместо `.svg`, то есть правки границ T-029; какой именно — здесь не решается

---

## 2. Э-1 — каркас в объёме первого среза

**Готово этапа:** два демо-маршрута из реестра; переход с переносом одного значения; teardown
обнуляет поля; в шапке «Внешних запросов: 0»; 10 МБ в воркере с прогрессом и отменой; двенадцать
гейтов красные на нарушающей сборке.
**Граница этапа:** не пишется ничего, чем не пользуется первый инструмент (implementation-plan §1.3).

- [ ] T-033 — описать схему реестра под Zod: `Tool.id`, маршрут, `sensitiveInput`, `continuable`, `chunk-budget`, ступень лестницы, пороги (US-002, ENT-03, ADR-06): границы — `src/registry/schema.ts`, `src/registry/types.ts`; готово — `pnpm test:unit -t registry-schema`
- [ ] T-034 — написать модуль реестра с двумя демо-записями и производными множествами (US-002, ИНВ-06): границы — `src/registry/tools.ts`, `src/registry/derive.ts`; готово — `pnpm test:unit -t registry-derive`: маршруты, sitemap, `hreflang` и палитра выводятся из одного источника
- [ ] T-035 — генерировать маршруты и `sitemap.xml` из реестра (US-002): границы — `src/pages/[...route].astro`, `src/pages/sitemap.xml.ts`; готово — `pnpm build` даёт ровно по странице на запись реестра
- [ ] T-218 — выводить `robots.txt` и `noindex` из одного признака индексации сборки (D-85, D-164): границы — `src/pages/robots.txt.ts`, `src/layouts/Base.astro`, `public/robots.txt`, `astro.config.mjs`; готово — `pnpm test:unit -t robots`: при закрытой индексации `Disallow: /` и `noindex`, при открытой — `Disallow: /s/` и `Sitemap:`, в профиле `selfhosted` — `Disallow: /` без `Sitemap`
- [ ] T-036 — включить G-07 «sitemap ≡ реестр» с негативной проверкой (US-002, US-047): границы — `scripts/gates/sitemap.ts`, `tests/gates/negative/sitemap.spec.ts`; готово — `pnpm gate:sitemap`
- [ ] T-037 — включить G-14 «множество пожизненных идентификаторов — надмножество предыдущего релиза»: `Tool.id`, `pageId`, маршруты, `ContractClause.id` (US-002, D-82 п. 2, п. 3, D-21, D-98): границы — `scripts/gates/tool-ids.ts`, `.github/workflows/release.yml`; готово — `pnpm gate:toolids` красный при исчезнувшем id
- [ ] T-214 — включить в G-02 условие «не растёт»: максимум initial JS по маршрутам не больше максимума предыдущего релиза (US-047, NFR-03, D-88, найдено T-016 2026-09-10): границы — `scripts/gates/size.ts`, `.github/workflows/release.yml`; готово — `pnpm gate:size` красный, если максимум вырос против предыдущего релиза, — тем же способом, что G-14 в T-037
- [ ] T-038 — включить G-08 «лестница ≡ реестр»: ступени отсутствующих инструментов не активны (US-002, D-49): границы — `src/registry/ladder.ts`, `scripts/gates/ladder.ts`; готово — `pnpm gate:ladder` красный на висящей ступени
- [ ] T-039 — показать сообщение на маршруте, которого нет в реестре, и отправить `result_failed(tool_not_found)` (US-002 крит. 4, D-63, D-133): границы — `src/pages/404.astro`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "маршрута нет"`
- [ ] T-217 — отдать страницу выведенной позиции: текст content-guide §5, вход в палитру, `noindex`, вне sitemap (US-002, D-82 п. 3, D-118, D-159): границы — `src/pages/[...route].astro`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "позиция выведена"` на сборке с подсаженной записью `withdrawn`
- [ ] T-040 — завести служебную страницу записью того же реестра (US-002 крит. 6, D-82 п. 1, D-160): границы — `src/registry/tools.ts`, `src/pages/[...route].astro`; готово — `pnpm gate:sitemap` и `pnpm gate:meta`: у служебной страницы непустое уникальное `description`
- [ ] T-041 — собрать SSG-шаблон лендинга: `title`, `description`, H1, вводный абзац, FAQ на сборке (US-001): границы — `src/layouts/Tool.astro`, `src/content/tools/`, `src/pages/[...route].astro`; готово — `pnpm test:e2e -g "FL-01 без JS"` при отключённом JS
- [ ] T-042 — положить поле ввода в DOM как textarea, пустое и в фокусе на первом кадре (US-001 крит. 2): границы — `src/ui/InputField.astro`; готово — `pnpm test:e2e -g "фокус на первом кадре"`
- [ ] T-219 — объявить недоступность оболочки сообщением зоны D, отрендеренным на сборке и снимаемым оболочкой при старте (US-001 крит. 5, D-172): границы — `src/layouts/Tool.astro`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "оболочка не поднялась"`: при заблокированном initial-чанке после задержки видно «Логика инструмента не загрузилась.», при обычной загрузке сообщения нет
- [ ] T-043 — включить G-06 «уникальность `title` и `description`» с негативной проверкой (US-001 крит. 4, D-160): границы — `scripts/gates/meta.ts`, `tests/gates/negative/meta.spec.ts`; готово — `pnpm gate:meta`, негативная проверка включает дубль `description` на служебной странице
- [ ] T-044 — уложить лендинг в LCP ≤ 1,2 с и Lighthouse mobile ≥ 95 (US-001 крит. 3, NFR-04): границы — `lighthouserc.json`, `src/layouts/Tool.astro`; готово — `pnpm gate:lh`
- [ ] T-045 — написать роутер над `<ClientRouter />`: карта `path → Route` из реестра, `replaceState` при подмене (US-003, ADR-02, D-47): границы — `src/router/router.ts`, `src/layouts/Base.astro`; готово — `pnpm test:int -t router`
- [ ] T-046 — написать буфер переноса: одно значение, обнуляется чтением (US-003, ENT-30, ИНВ-03): границы — `src/router/handoff.ts`; готово — `pnpm test:int -t handoff`: второе чтение отдаёт пусто
- [ ] T-047 — уложить переход в ≤ 300 мс и вернуть по «назад» маршрут без значения (US-003 крит. 1, 5; NFR-05, D-46 п. 6): границы — `tests/e2e/fl-09.spec.ts`; готово — `pnpm test:e2e -g "FL-09"`
- [ ] T-048 — написать teardown маршрута по перечню контракта на событии свопа `astro:before-swap` (US-038, ENT-23, ENT-25, D-105): границы — `src/router/teardown.ts`, `src/registry/schema.ts`; готово — `pnpm test:int -t teardown`
- [ ] T-049 — включить G-03 «нулевая персистентность»: три условия после каждой навигации (US-038, NFR-10, ИНВ-02): границы — `scripts/gates/persistence.ts`, `tests/invariants/persistence.spec.ts`; готово — `pnpm gate:persist`, негативная проверка красная
- [ ] T-050 — применить CSP без исключений на всех маршрутах и в образе (US-037, ИНВ-01): границы — `infra/headers/_headers`, `infra/docker/nginx.conf`, `tests/invariants/csp.spec.ts`; готово — `pnpm test:e2e -g "CSP"`
- [ ] T-051 — включить G-04 «сторонних origin = 0» с положительной проверкой по подстроке фикстуры (US-037, NFR-09): границы — `scripts/gates/egress.ts`, `tests/invariants/egress.spec.ts`; готово — `pnpm gate:egress`
- [ ] T-052 — задать границы чанков: по чанку на инструмент, бюджет 90 KB (US-005, NFR-06): границы — `astro.config.mjs`, `.size-limit.json`, `src/router/loadTool.ts`; готово — `pnpm gate:size`
- [ ] T-053 — показать состояние «логика не приехала» с текстом и путём восстановления (US-005 крит. 4, US-067, D-63): границы — `src/shell/toolSlot.ts`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "чанк не приехал"` с блокировкой запроса
- [ ] T-054 — заложить EN-ветку маршрутов в реестре и `hreflang`; пустая ветка не публикуется (US-006): границы — `src/registry/schema.ts`, `src/registry/derive.ts`, `src/pages/sitemap.xml.ts`; готово — `pnpm gate:sitemap`
- [ ] T-055 — сделать системную тему средствами CSS, ноль JS (US-007, D-58): границы — `src/styles/tokens.css`, `src/styles/base.css`; готово — `pnpm gate:a11y` в обеих темах, `pnpm gate:size` без прироста initial JS
- [ ] T-056 — написать протокол воркера: задача, прогресс, отмена, падение (US-031, КМП-09, ADR-08): границы — `src/worker/protocol.ts`, `src/worker/worker.ts`, `src/worker/client.ts`; готово — `pnpm test:int -t worker`
- [ ] T-057 — прогнать демо-операцию 10 МБ без блокировки главного потока > 50 мс (US-031, NFR-07, ИНВ-09): границы — `tests/integration/worker-load.spec.ts`, `scripts/gen-large-input.ts`; готово — `pnpm test:int -t worker-load`
- [ ] T-058 — написать обёртку виртуализации: число узлов DOM не растёт с размером (US-032, ADR-08): границы — `src/virtual/list.ts`; готово — `pnpm test:int -t virtual`
- [ ] T-059 — описать контракт инструмента: поля, перечень teardown, `chunk-budget`, `continuable`, `sensitiveInput` (US-029, ENT-25): границы — `src/registry/schema.ts`, `src/tools/contract.ts`; готово — `pnpm test:unit -t contract`
- [ ] T-060 — собрать единую форму: ввод, действия, область результата, копирование, «Очистить» (US-029, REQ-INPUT-07, REQ-INPUT-08): границы — `src/ui/ToolForm.astro`, `src/ui/actions.ts`; готово — `pnpm test:e2e -g "единая форма"`
- [ ] T-061 — завести статусные регионы `role="status"` и `role="alert"` в оболочке (US-029, REQ-CORE-08, D-50): границы — `src/shell/status.ts`, `src/shell/Shell.astro`; готово — `pnpm test:int -t status-regions`
- [ ] T-062 — обработать отказ буфера обмена: сообщение и оставшийся путь (US-029, D-64, ИНВ-12): границы — `src/ui/copy.ts`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "буфер недоступен"`
- [ ] T-063 — включить G-09: axe до и после JS, обе темы, все маршруты реестра (RUN-05, NFR-13, D-50): границы — `scripts/gates/a11y.ts`, `tests/invariants/a11y.spec.ts`; готово — `pnpm gate:a11y` — ноль нарушений A и AA
- [ ] T-064 — собрать каркас прогона без мыши и печатать долю (RUN-06, D-75; порог включается в Э-4): границы — `scripts/gates/no-mouse.ts`, `tests/e2e/no-mouse/`; готово — `pnpm gate:nomouse` печатает долю сценариев
- [ ] T-065 — собрать обвязку оболочки: шапка, вордмарк, статус сети, версия и дата сборки, ссылки (REQ-CORE-09, D-74): границы — `src/shell/Shell.astro`, `src/shell/network.ts`, `src/content/nav.ts`; готово — `pnpm test:e2e -g "обвязка оболочки"`
- [ ] T-066 — держать «недавние» ≤ 5 в памяти оболочки (КМП-01): границы — `src/shell/recent.ts`; готово — `pnpm test:int -t recent`: перезагрузку не переживают
- [ ] T-067 — включить G-05 «закрытые списки полей и имён событий» на схеме события (US-047, US-055 частично, ИНВ-05, D-136): границы — `src/analytics/schema.ts`, `scripts/gates/fields.ts`; готово — `pnpm gate:fields` красный на восьмом поле и красный на имени вне списка из пяти
- [ ] T-068 — включить G-10 «ноль порогов `provisional` в релизной сборке» (US-047, D-51): границы — `src/registry/thresholds.ts`, `scripts/gates/provisional.ts`; готово — `pnpm gate:provisional`
- [ ] T-069 — включить G-12 «слова-маркеры публичных обязательств = 0» (US-047, CB-08, D-77 п. 4): границы — `scripts/gates/marker-words.ts`, `scripts/gates/marker-words.list.txt`; готово — `pnpm gate:words` красный на подсаженной фразе про SLA
- [ ] T-070 — включить G-13 «все символы текстов продукта в сабсете» (US-047, D-79): границы — `scripts/gates/font-subset.ts`; готово — `pnpm gate:subset` красный на символе вне сабсета
- [ ] T-071 — написать негативные проверки для G-03…G-14, покрытие полное (US-047, D-106): границы — `tests/gates/negative/`; готово — `pnpm test:gates:negative`: каждый гейт хотя бы раз упал
- [ ] T-072 — выпустить тег `v0.1.0` и пройти демонстрацию Э-1: границы — `CHANGELOG.md`, `README.md`; готово — пять пунктов демонстрации воспроизведены на проде

---

## 3. Э-2 — первая позиция целиком

**Готово этапа:** человек вставляет ответ API на `dokey.ru`, получает дерево, находит поле,
копирует путь; ошибочный JSON даёт позицию за ≤ 300 мс; корпус фикстур останавливает сборку.
**Здесь же M-03** — две раздельные сверки (D-75 п. 3).

- [ ] T-073 — описать формат записи фикстуры и генератор синтетических токенов (TECH-11, ENT-21a): границы — `fixtures/schema.ts`, `fixtures/generate.ts`; готово — `pnpm test:unit -t fixtures-schema`
- [ ] T-074 — завести гейт против реальных секретов в репозитории (TECH-11, REQ-TRUST-06): границы — `scripts/gates/no-secrets.ts`, `.github/workflows/pr.yml`; готово — `pnpm gate:secrets` красный на подсаженном боевом токене
- [ ] T-075 — набрать корпус фикстур по ступеням с `expectedStep` и `expectedChips`, включая враждебные образцы с разметкой (US-013, D-73 п. 3, D-109, D-129): границы — `fixtures/corpus/`; готово — `pnpm test:unit -t corpus`, разметка из враждебного образца видна как текст и обработчик не исполнен
- [ ] T-076 — останавливать сборку при регрессии корпуса и группировать отчёт по ступеням (US-013 крит. 2): границы — `tests/unit/corpus.test.ts`; готово — `pnpm test:unit -t corpus` красный при подмене одного образца
- [ ] T-077 — написать лестницу специфичности: девять ступеней, порядок, интерфейс ступени (US-008, ENT-19): границы — `src/detect/ladder.ts`, `src/detect/types.ts`; готово — `pnpm test:unit -t ladder`
- [ ] T-078 — написать предикат осмысленности `meaningfulWhen` для каждой ступени (US-008, D-73 п. 3, D-109): границы — `src/detect/steps/`; готово — `pnpm test:unit -t meaningful`
- [ ] T-220 — проверять линейность распознавателей ступеней на враждебных образцах (US-013 крит. 5, THR-04, D-168): границы — `tests/unit/detect-linear.test.ts`, `fixtures/corpus/hostile/`; готово — `pnpm test:unit -t linear`: у каждой ступени время на входе 4n не больше 8× времени на n, подсаженный распознаватель `/(a+)+$/` даёт красный
- [ ] T-079 — применять самого специфичного кандидата немедленно, менять маршрут через `replaceState` (US-008, D-47): границы — `src/detect/apply.ts`, `src/router/router.ts`; готово — `pnpm test:e2e -g "FL-02"`
- [ ] T-080 — объявлять применённого кандидата статусным регионом (US-008 крит. 7): границы — `src/detect/apply.ts`, `src/shell/status.ts`; готово — `pnpm test:int -t status-regions`
- [ ] T-081 — показать чипсы всех применимых кандидатов плюс «Текст» в порядке лестницы (US-009, D-66): границы — `src/detect/chips.ts`, `src/ui/Chips.astro`; готово — `pnpm test:e2e -g "FL-03"`
- [ ] T-082 — сделать ручной выбор чипса побеждающим всегда, без перестановки позиций (US-009, D-66): границы — `src/detect/chips.ts`; готово — `pnpm test:int -t chips`
- [ ] T-083 — запретить замену исходного ввода результатом (US-010, REQ-INPUT-03): границы — `src/ui/ToolForm.astro`, `src/detect/apply.ts`; готово — `pnpm test:e2e -g "ввод не заменяется"`
- [ ] T-084 — посчитать ввод: символы, байты UTF-8, строки, слова, суррогатные пары (US-011 крит. 5): границы — `src/detect/counter.ts`, `src/ui/Counter.astro`; готово — `pnpm test:unit -t counter`
- [ ] T-085 — показать чипсы кодирования вместо пустого ответа (US-011, REQ-INPUT-04): границы — `src/detect/chips.ts`, `src/content/empty.ts`; готово — `pnpm test:e2e -g "нет кандидата"`
- [ ] T-086 — собрать стенд замеров TTR: устройство, профиль, троттлинг slow 4G, воспроизводимый скрипт (TECH-07, NFR-02): границы — `scripts/bench-ttr.ts`, `docs/bench/README.md`; готово — `pnpm bench:ttr` дважды с расхождением < 10 %
- [ ] T-087 — написать разбор JSON с позицией ошибки: строка и колонка (US-019 крит. 4): границы — `src/tools/json/parse.ts`; готово — `pnpm test:unit -t json-parse`
- [ ] T-088 — написать форматирование, минификацию и сортировку ключей по собственным ключам (US-019 крит. 1–3, SEC-23): границы — `src/tools/json/format.ts`; готово — `pnpm test:unit -t json-format`
- [ ] T-089 — показать ошибку разбора за ≤ 300 мс без очистки ввода (US-019, NFR-15): границы — `src/tools/json/ui.ts`, `tests/e2e/fl-06.spec.ts`; готово — `pnpm test:e2e -g "FL-06 ошибка"`
- [ ] T-090 — унести операции JSON в воркер поверх протокола Э-1 (US-019, US-031): границы — `src/tools/json/worker-ops.ts`, `src/worker/worker.ts`; готово — `pnpm test:int -t json-worker`; блок: T-056
- [ ] T-091 — объявить и применить ось порога `jsonDepth` (US-019 крит. 6, D-73 п. 1): границы — `src/registry/thresholds.ts`, `src/tools/json/parse.ts`; готово — `pnpm test:unit -t depth`
- [ ] T-092 — ответить вердиктом на явно запрошенную валидацию (US-019 крит. 7, D-52 п. 3): границы — `src/tools/json/ui.ts`; готово — `pnpm test:e2e -g "валидация запрошена"`
- [ ] T-093 — построить модель дерева потоковым обходом (US-020 крит. 1): границы — `src/tools/json/tree.ts`; готово — `pnpm test:unit -t tree`
- [ ] T-094 — отрисовать дерево через виртуализатор: в DOM только видимые узлы (US-020, REQ-LIMIT-02): границы — `src/tools/json/tree-view.ts`, `src/virtual/list.ts`; готово — `pnpm test:int -t virtual-tree`; блок: T-058
- [ ] T-095 — искать по ключам и значениям сразу (US-020 крит. 2, 6; D-72): границы — `src/tools/json/search.ts`; готово — `pnpm test:unit -t json-search`
- [ ] T-096 — скопировать путь узла подписью «Скопировать путь» (US-020 крит. 4): границы — `src/tools/json/path.ts`, `src/ui/copy.ts`; готово — `pnpm test:e2e -g "копировать путь"`
- [ ] T-097 — показать сообщение, когда поиск ничего не нашёл (US-064, REQ-LIMIT-04): границы — `src/tools/json/search.ts`, `src/content/empty.ts`; готово — `pnpm test:e2e -g "поиск пуст"`
- [ ] T-098 — показать разобранный пример на первом экране до ввода (US-059, REQ-CONTENT-02): границы — `src/tools/json/example.ts`, `src/content/tools/json.md`; готово — `pnpm test:e2e -g "первый экран"`
- [ ] T-099 — развести тексты пустой вставки и пустого поля (US-060, REQ-INPUT-04): границы — `src/ui/ToolForm.astro`, `src/content/empty.ts`; готово — `pnpm test:e2e -g "пустая вставка"`
- [ ] T-100 — написать лендинг JSON Workbench без длинного SEO-текста (US-044, REQ-CONTENT-03): границы — `src/content/tools/json.md`; готово — `pnpm gate:meta && pnpm gate:lh`
- [ ] T-101 — показать счётчик внешних запросов в шапке на всех маршрутах (US-040, ENT-24): границы — `src/egress/counter.ts`, `src/shell/Shell.astro`; готово — `pnpm test:int -t egress-counter`
- [ ] T-102 — снять числа порогов по процедуре D-51 и записать их в реестр (Ф-4, REQ-LIMIT-03): границы — `scripts/bench-thresholds.ts`, `src/registry/thresholds.ts`; готово — `pnpm gate:provisional` зелёный по осям JSON
- [ ] T-103 — добавить запись JSON Workbench в реестр (US-002, ИНВ-07): границы — `src/registry/tools.ts`, `src/registry/ladder.ts`; готово — `pnpm gates`
- [ ] T-104 — снять M-03 двумя раздельными сверками: каркас Э-1 и позиция Э-2 (D-75 п. 3): границы — `docs/estimates.md`; готово — документ содержит два коэффициента, дату и способ переноса на семь позиций
- [ ] T-105 — выпустить тег `v0.2.0` и пройти демонстрацию Э-2: границы — `CHANGELOG.md`; готово — сценарий этапа воспроизведён на проде посторонним человеком

---

## 4. Э-3 — каталог Must и петля

**Готово этапа:** четыре инструмента; токен разобран и подпись проверена локально; уход с маршрута
обнулил ключ и токен, и это показывает гейт; результат уезжает в следующий инструмент одним
действием.

- [ ] T-106 — разобрать JWT на header, payload и claim-ы; поле ключа свёрнуто (US-021 крит. 1): границы — `src/tools/jwt/parse.ts`, `src/tools/jwt/ui.ts`; готово — `pnpm test:unit -t jwt-parse`
- [ ] T-107 — показать `exp`, `iat`, `nbf` и срок действия; «Истёк», отсутствие `exp` — не ошибка (US-021 крит. 2, 3): границы — `src/tools/jwt/claims.ts`, `src/content/tools/jwt.md`; готово — `pnpm test:unit -t jwt-claims`
- [ ] T-108 — показать ошибку разбора обрезанного токена за ≤ 300 мс без очистки ввода (US-021 крит. 4, NFR-15): границы — `src/tools/jwt/parse.ts`; готово — `pnpm test:e2e -g "обрезанный токен"`
- [ ] T-109 — описать контракт JWT: `sensitiveInput = true`, перечень teardown, `continuable = false` (US-021, ИНВ-04, D-48): границы — `src/tools/jwt/contract.ts`; готово — `pnpm gate:persist`: подстрока токена-фикстуры не найдена ни на одной поверхности
- [ ] T-110 — проверить подпись HS256 на Web Crypto: три вердикта и `alg: none` (US-022, D-68, ADR-18): границы — `src/tools/jwt/verify.ts`; готово — `pnpm test:unit -t jwt-verify`
- [ ] T-111 — проверять `crypto.subtle` до показа действия и называть путь при отказе (US-022, D-64, ИНВ-12): границы — `src/tools/jwt/ui.ts`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "незащищённый контекст"`
- [ ] T-112 — замаскировать поле ключа без `type="password"` (US-041, REQ-TRUST-05): границы — `src/ui/SecretField.astro`; готово — `pnpm test:e2e -g "поле ключа"`, менеджер паролей не предлагает сохранение (пункт RUN-17)
- [ ] T-113 — закодировать и раскодировать Base64 текста в обоих алфавитах (US-023, REQ-TOOL-03): границы — `src/tools/base64/codec.ts`; готово — `pnpm test:unit -t base64`
- [ ] T-114 — закодировать файл чанками через воркер (US-024, REQ-LIMIT-05): границы — `src/tools/base64/file.ts`, `src/worker/worker.ts`; готово — `pnpm test:int -t base64-file`; блок: T-056
- [ ] T-115 — объяснить отказ по размеру файла позиционированием, а не извинением (US-024, REQ-LIMIT-04): границы — `src/tools/base64/file.ts`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "файл за порогом"`
- [ ] T-116 — закодировать и раскодировать URL, разобрать query (US-025, REQ-TOOL-04): границы — `src/tools/url/codec.ts`; готово — `pnpm test:unit -t url`
- [ ] T-117 — показать чипсы кодирования на вводе URL (US-025, REQ-INPUT-04): границы — `src/tools/url/ui.ts`, `src/detect/chips.ts`; готово — `pnpm test:e2e -g "чипсы кодирования"`
- [ ] T-118 — сделать «Продолжить с этим»: одно значение через буфер, отдельное событие на шаг (US-012, FL-07): границы — `src/ui/actions.ts`, `src/router/handoff.ts`; готово — `pnpm test:e2e -g "FL-07"`; блок: T-046
- [ ] T-119 — запретить перенос из инструмента с `sensitiveInput` (US-012, D-46 п. 4, ИНВ-04): границы — `src/router/handoff.ts`, `tests/invariants/persistence.spec.ts`; готово — `pnpm gate:persist`
- [ ] T-120 — добавить записи `jwt`, `base64`, `url` в реестр и активировать их ступени (US-002): границы — `src/registry/tools.ts`, `src/registry/ladder.ts`; готово — `pnpm gates`
- [ ] T-121 — написать лендинги трёх позиций (US-044 на каждую позицию): границы — `src/content/tools/jwt.md`, `base64.md`, `url.md`; готово — `pnpm gate:meta`
- [ ] T-122 — добавить фикстуры трёх новых типов в корпус (US-013): границы — `fixtures/corpus/`; готово — `pnpm test:unit -t corpus`
- [ ] T-123 — выпустить тег `v0.3.0` и пройти демонстрацию Э-3: границы — `CHANGELOG.md`; готово — сценарий этапа воспроизведён на проде

---

## 5. Э-4 — вход за два нажатия

**Готово этапа:** Ctrl/Cmd + K из любого места, две буквы, инструмент открыт; `жсон` находит JSON
Workbench; неприехавшая логика объясняет себя текстом. **Жёсткий минимум RISK-06 собран** — в
редакции roadmap §2.5.

- [ ] T-124 — открыть палитру по Ctrl/Cmd + K, кнопке в шапке и `/` (US-014, REQ-ENTRY-01): границы — `src/palette/palette.ts`, `src/shell/Shell.astro`; готово — `pnpm test:e2e -g "FL-08"`
- [ ] T-125 — искать по трём источникам имени и по недавним точным совпадением подстроки (US-014, backlog §4.2 п. 7): границы — `src/palette/search.ts`, `src/registry/derive.ts`; готово — `pnpm test:unit -t palette-search`
- [ ] T-126 — нормализовать раскладку: `жсон` находит JSON Workbench (US-014 крит. 8): границы — `src/palette/layout-map.ts`; готово — `pnpm test:unit -t layout-map`
- [ ] T-127 — уложить вход в два нажатия и ≤ 300 мс до открытого инструмента (US-014, NFR-05, CB-05): границы — `src/palette/palette.ts`, `src/router/loadTool.ts`; готово — `pnpm test:e2e -g "два нажатия"`
- [ ] T-128 — уложить палитру в бюджет initial JS или вынести чанком с предзагрузкой на `idle` (US-014, NFR-03, РР-01): границы — `.size-limit.json`, `src/palette/index.ts`; готово — `pnpm gate:size`
- [ ] T-129 — показать сообщение, когда палитра не нашла ни одной позиции (US-063, REQ-LIMIT-04): границы — `src/palette/empty.ts`, `src/content/empty.ts`; готово — `pnpm test:e2e -g "палитра пуста"`
- [ ] T-130 — развести четыре состояния недоступной логики разными текстами и отправить по каждому `result_failed` (US-067, D-63, D-133): границы — `src/shell/toolSlot.ts`, `src/content/errors.ts`, `src/analytics/send.ts`; готово — `pnpm test:e2e -g "четыре состояния"`, в перехвате запроса причина `tool_unavailable`, а на маршруте вне реестра — `tool_not_found`
- [ ] T-131 — включить порог G-11 ≥ 60 % сценариев без мыши (NFR-08, D-75): границы — `scripts/gates/no-mouse.ts`, `tests/e2e/no-mouse/`; готово — `pnpm gate:nomouse` красный ниже 60 %
- [ ] T-132 — сделать палитру доступной: ловушка фокуса, Esc, объявление числа результатов (US-014, NFR-13): границы — `src/palette/a11y.ts`; готово — `pnpm gate:a11y`
- [ ] T-207 — завести корневой маршрут `/` записью реестра: оболочка с открытой палитрой, цель `start_url` (US-014, D-91, D-82 п. 1): границы — `src/registry/pages.ts`, `src/pages/index.astro`; готово — `curl https://dokey.ru/` отдаёт оболочку, `pnpm test:e2e -t root-palette`: вход из PWA открывает палитру
- [ ] T-133 — выпустить тег `v0.4.0`, зафиксировать сбор жёсткого минимума: границы — `CHANGELOG.md`, `docs/release-checklist.md`; готово — сценарий этапа воспроизведён; состав жёсткого минимума записан в редакции roadmap §2.5

---

## 6. Э-5 — объявленные пороги

**Готово этапа:** число порога напечатано в интерфейсе и совпадает с реестром; отказ объяснён
позиционированием; сборка с `provisional` не выпускается.

- [ ] T-134 — показать мягкий порог: предупреждение с числом и действием «Обработать» (US-033, REQ-LIMIT-03): границы — `src/ui/Threshold.astro`, `src/registry/thresholds.ts`; готово — `pnpm test:e2e -g "мягкий порог"`
- [ ] T-135 — показать жёсткий порог: отказ, объяснённый позиционированием (US-034, REQ-METR-02): границы — `src/ui/Threshold.astro`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "жёсткий порог"`
- [ ] T-136 — проверить арифметику порогов: `hard > soft`, ось, класс платформы, флаг `provisional` (US-033, US-034, ENT-22): границы — `src/registry/thresholds.ts`; готово — `pnpm test:unit -t thresholds`
- [ ] T-137 — показать прогресс и отмену: после «Отменить» область возвращается к состоянию до запуска (US-035, D-71): границы — `src/worker/client.ts`, `src/ui/Progress.astro`; готово — `pnpm test:int -t cancel`
- [ ] T-138 — прогнать инвариант «молчаливого отказа нет» по всем маршрутам реестра (US-035, REQ-LIMIT-04): границы — `tests/invariants/no-silent-failure.spec.ts`; готово — `pnpm test:e2e -g "инвариант отказа"`
- [ ] T-139 — сгенерировать большие вводы из зерна по числу порога (D-110): границы — `scripts/gen-large-input.ts`, `tests/e2e/thresholds.spec.ts`; готово — `pnpm test:e2e -g "порог"` без единого мегабайта в репозитории
- [ ] T-140 — прогнать матрицу браузеров: три движка и мобильные профили (TECH-08, NFR-14, RUN-04): границы — `playwright.config.ts`, `.github/workflows/matrix.yml`; готово — `pnpm test:matrix`
- [ ] T-221 — определять класс платформы по `(pointer: coarse)` без чтения UA (US-034, ENT-22, D-161): границы — `src/shell/platform.ts`; готово — `pnpm test:e2e -g "класс платформы"`: профили Pixel и iPhone дают `mobile`, десктоп — `desktop`
- [ ] T-141 — задать класс порогов `mobile` по результатам матрицы (D-51, D-161): границы — `src/registry/thresholds.ts`; готово — `pnpm gate:provisional`; блок: T-140
- [ ] T-142 — выпустить тег `v0.5.0` и пройти демонстрацию Э-5: границы — `CHANGELOG.md`; готово — числа порогов на странице совпадают с реестром

---

## 7. Э-6 — офлайн и входы снаружи

**Готово этапа:** выключенная сеть — любой инструмент каталога работает, в шапке «Офлайн», событие
не ушло и не скопилось; новая сборка предлагает «Обновить» строкой.
**Единственный этап без дешёвого отката** (implementation-plan §4.3).

- [ ] T-143 — собрать service worker на `injectManifest`: precache оболочки и ядра (US-004, ADR-09): границы — `src/sw/sw.ts`, `astro.config.mjs`; готово — `pnpm test:e2e -g "офлайн оболочка"`
- [ ] T-144 — докачивать чанки всего каталога после первой отрисовки; редактор — только runtime-кеш (US-004, D-63, NFR-18): границы — `src/sw/sw.ts`, `.size-limit.json`; готово — `pnpm gate:size`: precache ≤ 120 KB gzip
- [ ] T-145 — версионировать кеш идентификатором сборки и чистить прошлые на `activate` (US-004, ИНВ-10): границы — `src/sw/sw.ts`; готово — `pnpm test:e2e -g "смена версии"`
- [ ] T-146 — показать тихую строку «Вышла новая версия» и вызывать `skipWaiting` только по действию (D-67, ИНВ-10): границы — `src/sw/update.ts`, `src/shell/Shell.astro`; готово — `pnpm test:e2e -g "обновление"`
- [ ] T-147 — оставлять файлы прошлых сборок на 30 дней правилом деплоя (D-67 п. 1): границы — `.github/workflows/release.yml`, `wrangler.toml`; готово — чанк предыдущего тега доступен после нового деплоя
- [ ] T-148 — показать статус «Офлайн» и не отправлять события офлайн (US-058, REQ-CORE-09, D-44): границы — `src/shell/network.ts`, `src/analytics/send.ts`; готово — `pnpm test:int -t offline`
- [ ] T-149 — отработать первый визит без кеша, в том числе без сети (US-061, REQ-CORE-02): границы — `tests/e2e/fl-01.spec.ts`, `src/content/errors.ts`; готово — `pnpm test:e2e -g "первый визит"`
- [ ] T-150 — отработать первый запуск из установленного PWA (US-062, REQ-ENTRY-02): границы — `tests/e2e/fl-14.spec.ts`; готово — `pnpm test:e2e -g "запуск из PWA"`
- [ ] T-151 — сгенерировать манифест PWA из реестра (US-015, ИНВ-06): границы — `src/pages/manifest.webmanifest.ts`; готово — `pnpm build`, манифест содержит ровно маршруты реестра
- [ ] T-152 — обеспечить установимость и запуск из иконки (US-015, REQ-ENTRY-04): границы — `src/pages/manifest.webmanifest.ts`, `public/icons/`; готово — установка проходит в Chrome и Safari (пункт RUN-17)
- [ ] T-153 — сгенерировать `opensearch.xml` из реестра и объявить его в разметке (US-016, D-70): границы — `src/pages/opensearch.xml.ts`, `src/layouts/Base.astro`; готово — браузер принимает описание в четырёх движках эксперимента D-70
- [ ] T-154 — написать страницу «Держать под рукой» об обоих механизмах (US-017, REQ-ENTRY-04): границы — `src/content/pages/keep-handy.md`, `src/registry/tools.ts`; готово — `pnpm gate:sitemap && pnpm gate:words`; блок: T-152, T-153
- [ ] T-155 — прогнать инвариант офлайна и четырёх состояний по всем маршрутам (RUN-13, D-63): границы — `tests/invariants/offline.spec.ts`; готово — `pnpm test:e2e -g "инвариант офлайна"`
- [ ] T-156 — поднять staging `stage.dokey.ru` с профилем без аналитики, `noindex` и `Disallow: /` (D-111): границы — `wrangler.toml`, `.github/workflows/pr.yml`; готово — сборка PR по метке открывается по адресу staging, `noindex` на месте, событий в Umami ноль
- [ ] T-157 — выпустить тег `v0.6.0` и пройти демонстрацию Э-6: границы — `CHANGELOG.md`; готово — сценарий этапа воспроизведён, включая обновление с предыдущего тега на проде

---

## 8. Э-7 — каталог 8 из 8

**Готово этапа:** M-02 — шесть инструментов, палитра, определитель; единая форма принята на всех
восьми позициях одним прогоном.
**Первый кандидат на демонтаж** при срабатывании RISK-06 после Э-11.

- [ ] T-158 — конвертировать время: секунды и миллисекунды, ISO 8601, UTC и локальное (US-026, ADR-18): границы — `src/tools/unixtime/convert.ts`; готово — `pnpm test:unit -t unixtime`
- [ ] T-159 — добавить ступень лестницы для timestamp и фикстуры к ней (US-026, US-013): границы — `src/registry/ladder.ts`, `fixtures/corpus/`; готово — `pnpm test:unit -t corpus && pnpm gate:ladder`
- [ ] T-160 — сгенерировать UUID v4, v7 и ULID импортом по функциям (US-027, ADR-18): границы — `src/tools/uuid/generate.ts`; готово — `pnpm test:unit -t uuid && pnpm gate:size`
- [ ] T-161 — разобрать и проверить существующее значение UUID и ULID (US-028): границы — `src/tools/uuid/parse.ts`; готово — `pnpm test:unit -t uuid-parse`
- [ ] T-162 — сделать экран генератора, где ввода не нужно (US-066, REQ-TOOL-07): границы — `src/tools/uuid/ui.ts`, `src/ui/ToolForm.astro`; готово — `pnpm test:e2e -g "FL-12"`
- [ ] T-163 — добавить записи `unixtime` и `uuid` в реестр с лендингами (US-002, US-044): границы — `src/registry/tools.ts`, `src/content/tools/unixtime.md`, `uuid.md`; готово — `pnpm gates`
- [ ] T-164 — принять единую форму инвариантным прогоном по восьми позициям (US-029, REQ-TOOL-07): границы — `tests/invariants/tool-form.spec.ts`; готово — `pnpm test:e2e -g "единая форма"`
- [ ] T-165 — свести бюджет чанков каталога после шести позиций (NFR-18, РР-03): границы — `.size-limit.json`; готово — `pnpm gate:size`: сумма чанков в precache ≤ 120 KB gzip
- [ ] T-166 — выпустить тег `v0.7.0` и зафиксировать M-02 (8 из 8): границы — `CHANGELOG.md`, `docs/release-checklist.md`; готово — контракт M-02 отмечен с перечислением восьми позиций

---

## 9. Э-8 — доверие и контент

**Готово этапа:** страница бенчмарка с методикой, скриптом и проигранными замерами; чужой человек
повторяет замер по инструкции.

- [ ] T-167 — написать страницу «Как это проверить»: процедура, пустой список служебных ключей (D-124), процедура сверки манифеста хешей прода (D-123), оператор и контакт субъекта (D-125) (US-039, D-71): границы — `src/content/pages/how-to-verify.md`, `src/registry/tools.ts`; готово — `pnpm gate:sitemap && pnpm gate:words`
- [ ] T-168 — не пускать секреты в события, тексты ошибок и фикстуры (US-042, REQ-TRUST-06): границы — `scripts/gates/egress.ts`, `src/content/errors.ts`; готово — `pnpm gate:egress` с положительной проверкой по подстроке фикстуры
- [ ] T-169 — описать методику бенчмарка TTR и сделать её воспроизводимой (US-043, REQ-CONTENT-01): границы — `scripts/bench-ttr.ts`, `docs/bench/method.md`; готово — `pnpm bench:ttr` дважды с расхождением < 10 %; блок: T-086
- [ ] T-170 — снять замеры против пяти площадок и сохранить результат как есть (US-043, D-19 п. 5): границы — `docs/bench/results.json`, `scripts/bench-ttr.ts`; готово — `pnpm bench:compare`, проигранные замеры в файле не отредактированы
- [ ] T-171 — собрать страницу бенчмарка: числа, методика, дата, оговорки (US-043): границы — `src/content/pages/benchmark.md`, `src/registry/tools.ts`; готово — `pnpm gate:meta && pnpm gate:words`
- [ ] T-172 — написать страницу «Развёртывание в своей сети», сверенную с реальным образом (US-045, REQ-DIST-02): границы — `src/content/pages/self-hosting.md`; готово — команды со страницы выполняются как есть на чистой машине, страница говорит, что аналитики в образе нет намеренно и `/s/*` отвечает `404` (D-166); блок: T-023
- [ ] T-173 — написать раздел об отсутствии обязательств и пополнить список слов-маркеров (US-046, CB-08): границы — `src/content/pages/no-commitments.md`, `scripts/gates/marker-words.list.txt`; готово — `pnpm gate:words`
- [ ] T-174 — выпустить тег `v0.8.0` и зафиксировать M-07: границы — `CHANGELOG.md`; готово — страница бенчмарка открыта на проде вместе с методикой и скриптом

---

## 10. Э-9 — профиль selfhosted и контракт

**Готово этапа:** в артефакте профиля `selfhosted` отправителя событий физически нет; шаблон issue
принимает предложение инструмента по семи пунктам контракта.

- [ ] T-175 — собрать профиль `selfhosted` без аналитики физически (US-049, ENT-26, ИНВ-11): границы — `astro.config.mjs`, `src/analytics/index.ts`, `.github/workflows/release.yml`; готово — поиск строки `/s/event` в артефакте профиля не даёт совпадений
- [ ] T-222 — исключить `sitemap.xml` из профиля `selfhosted` и научить G-07 профилю (US-049, ENT-26, ИНВ-06, D-163): границы — `src/pages/sitemap.xml.ts`, `scripts/gates/sitemap.ts`, `tests/gates/negative/sitemap.spec.ts`; готово — `pnpm gate:sitemap` зелёный в обоих профилях, подсаженный sitemap в `selfhosted` — красный
- [ ] T-176 — завести гейт сборки образа: профиль, отсутствие аналитики, ноль сетевых обращений (US-049, NFR-16, D-166): границы — `scripts/gates/selfhosted.ts`, `tests/system/image.spec.ts`; готово — `pnpm test:image`, `GET` и `POST /s/event` к образу — `404`
- [ ] T-177 — оформить контракт инструмента как документ и шаблон issue (US-050, D-21): границы — `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/tool-proposal.yml`; готово — шаблон содержит семь пунктов контракта, включая `chunk-budget`
- [ ] T-178 — завести расписание ежемесячной пересборки с тремя предохранителями и записью исхода коммитом (US-051, D-59, D-112): границы — `.github/workflows/rebuild.yml`; готово — ручной запуск проходит, провал заводит issue, три провала отключают расписание
- [ ] T-179 — публиковать дату последней успешной пересборки, а не периодичность (US-051, CB-08): границы — `README.md`, `src/content/pages/self-hosting.md`; готово — `pnpm gate:words`
- [ ] T-180 — принять US-048 на релизном образе: подпись, provenance, SBOM, инструкция (RUN-15): границы — `tests/system/supply-chain.spec.ts`; готово — `pnpm verify:supply`
- [ ] T-181 — выпустить тег `v0.9.0` и пройти демонстрацию Э-9: границы — `CHANGELOG.md`; готово — образ поднят в изолированном контуре по странице развёртывания

---

## 11. Э-10 — инструментовка

**Готово этапа:** в Umami видна доля визитов с результатом и категории отказа; перехват сети
показывает ровно семь полей; при DNT отправлено ноль событий.

- [ ] T-182 — описать закрытый список полей события и схему (US-055, ENT-29, ИНВ-05): границы — `src/analytics/schema.ts`; готово — `pnpm test:unit -t event-schema`
- [ ] T-183 — включить G-05 в боевом виде: множество полей ровно равно списку (US-055, REQ-METR-04): границы — `scripts/gates/fields.ts`, `tests/gates/negative/fields.spec.ts`; готово — `pnpm gate:fields` красный на восьмом поле
- [ ] T-184 — отправлять событие с таймаутом ≤ 1 с, нулём повторов и молчаливой деградацией (NFR-11): границы — `src/analytics/send.ts`; готово — `pnpm test:int -t analytics-degrade`: 429, 502 и обрыв дают 0 видимых ошибок, запрос несёт `Content-Type: application/json` (D-165)
- [ ] T-185 — отправлять «результат получен» с `ttr` и `toolId` (US-052, EVT-01): границы — `src/analytics/events.ts`, `src/ui/ToolForm.astro`; готово — `pnpm test:int -t evt-success`
- [ ] T-186 — отправлять «результат не получен» с категорией причины (US-053, REQ-METR-02): границы — `src/analytics/events.ts`, `src/content/errors.ts`; готово — `pnpm test:int -t evt-failure`
- [ ] T-187 — отправлять событие ручного переключения чипса (US-054, REQ-METR-03): границы — `src/detect/chips.ts`, `src/analytics/events.ts`; готово — `pnpm test:int -t evt-chip`
- [ ] T-188 — отправлять событие открытия инструмента с источником входа (US-052, REQ-METR-08, D-74): границы — `src/router/router.ts`, `src/analytics/events.ts`; готово — `pnpm test:int -t evt-open`
- [ ] T-189 — уважать DoNotTrack и Global Privacy Control (US-056, REQ-METR-05): границы — `src/analytics/send.ts`; готово — `pnpm test:int -t dnt`: 0 событий, 0 мс прироста TTR
- [ ] T-190 — не отправлять и не копить события офлайн (US-058, D-44): границы — `src/analytics/send.ts`, `src/shell/network.ts`; готово — `pnpm test:int -t offline-events`: ни очереди, ни счётчиков
- [ ] T-191 — вывести полевой TTR одним несегментированным числом (US-057, REQ-METR-06): границы — `infra/vps/umami-queries.sql`, `docs/metrics.md`; готово — запрос возвращает одно число на тестовом наборе событий
- [ ] T-208 — отправлять событие входа с маркером источника (US-068, REQ-METR-09, D-99): границы — `src/analytics/events.ts`, `src/shell/AppShell.astro`; готово — `pnpm test:int -t evt-entry`: маркер в `url`, новых полей нет; при DNT, GPC и офлайн событие не отправлено
- [ ] T-192 — выпустить тег `v0.10.0` и пройти демонстрацию Э-10: границы — `CHANGELOG.md`; готово — события видны в Umami, перехват сети показывает ровно семь полей

---

## 12. Э-11 — Could

**Демонтируется первым** при срабатывании плана реакции RISK-06 — целиком, до Э-7.

- [ ] T-193 — подключить CodeMirror 6 тонкой сборкой в JSON Workbench (US-030, ADR-07): границы — `src/editor/editor.ts`, `src/tools/json/ui.ts`; готово — `pnpm gate:size`: чанк редактора ≤ 90 KB gzip и вне precache
- [ ] T-194 — подменить textarea редактором после первой отрисовки, не двигая каретку (US-030, FL-06 A3): границы — `src/editor/mount.ts`; готово — `pnpm test:e2e -g "FL-06 A3"`
- [ ] T-195 — читать файлы чанками и завести отдельный порог хэширования (US-036, REQ-LIMIT-05): границы — `src/tools/base64/file.ts`, `src/registry/thresholds.ts`; готово — `pnpm test:int -t chunked-read`
- [ ] T-196 — показать одноразовую подсказку об активации ярлыка один раз за сессию вкладки, без записи на устройство (US-018, D-71, D-124): границы — `src/shell/hint.ts`, `src/content/pages/how-to-verify.md`; готово — `pnpm gate:persist` зелёный при нуле служебных ключей: подсказка показывается один раз за сессию вкладки и в `localStorage` не пишется ничего
- [ ] T-197 — не показывать подсказку на первом визите (US-065, REQ-CONTENT-02): границы — `src/shell/hint.ts`; готово — `pnpm test:e2e -g "первый визит без подсказки"`
- [ ] T-198 — выпустить тег `v0.11.0` и пройти демонстрацию Э-11: границы — `CHANGELOG.md`; готово — редактор и чанковое чтение работают на проде

---

## 13. Релизная неделя

**Готово:** M-01 — три артефакта; двенадцать пунктов критерия готовности roadmap §2.4 отмечены с
доказательством.

- [ ] T-199 — составить список площадок обоих языков с разделением «с порогом» и «без порога» (M-12, D-77 п. 2; **срок 2026-11-16**): границы — `docs/channel/listings.md`; готово — каждая строка несёт критерий D-36 и отметку о пороге звёзд
- [ ] T-200 — пройти ручной релизный чек-лист (RUN-17: NFR-11, 12, 14, 15, 16, скринридер, PWA, ярлык, менеджер паролей): границы — `docs/release-checklist.md`; готово — чек-лист заполнен и приложен к тегу
- [ ] T-201 — открыть индексацию: переключить признак индексации сборки, из которого выводятся `noindex` и `robots.txt` (обратная к T-012, D-164): границы — `astro.config.mjs`; готово — `curl https://dokey.ru/robots.txt` отдаёт `Disallow: /s/` и `Sitemap:`, `noindex` на страницах нет
- [ ] T-202 — отметить двенадцать пунктов критерия готовности релиза (roadmap §2.4, D-145): границы — `docs/release-checklist.md`; готово — каждый пункт имеет ссылку на прогон или артефакт, а не отметку словом
- [ ] T-203 — выпустить тег `v1.0.0` и предъявить три артефакта (M-01): границы — `CHANGELOG.md`; готово — прод на `dokey.ru`, git-тег, подписанный образ в GHCR
- [ ] T-204 — подать заявки в накопительные слои 2 и 3 (TECH-14, D-36): границы — `docs/channel/listings.md`; готово — по каждой строке проставлена дата подачи
- [ ] T-205 — опубликовать Show HN углом бенчмарка, один раз, после накопительных слоёв (TECH-14, D-36): границы — `docs/channel/listings.md`; готово — ссылка на публикацию записана
- [ ] T-206 — проверить выживание записей через семь дней (M-13): границы — `docs/channel/listings.md`; готово — столбец «на месте через 7 дней» заполнен по каждой поданной строке

---

## 14. Сводка

### 14.1. Задачи по этапам

| Этап | Задачи | Истории | Работа без истории |
|---|---|---|---|
| Э-0 | T-001…T-032, T-209 (33) | 1 (US-048, инфраструктура) | TECH-01…05, 09, 10, 12, 13, 15 |
| Э-1 | T-033…T-072 (40) | 12 | двенадцать гейтов, доступность |
| Э-2 | T-073…T-105 (33) | 12 | TECH-07, TECH-11, замер D-51, сверки M-03 |
| Э-3 | T-106…T-123 (18) | 7 | — |
| Э-4 | T-124…T-133, T-207 (11) | 3 | порог G-11, корневой маршрут (D-91) |
| Э-5 | T-134…T-142 (9) | 3 | TECH-08, генератор вводов |
| Э-6 | T-143…T-157 (15) | 6 | staging |
| Э-7 | T-158…T-166 (9) | 4 | приёмка US-029 |
| Э-8 | T-167…T-174 (8) | 5 | — |
| Э-9 | T-175…T-181 (7) | 3 | приёмка US-048 |
| Э-10 | T-182…T-192, T-208 (12) | 8 | — |
| Э-11 | T-193…T-198 (6) | 4 | — |
| Релиз | T-199…T-206 (8) | — | M-12, TECH-14, RUN-17 |
| **Итого** | **209** | **68** | **пятнадцать TECH-задач** |

### 14.2. История → задачи

| US | Задачи | US | Задачи |
|---|---|---|---|
| US-001 | T-041…T-044, T-219 | US-035 | T-137, T-138 |
| US-002 | T-033…T-040, T-103, T-120, T-163, T-217 | US-036 | T-195 |
| US-003 | T-045…T-047 | US-037 | T-050, T-051 |
| US-004 | T-143…T-145, T-155, T-165 | US-038 | T-048, T-049 |
| US-005 | T-052, T-053 | US-039 | T-167 |
| US-006 | T-054 | US-040 | T-101 |
| US-007 | T-055 | US-041 | T-112 |
| US-008 | T-077…T-080 | US-042 | T-168 |
| US-009 | T-081, T-082 | US-043 | T-169…T-171 |
| US-010 | T-083 | US-044 | T-100, T-121, T-163 |
| US-011 | T-084, T-085 | US-045 | T-172 |
| US-012 | T-118, T-119 | US-046 | T-173 |
| US-013 | T-073…T-076, T-122, T-159, T-220 | US-047 | T-013…T-019, T-067…T-071 |
| US-014 | T-124…T-128, T-132 | US-048 | T-020…T-025, T-180 |
| US-015 | T-151, T-152 | US-049 | T-175, T-176, T-222 |
| US-016 | T-153 | US-050 | T-004, T-177 |
| US-017 | T-154 | US-051 | T-178, T-179 |
| US-018 | T-196 | US-052 | T-185, T-188 |
| US-019 | T-087…T-092 | US-053 | T-186 |
| US-020 | T-093…T-096 | US-054 | T-187 |
| US-021 | T-106…T-109 | US-055 | T-182, T-183 |
| US-022 | T-110, T-111 | US-056 | T-189 |
| US-023 | T-113 | US-057 | T-191 |
| US-024 | T-114, T-115 | US-058 | T-148, T-190 |
| US-025 | T-116, T-117 | US-059 | T-098 |
| US-026 | T-158, T-159 | US-060 | T-099 |
| US-027 | T-160 | US-061 | T-028, T-149 |
| US-028 | T-161 | US-062 | T-150 |
| US-029 | T-059…T-062, T-164 | US-063 | T-129 |
| US-030 | T-193, T-194 | US-064 | T-097 |
| US-031 | T-056, T-057 | US-065 | T-197 |
| US-032 | T-058 | US-066 | T-162 |
| US-033 | T-134, T-136 | US-067 | T-053, T-130 |
| US-034 | T-135, T-136, T-221 | | |

**Проверка:** 68 историй, ни одной без задачи; тринадцать задач служат больше чем одной истории и
перечислены в обеих строках — это места, где чекбокс истории закрывается не своим этапом
(US-002, US-013, US-044, US-047, US-048, US-058, US-067).

### 14.3. Задачи, заблокированные вопросом

Задач с пометкой `⚠ Q-NN` не осталось: последняя, T-239, разблокирована решением D-182 (2026-09-22); до неё — T-011 решениями D-178 и D-179 (2026-09-17).

| Задача | Вопрос | Этап |
|---|---|---|
| ~~T-002~~ | ~~Q-233~~ — закрыт **D-155** 2026-09-10: организация `dokey-app`, логин `dokey` занят | Э-0 |
| ~~T-202~~ | ~~Q-214~~ — закрыт **D-145** 2026-09-10: критерий требует G-01…G-14 без исключений | Релиз |

---

## 15. Журнал

Строка на закрытую задачу: дата, номер, коммит, что стало правдой. Пишется тем же коммитом,
что и отметка `- [x]`.

- 2026-09-08 T-003 `2edc5e8` — LICENSE Apache-2.0, NOTICE, CONTRIBUTING, CODE_OF_CONDUCT и SECURITY лежат в корне
- 2026-09-08 T-004 `2edc5e8` — шаблоны issue и PR повторяют семь пунктов контракта D-21
- 2026-09-08 T-005 `2edc5e8` — каркас Astro 7.3.1 даёт статический каталог; клиентского JS в сборке ноль байт
- 2026-09-08 T-006 `2edc5e8` — дерево каталогов повторяет КМП-01…КМП-15; назначение каждого — в src/README.md
- 2026-09-08 T-007 `2edc5e8` — oxlint 1.81.0 и Prettier 3.9.6 проходят по всему репозиторию без замечаний
- 2026-09-08 T-008 `2edc5e8` — Vitest 5.0.0 двумя проектами; RUN-02 идёт в браузере, а не в jsdom
- 2026-09-08 T-009 `2edc5e8` — Playwright 1.63.0, шесть сценариев на заглушке, сервер гасится после прогона
- 2026-09-08 T-010 `2edc5e8` — заголовки ответа — один источник infra/headers/_headers; check:headers их печатает
- 2026-09-08 T-017 `2edc5e8` — Lighthouse CI на запиннутом пресете, утверждения по медиане прогонов
- 2026-09-08 T-018 `2edc5e8` — четырнадцать адресов гейтов; pnpm gates зелёный и различает «проверено» и «пусто»
- 2026-09-08 T-020 `2edc5e8` — образ multi-stage на nginx 1.30.4-alpine поднимается non-root и отдаёт заглушку
- 2026-09-10 T-209 `63ab142` — правки решений D-115…D-150 доведены по документам: ENT-29 семью полями, шесть категорий отказа, пять имён событий, PD-05/PD-06/PD-08 сняты, три списка на SCR-10
- 2026-09-15 T-016 `e83245f` — G-02 меряет initial JS графом от каждого HTML сборки через es-module-lexer (D-88, D-156, D-158), шрифты критического пути — три файла D-157; негативные сборки — T-019
- 2026-09-18 T-235 `e6d7584` — NEL Cloudflare в зоне dokey.ru выключен (D-180); RUN-16 падает на `nel`, `report-to` и `reporting-endpoints`, на проде зелёный
- 2026-09-20 T-241 `9df8c97` — случаи `tests/unit` с дочерним процессом уложены в таймаут Vitest: работа один раз на файл и параллельно, `spawnSync` заменён асинхронным `execFile`; 51 из 51 зелёный при занятых ядрах
- 2026-09-22 T-238 `c68ec4f` — `docs-structure.test.ts` читает `docs/**/*.md` один раз на файл и параллельно; вместе с T-241 `pnpm test:unit` зелёный три прогона подряд
- 2026-09-22 T-234 `754fd5a` — частные правила `_headers` отсоединяют `Cache-Control` из `/*`, гейт ловит склейку; прод проверяет T-237 после `v0.0.1`
- 2026-09-22 T-224 `741a70a` — таблицы `src/README.md` и `scripts/gates/README.md` пересчитаны `prettier --write`, содержимое ячеек не менялось; Prettier по `git ls-files` зелёный
- 2026-09-22 T-240 `fb7d907` — `gate:headers-parity` без правила `/*` в `_headers` даёт `fail` «склейку частных правил проверить не с чем», а не «чисто»; на текущем файле зелёный
- 2026-09-22 T-242 `8ce2233` — `gate:headers-parity` берёт сверяемые заголовки только из правила `/*`: заданный или снятый (`! Name`) вне `/*` даёт `fail`; на текущем файле зелёный; nginx-сторону — T-243
- 2026-09-22 T-243 `b6922c0` — `gate:headers-parity` берёт сверяемые `add_header` nginx только с уровня server: заголовок вне server и любой `add_header` во вложенном `location`/`if` дают `fail`; на текущем файле зелёный
- 2026-09-22 T-228 `4c822bf` — RUN-03 больше не зависает на раннере: `astro preview` запускается с `--background` явно и с потолком 60 с у `spawnSync`, teardown берёт тот же вызов, у job `e2e` `timeout-minutes: 10`; прогон PR — 6 passed за 40 с
- 2026-09-22 T-244 `305036d` — у всех шести job'ов `pr.yml` есть `timeout-minutes` (5–10 мин, запас 5,6–11x к худшему зелёному прогону из сорока); RUN-01 валится на job без потолка и на нераспознанном ключе job
- 2026-09-23 T-249 `0d54c2a` — у `gates` и `image` `release.yml` есть `timeout-minutes` (15 и 20 мин, обоснование у каждого), а RUN-01 проверяет потолки по всему каталогу `.github/workflows`, а не по одному файлу
- 2026-09-23 T-250 `1a32ee6` — пустое значение `timeout-minutes` во всех семи формах (`null`, `Null`, `NULL`, `~`, `''`, `""`, ничего) даёт «нет timeout-minutes»: потолок — только десятичное число или `${{ … }}`, расхождения с js-yaml 4.3.2 держатся в сторону ложной красноты
- 2026-09-23 T-251 `33b521c` — у `rebuild` и `probe` есть `timeout-minutes` (5 и 10 мин по составу шагов: зелёного прогона нет ни у одного из двух workflow), список исключений в RUN-01 удалён целиком, а тревога синтетики переживает отмену job по потолку — `if: ${{ failure() || cancelled() }}` и свой потолок 2 мин у шага сверки; заодно записан факт «`synthetic.yml` в GitHub `disabled_manually`, прод не наблюдается ничем» в T-031
- 2026-09-23 T-252 `e2fc583` — потолком засчитывается только десятичное целое без знака (и `${{ … }}`): дробное `5.5`, `0.5`, `5.0`, `"5.5"`, `.5`, `5.` и знаковое или экзотическое `+5`, `5e1`, `0x10`, `0o12` дают «нет timeout-minutes», потому что документация GitHub о `timeout-minutes` требует «positive integer», а `0.5` у настоящего преобразователя обернулось бы потолком `0`; на четырёх workflow проверка зелёная
- 2026-09-23 T-254 `ce6255a` — потолком засчитывается только положительное десятичное целое без ведущих нулей (и `${{ … }}`): `0`, `00`, `000`, `"0"`, `'0'`, а также `05` и `007` дают «нет timeout-minutes», потому что документация GitHub требует «positive integer», а реализация того же ключа у шага при нуле потолка не ставит вовсе (`actions/runner`, `StepsRunner.cs`: `if (timeoutMinutes > 0)`); неподтверждённое утверждение комментария о немедленном обрыве снято, расхождения с js-yaml 4.3.2 остались только в сторону ложной красноты, на четырёх workflow проверка зелёная
- 2026-09-23 T-253 `187b940` — предмет `pnpm format:check` и `pnpm format` — список git (`ls-files --cached --others --exclude-standard`) в `scripts/format.ts`, а не обход рабочего каталога: локально исключённое (`.git/info/exclude`) больше не красит чистое дерево, новый файл до `git add` уже проверяется, а пустой список и отсутствие git дают сбой, а не зелёное; набор файлов в CI после checkout прежний (341 = `git ls-files`), состав шагов CI не менялся, класс держит RUN-01 `tests/unit/format-subject.test.ts`
- 2026-09-23 T-229 `d9e90b5` — три прогона Lighthouse собираются одним `lhci collect`: отчёты маршрута попадают в одну группу `finalUrl` (случайный порт сервера сбора больше не дробит её), и медиана `lhci assert` считается по всем трём; полнота набора сверяется с планом маршрутов из того же источника, что и адреса сбора; пять перезапусков job «Сборка и гейты» на `132b01e` дали один вердикт — «отчётов 6: маршрутов 2 × прогонов 3»
- 2026-09-23 T-255 `a13b68e` — G-01 считает не отчёты, а значения аудита в них: список сверяемых утверждений уровня `error` выведен из `ci.assert.assertions` правилами самого `@lhci` (`kebabCase`, разрез по `.`/`:`, выбор величин `getStandardAssertionResults`, умолчание `minScore: 0.9`, `notApplicable`→1, `informative`→0), значения считаются по первичным `lhr-*.json`, а не по `assertion-results.json` с одними провалами, и прогон, где аудит дал число не во всех отчётах маршрута, валит гейт с именем утверждения и числом значений; непрочитаемая конфигурация (`preset`, `assertMatrix`, `resource-summary` и соседи) и пустой список error-утверждений тоже валят; на текущей сборке «отчётов 6: маршрутов 2 × прогонов 3; сверяемых утверждений 4, значений у каждого 3»
- 2026-09-22 T-239 `bf93690` — api-spec §4.0 и §4.6 называют оба префикса ЭП-06: `/_astro/{name}.{hash}.{ext}` и `/fonts/{семейство}-{начертание}.{hash}.woff2` (D-181); `/assets/` в файле не осталось, спрайт — инлайновый, `openapi.yaml` перегенерирует T-245 (D-182)
- 2026-09-23 T-013 `792a12b` — запрет SEC-21 держит oxlint: в `src/**` краснеют `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `srcdoc` (и на чтении, не только на присваивании) и `document.write`/`document.writeln`, привязанные к объекту `document` ради REQ-INPUT-07 — `navigator.clipboard.writeText` и `.write` не задеты (D-64); вне `src/**` запрет не действует, состав шести job’ов `pr.yml` не тронут, в шапку вписан датированный факт «на 2026-09-23 `main` не защищён» (снимает T-014); мимо правила проходят `setAttribute` (T-258), `Object.assign`, вычисляемый доступ, `Reflect.set`, псевдоним `document`, `eval` и `set:html` (T-256)
- 2026-09-23 T-019 `4a05e78` — негативный прогон `test:gates:negative`: третий проект Vitest, стенд поднимает гейт целиком отдельным процессом в каталоге фикстуры и читает приговор по маркеру `__GATE__` — засчитывается только исход `fail` **вместе** с кодом 1, потому что ненулевым кодом кончается и падение процесса; одиннадцать нарушающих сборок — превышение бюджета initial JS, путь бюджета без файла при нашедших соседях, шесть форм спецификатора, не разрешающегося в файл сборки, повтор `title`, повтор `description` и пустота обоих операндов в трёх формах каждая; прогон идёт седьмым job `pr.yml` с потолком 5 мин; покрытие по `GATES` пока не самопроверяется — T-261, случай G-01 — T-260, RUN-16 — T-259
- 2026-09-23 T-259 `bec63e9` — негативный случай RUN-16: обе копии политики печатаются из одной таблицы заголовков в теле набора, а не снимаются с настоящих `infra/headers/_headers` и `infra/docker/nginx.conf` — набор судит гейт, а не сегодняшнюю политику; сторожевой случай доказывает, что неизменённая пара даёт `pass` вместе с кодом 0, и пять нарушающих пар красят гейт исходом `fail` вместе с кодом 1: HSTS пропал из `_headers`, стоит в частном правиле вместо `/*`, вернулся в `nginx.conf` на уровень `server`, спрятан внутрь `location`, переехал совсем (обе половины правила профиля D-150 п. 3 в одном приговоре); приговор сверяется целиком, а не подстрокой, и снятие любой из двух половин `PROD_ONLY` в гейте оставляет прогон красным — проверено откатом правки (3 и 2 случая соответственно); RUN-16 в четырнадцать `run-all.ts` не входит, и перечень стенда стал надмножеством списка гейтов — направление самопроверки T-261 от этого не страдает; HSTS внутри `location` гейт называет снятым набором уровня `server`, а не заголовком прода в образе (`fromNginxConf` берёт только уровень `server`) — случай пинует наблюдаемое поведение, правка гейта за границами задачи; склейка `Cache-Control` — T-236
- 2026-09-24 T-236 `5c88bbb` — негативные случаи RUN-16 на склейку `_headers`: кеш-лестница настоящего файла (`/_astro/*`, `/fonts/*`, `/sw.js`, `/robots.txt`) печатается в ту же фикстуру из второй таблицы рядом с таблицей заголовков T-259, и нарушение вносит один knob — перечень путей, у которых снята строка `! Cache-Control`; три нарушающие сборки красят гейт исходом `fail` вместе с кодом 1: склеивается одно правило, склеиваются все четыре (приговор называет каждое в порядке файла, а перечень случая берётся из той же таблицы, что печатает фикстуру, — новая ступень лестницы войдёт в случай сама) и склеивается не кеш, а сверяемый заголовок — HSTS стоит и в `/*`, и в частном правиле, и приговор называет обе проблемы, «задан вне `/*`» и «склеится», то есть `outsideRoot` и `concatenated` не маскируют друг друга; приговор сверяется целиком, сторожевой случай с полной лестницей остался `pass` вместе с кодом 0, а снятие вызова `concatenated(headersText)` в гейте краснит ровно эти три случая и никакие другие — проверено откатом правки; сам гейт не тронут, случай пинует наблюдаемое поведение; равенство шести общих заголовков по-прежнему без случая — T-264
