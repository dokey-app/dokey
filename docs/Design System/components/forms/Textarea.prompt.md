Payload input. Monospace by default because almost every ToolBox textarea holds machine data.

\`\`\`jsx
<Textarea label="Исходный текст" rows={10} placeholder="Вставьте JSON…" />
\`\`\`

Set `mono={false}` only for genuine prose (feedback form).
