Only icon primitive in the system. Lucide, 1.75 stroke, 16px inside controls, 18–20px in panel headers, 22px in tool cards.

\`\`\`jsx
<Icon name="key-round" size={18} />
\`\`\`

Load `<script src="https://unpkg.com/lucide@0.454.0/dist/umd/lucide.js"></script>` before the bundle. Never inline hand-drawn SVG paths instead.
