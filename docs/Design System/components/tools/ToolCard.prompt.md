Intentional addition — the catalogue tile is the portal's signature object and must be identical everywhere it appears.

\`\`\`jsx
<ToolCard icon={<Icon name="key-round" size={22} />} title="JWT decoder" description="Разбор и проверка подписи токена" category="Безопасность" shortcut="⌘1" />
\`\`\`

Fixed anatomy: soft accent icon tile → title → one-line description → category / shortcut row. Hover lifts 1px with an accent border (from `Card interactive`).
