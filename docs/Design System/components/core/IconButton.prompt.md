Icon-only button — toolbar affordances (copy, theme toggle, fullscreen, kebab menu).

\`\`\`jsx
<IconButton label="Копировать" variant="ghost"><Icon name="copy" /></IconButton>
\`\`\`

Always pass `label`. Default `ghost` sits directly on panel headers; `secondary` when it needs a border against a busy background.
