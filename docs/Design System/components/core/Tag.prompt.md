Pill token for catalogue filters and tool categories.

\`\`\`jsx
<Tag interactive selected>Кодирование</Tag>
\`\`\`

Pill radius here is deliberate — it is the only pill shape in the system, which is what makes filters read as filters. Never use for status (that is `Badge`).
