# T-239 — путь ЭП-06: `/_astro/*` и `/fonts/*` вместо `/assets/`

## Request
- [ ] T-239 — привести путь ЭП-06 к факту (D-181, D-182): api-spec §4.0 и §4.6 называют хешированные ассеты `/assets/{name}.{hash}.{ext}`, а сборка Astro и оба файла заголовков отдают их из `/_astro/*` и `/fonts/*` (найдено в /check T-234 2026-09-22): границы — `docs/api-spec.md` (§4.0 и §4.6); `docs/openapi.yaml` вне границ — его перегенерирует T-245 (D-182 п. 2–3); готово — `grep -rn "/assets/" docs/api-spec.md` ничего не находит, путь ЭП-06 в §4.0 и §4.6 — `/_astro/{name}.{hash}.{ext}` и `/fonts/{семейство}-{начертание}.{hash}.woff2`, то есть ровно правила `/_astro/*` и `/fonts/*` из `infra/headers/_headers`, `pnpm test:unit` зелёный

Links: api-spec §4.0, §4.6 ЭП-06 · D-181 · D-182 · D-107 · T-234 · T-245

## Context
- Docs read: api-spec §4.0 (строка ЭП-06, стр. 400) и §4.6 (стр. 652–669, абзац про шрифт по
  D-181); `infra/headers/_headers` (правила `/_astro/*` и `/fonts/*` — длинный TTL и `immutable`);
  `infra/docker/nginx.conf` (`map $uri`: `~^/_astro/` и `~^/fonts/`); `astro.config.mjs`
  (`build.assets` не задан — каталог по умолчанию `_astro`); `dist/_astro/Base.irFayD0l.css` —
  факт формы имени `{name}.{hash}.{ext}`; D-181 (шрифт — ассет ЭП-06, путь `/fonts/...`);
  план T-234; LESSONS (правка markdown скриптом съедает пустые строки — правим Edit'ом).
- `/assets/` в `docs/` как **контракт** встречается только в api-spec (стр. 400, 652) и
  `openapi.yaml` (стр. 236); architecture, data-model, test-plan его не содержат — правок вне
  границ нет. Как **цитата прошлого состояния** он есть ещё в тексте D-182 (decisions.md), в строках
  T-239 и T-245 в tasks.md, в закрытом Q-246 (open-questions.md) и в этом плане — там он описывает
  найденное расхождение и правке не подлежит.
- **Q-246 закрыт D-182** (2026-09-22): генератора `openapi.yaml` нет, запрет `Edit`/`Write` на
  файл в `.claude/settings.json` не снимается и не обходится, а перегенерацию и весь накопленный
  долг (упразднённый ЭП-08 `/s/script.js` по D-93, отсутствующий ЭП-12 `/robots.txt` по D-164,
  корень по D-91, профиль ЭП-03 по D-163, путь ЭП-06) берёт новая задача **T-245** — генератор
  `scripts/gen-openapi.ts` плюс разовое разрешение человека на запись выводом скрипта. T-239
  сужена до api-spec и с T-245 не связана `блок:`: api-spec — источник, он верен до генерации.
- Decisions applied: D-181 п. 1 — `/fonts/*` входит в ЭП-06; D-182 п. 3 — границы T-239.

## Approach
Путь ЭП-06 — **два префикса, как в `_headers`**: `/_astro/{name}.{hash}.{ext}` (чанки, стили,
спрайт — хеш ставит сборщик) и `/fonts/{семейство}-{начертание}.{hash}.woff2` (хеш пишет сабсеттер,
D-181). Хеш назван так, как его делает каждый источник, — «хеш сборки» в имени `_astro` верен,
для шрифта уже записан D-181. В api-spec правятся строка сводки §4.0 и заголовок операции §4.6;
абзац про шрифт остаётся, меняется только ссылка на то, что это второй путь той же точки.
Rejected:
- один путь `/_astro/*`, шрифты за скобками — противоречит D-181 п. 1 и правилу `/fonts/*` в `_headers`;
- перенести ассеты в `/assets/` настройкой `build.assets` — меняет две копии политики заголовков
  и прод ради совпадения с документом; факт прав, документ нет;
- править `openapi.yaml` здесь — любым способом: точечно руками, скриптом мимо генератора или с
  разовым снятием запрета. Все три отвергнуты D-182: файл выводится генератором из T-245.

## Out of scope
- **`docs/openapi.yaml` целиком** — операция `getAsset` на `/assets/{assetFile}` и остальной дрейф
  (ЭП-08 после D-93, ЭП-12 по D-164, корень по D-91, профиль ЭП-03 по D-163) — предмет **T-245**
  (D-182 п. 2). Ни файл, ни `.claude/settings.json` эта задача не трогает.
- `infra/headers/*`, `infra/docker/nginx.conf`, `astro.config.mjs` — факт верен, не меняется.

## Steps
- [ ] Путь ЭП-06 в api-spec: files `docs/api-spec.md` (§4.0 строка ЭП-06, §4.6 заголовок
  операции и строка «Назначение» — чанк, стиль, спрайт в `/_astro/`, шрифт в `/fonts/`), правка
  инструментом Edit, не `sed`. В ячейку «Метод и путь» §4.0 идут **оба** пути —
  `GET /_astro/{name}.{hash}.{ext}` и `GET /fonts/{семейство}-{начертание}.{hash}.woff2`: T-245
  берёт список путей из этой таблицы, и один префикс уронил бы шрифты из будущей генерации;
  verify: `grep -n "/assets/" docs/api-spec.md` пусто; `grep -n "_astro" docs/api-spec.md` находит
  строку 400 (§4.0) и заголовок операции §4.6; в строке ЭП-06 §4.0 стоят оба пути —
  `sed -n '400p' docs/api-spec.md` показывает и `/_astro/`, и `/fonts/`; `pnpm test:unit -t
  "структура документов"` зелёный
- [ ] Q-246 в таблице «Закрытые вопросы»: files `docs/open-questions.md` (строка Q-246 по образцу
  Q-244 — вопрос, решение D-182, дата) — доделка собственной работы задачи: зачёркнутая строка
  вверху есть, строки в таблице нет; verify: `grep -c "Q-246" docs/open-questions.md` ≥ 3 (сводка
  дат, зачёркнутая строка, таблица закрытых), `pnpm test:unit -t "структура документов"` зелёный
- [ ] Очередь: files `docs/tasks.md` — в графе «готово» T-245 убрать конкретный валидатор
  (`pnpm dlx @redocly/cli lint`: инструмента нет ни в tech-stack, ни в `package.json`), оставить
  требование «проходит валидацию OpenAPI, инструмент выбирает план T-245»; завести строку T-246 о
  долге вне границ — `docs/open-questions.md:36` «Принятые решения: … D-01…D-179» отстала от
  decisions (D-180, D-181, D-182); verify: `grep -n "redocly" docs/tasks.md` пусто,
  `grep -n "T-246" docs/tasks.md` находит одну строку формата соседних
- [ ] Приёмка: files — нет (`- [x]` и строку журнала ставит ship); verify: чек-лист
  `.github/pull_request_template.md` целиком (`pnpm lint`, `format:check`, `typecheck`,
  `test:unit`), команда графы «готово»: `grep -rn "/assets/" docs/api-spec.md` пусто

## Отклонения
- **План одобрен ревью 2026-09-22** (agent `aa4e9530ddcb925e7`, вердикт APPROVE, блокирующих нет);
  замечания-NIT учтены правкой плана перед build.
- По NIT ревью в план добавлены шаги 2 и 3: строка Q-246 в таблице «Закрытые вопросы»
  (доделка собственной работы задачи — зачёркнутая строка у Q-246 есть, строки в таблице нет) и
  две правки очереди — валидатор в графе «готово» T-245 и строка T-246 о долге
  `open-questions.md:36`. Границы задачи из tasks.md этим расширены сознательно, решением
  оркестратора; `docs/openapi.yaml` и `.claude/settings.json` по-прежнему не трогаются.
- Строка про пропущенный в таблице Q-245 не заводится: это не предмет T-239.
- Шаг приёмки больше не ставит `- [x]` и строку журнала — их пишет ship.

## Done when
- `grep -rn "/assets/" docs/api-spec.md` ничего не находит.
- Путь ЭП-06 в api-spec §4.0 и §4.6 — `/_astro/{name}.{hash}.{ext}` и
  `/fonts/{семейство}-{начертание}.{hash}.woff2`, то есть ровно правила `/_astro/*` и `/fonts/*`
  из `infra/headers/_headers` (и `map` в `nginx.conf`).
- `docs/openapi.yaml` не изменён: его перегенерирует T-245 (D-182).
- У Q-246 есть строка в таблице «Закрытые вопросы» `docs/open-questions.md`.
- В графе «готово» T-245 нет невыбранного инструмента валидации; строка T-246 о долге
  `open-questions.md:36` заведена.
- `pnpm test:unit`, `lint`, `format:check`, `typecheck` зелёные.
