# pnpm 12 читает настройки отсюда, а не из поля "pnpm" в манифесте (ADR-04).
# Скрипты установки разрешены поимённо: список — ревьюируемая строка диффа,
# а не молчаливое «разрешить всё» (цепочка поставки, TECH-10).
allowBuilds:
  esbuild: true
  workerd: true
